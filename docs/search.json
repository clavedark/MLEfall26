[
  {
    "objectID": "binaryextensions126.html",
    "href": "binaryextensions126.html",
    "title": "Diagnostics, Classification, and Fit",
    "section": "",
    "text": "We have a fitted binary-response model. This lecture takes on five questions:\n\nIs the link function the right shape? Symmetry, asymmetry, and the scobit alternative.\nHow well does it classify? Thresholds, confusion matrices, ROC curves, AUC \u2014 and why classification is a natural question for a House roll call and an unnatural one for the democratic peace.\nDoes the model estimate? Specifically: is there separation in the data, and if so, what do we do about it?\nWhat about rare events? When \\(y\\) is sparse, the estimator is biased \u2014 by how much, and what to do.\nHow well does it fit? The \u201ctrinity\u201d of tests (LLR, Wald, LM) and the information criteria (AIC, BIC).\n\nWe work the lecture with two running examples, chosen so they disagree with each other.\nThe first is the democratic peace data from Oneal and Russett (1997): dispute (yes/no) regressed on shared border, minimum dyad democracy, capabilities ratio, and an alliance indicator.\n\n\ncode\ndpm &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n           family = binomial(link = \"logit\"), data = dp)\n\nmodelsummary(dpm,\n             coef_rename = c(\"border\" = \"Shared border\",\n                             \"deml\"   = \"Min. democracy\",\n                             \"lncaprat\" = \"Capabilities ratio (log)\",\n                             \"ally\"   = \"Allied\"),\n             gof_omit = \"AIC|BIC|RMSE|F\",\n             notes = \"Baseline democratic-peace model. Used throughout this file.\")\n\n\n\n\n    \n\n    \n    \n      \n        \n        \n              \n                 \n                (1)\n              \n        \n        Baseline democratic-peace model. Used throughout this file.\n        \n                \n                  (Intercept)             \n                  -2.987   \n                \n                \n                                          \n                  (0.107)  \n                \n                \n                  Shared border           \n                  1.036    \n                \n                \n                                          \n                  (0.088)  \n                \n                \n                  Min. democracy          \n                  -0.070   \n                \n                \n                                          \n                  (0.007)  \n                \n                \n                  Capabilities ratio (log)\n                  -0.227   \n                \n                \n                                          \n                  (0.024)  \n                \n                \n                  Allied                  \n                  -0.842   \n                \n                \n                                          \n                  (0.081)  \n                \n                \n                  Num.Obs.                \n                  20990    \n                \n                \n                  Log.Lik.                \n                  -3507.152\n                \n        \n      \n    \n\n\n\nThe second example is a single roll-call vote: the US House vote on NAFTA in November 1993, from Box-Steffensmeier, Arnold, and Zorn (1997). A yes vote is regressed on party and the member\u2019s 1993 AFL-CIO COPE score, a roll-call-based measure of how reliably a member voted with organized labor.\n\n\ncode\nnaftam &lt;- glm(vote ~ democrat + cope93,\n              family = binomial(link = \"logit\"), data = nafta)\n\nmodelsummary(naftam,\n             coef_rename = c(\"democrat\" = \"Democrat\",\n                             \"cope93\"   = \"COPE score (pro-labor)\"),\n             gof_omit = \"AIC|BIC|RMSE|F\",\n             notes = \"US House vote on NAFTA, 1993. y = 1 is a yes vote.\")\n\n\n\n\n    \n\n    \n    \n      \n        \n        \n              \n                 \n                (1)\n              \n        \n        US House vote on NAFTA, 1993. y = 1 is a yes vote.\n        \n                \n                  (Intercept)           \n                  2.380   \n                \n                \n                                        \n                  (0.272) \n                \n                \n                  Democrat              \n                  1.990   \n                \n                \n                                        \n                  (0.504) \n                \n                \n                  COPE score (pro-labor)\n                  -0.054  \n                \n                \n                                        \n                  (0.007) \n                \n                \n                  Num.Obs.              \n                  434     \n                \n                \n                  Log.Lik.              \n                  -229.952\n                \n        \n      \n    \n\n\n\nThe two are deliberately chosen to sit at opposite ends of one dimension: how skewed \\(y\\) is. Disputes occur in 4.5% of dyad-years; the House split 234\u2013200, so \\(\\bar{y} = 0.54\\). Nearly everything in this lecture \u2014 which link to use, whether classification means anything, whether the estimator is biased \u2014 turns on that difference."
  },
  {
    "objectID": "binaryextensions126.html#asymmetric-alternatives",
    "href": "binaryextensions126.html#asymmetric-alternatives",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Asymmetric alternatives",
    "text": "Asymmetric alternatives\nThree CDFs that come up:\n\nLogistic (logit): symmetric, \\(Pr(y=1\\mid x\\beta=0) = 0.5\\), steepest at \\(x\\beta = 0\\).\nComplementary log-log: skewed, \\(Pr(y=1\\mid x\\beta=0) = 1 - e^{-1} \\approx 0.63\\). It is also steepest at \\(x\\beta = 0\\) \u2014 so it moves peak sensitivity up the probability scale, not down. Naturally suited to event-occurrence problems with an \u201cexposure time\u201d interpretation.\nSkewed logit / scobit (Burr-10) (Nagler 1994): \\(Pr(y=1) = \\frac{1}{(1+e^{-x\\beta})^\\alpha}\\). Reduces to logit when \\(\\alpha = 1\\). Otherwise \\(Pr(y=1 \\mid x\\beta = 0) = 2^{-\\alpha}\\), the curve is steepest at \\(x\\beta = \\log \\alpha\\), and the probability at that steepest point is \\((1 + 1/\\alpha)^{-\\alpha}\\). So \\(\\alpha &gt; 1\\) shifts the curve right in \\(x\\beta\\) but moves peak sensitivity down in probability (to 0.44 at \\(\\alpha = 2\\)); \\(\\alpha &lt; 1\\) does the reverse (up to 0.58 at \\(\\alpha = 0.5\\)).\n\n\n\ncode\n# the four links, as functions of the linear predictor\nlinks &lt;- list(\n  \"Logistic\"         = function(z) plogis(z),\n  \"Cloglog\"          = function(z) 1 - exp(-exp(z)),\n  \"Scobit (\u03b1 = 0.5)\" = function(z) 1 / (1 + exp(-z))^0.5,\n  \"Scobit (\u03b1 = 2)\"   = function(z) 1 / (1 + exp(-z))^2\n)\n\n# symmetric numerical derivative: the marginal effect of a unit change in xb\nd_num &lt;- function(f, z, h = 1e-5) (f(z + h) - f(z - h)) / (2 * h)\n\n# grid runs left to -7 so it covers the democratic peace model's fitted values\nxb_grid &lt;- seq(-7, 5, length.out = 600)\n\ncurves &lt;- map_dfr(names(links), function(nm)\n  tibble(link = nm, xb = xb_grid,\n         p  = links[[nm]](xb_grid),\n         me = d_num(links[[nm]], xb_grid)))\n\n# the three landmarks, one row per link\nkey &lt;- map_dfr(names(links), function(nm) {\n  f  &lt;- links[[nm]]\n  pk &lt;- optimize(function(z) d_num(f, z), c(-6, 6), maximum = TRUE)\n  tibble(link    = nm,\n         p_at_0  = f(0),\n         xb_half = uniroot(function(z) f(z) - 0.5, c(-8, 8))$root,\n         xb_peak = pk$maximum,\n         p_peak  = f(pk$maximum),\n         me_peak = pk$objective)\n})\n\n# observed range of the linear predictor in each of the two running models\nbands &lt;- tibble(\n  model = c(\"democratic peace\", \"NAFTA vote\"),\n  lo    = c(min(predict(dpm)), min(predict(naftam))),\n  hi    = c(max(predict(dpm)), max(predict(naftam))))\n\n# draw each observed range as a labelled bar; y positions are set per panel\nrange_bars &lt;- function(ys) {\n  b &lt;- bands %&gt;% mutate(y = ys)\n  list(\n    geom_segment(data = b, aes(x = lo, xend = hi, y = y, yend = y),\n                 inherit.aes = FALSE, color = \"grey25\", linewidth = 2.2,\n                 lineend = \"butt\"),\n    geom_text(data = b, aes(x = lo, y = y, label = model), inherit.aes = FALSE,\n              hjust = 0, vjust = -0.9, size = 2.9, color = \"grey15\")\n  )\n}\n\ncols &lt;- c(\"Logistic\"         = \"#005A43\",\n          \"Cloglog\"          = \"#6CC24A\",\n          \"Scobit (\u03b1 = 0.5)\" = \"#A7DA92\",\n          \"Scobit (\u03b1 = 2)\"   = \"#000000\")\n\np_cdf &lt;- ggplot(curves, aes(xb, p, color = link, linetype = link)) +\n\n  geom_hline(yintercept = 0.5, color = \"grey60\", linetype = \"dotted\") +\n  geom_vline(xintercept = 0,   color = \"grey60\", linetype = \"dotted\") +\n  geom_line(linewidth = 0.8) +\n  geom_point(data = key, aes(x = 0, y = p_at_0, color = link),\n             inherit.aes = FALSE, size = 2.4, show.legend = FALSE) +\n  geom_point(data = key, aes(x = xb_half, y = 0.5, color = link),\n             inherit.aes = FALSE, size = 2.6, shape = 1, stroke = 1.1,\n             show.legend = FALSE) +\n  range_bars(c(0.90, 0.79)) +\n  annotate(\"text\", x = -6.9, y = 1.0, hjust = 0, size = 2.9, color = \"grey15\",\n           label = \"observed range of x-beta:\") +\n  scale_color_manual(values = cols) +\n  labs(subtitle = \"(a) The curves. Filled dots: Pr(y=1) at x\u03b2 = 0. Open circles: where each curve crosses 0.5.\",\n       x = NULL, y = expression(Pr(y == 1)), color = NULL, linetype = NULL) +\n  theme_minimal()\n\np_me &lt;- ggplot(curves, aes(xb, me, color = link, linetype = link)) +\n\n  geom_vline(xintercept = 0, color = \"grey60\", linetype = \"dotted\") +\n  geom_line(linewidth = 0.8) +\n  geom_point(data = key, aes(x = xb_peak, y = me_peak, color = link),\n             inherit.aes = FALSE, size = 2.4, show.legend = FALSE) +\n  range_bars(c(0.445, 0.405)) +\n  expand_limits(y = 0.48) +\n  scale_color_manual(values = cols) +\n  labs(subtitle = \"(b) The slopes of those curves \u2014 the marginal effect. Dots mark each peak.\",\n       x = expression(x*beta),\n       y = expression(partialdiff * Pr(y == 1) / partialdiff * (x*beta)),\n       color = NULL, linetype = NULL) +\n  theme_minimal()\n\n(p_cdf / p_me) +\n  plot_layout(guides = \"collect\") +\n  plot_annotation(\n    title = \"Symmetry decides where the model is most sensitive\",\n    subtitle = \"Logistic is symmetric; cloglog and scobit (\u03b1 \u2260 1) are not.\"\n  ) &\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure\u00a01\n\n\n\n\n\nRead the two panels together.1 Panel (a) is the familiar picture of four CDFs. Panel (b) is the one that matters for interpretation: it plots the slope of each curve, \\(\\partial Pr(y=1) / \\partial(x\\beta)\\), which is the marginal effect of a one-unit change in the linear predictor. The link function\u2019s shape is an assumption about the top panel; what you actually report to a reader is the bottom panel.\nThree things to take from it.\nThe peak is in a different place for every link. The logistic peaks at \\(x\\beta = 0\\) with a slope of 0.25 \u2014 the familiar \\(\\pi(1-\\pi)\\) maximum. Cloglog peaks at \\(x\\beta = 0\\) too, but its peak is taller (0.37) and it sits at \\(\\widehat{\\pi} = 0.63\\), not 0.5. The two scobits move the peak off the origin entirely: \\(\\alpha = 2\\) peaks at \\(x\\beta = 0.69\\), \\(\\alpha = 0.5\\) at \\(x\\beta = -0.69\\). Same estimated \\(x\\beta\\), four different answers to \u201chow much does a one-unit change buy you?\u201d\n\\(x\\beta = 0\\) means something different in each model. The filled dots in panel (a) sit at four different heights: 0.5 for logit, 0.63 for cloglog, 0.71 for scobit with \\(\\alpha = 0.5\\), and 0.25 for scobit with \\(\\alpha = 2\\). Zero on the linear predictor is not a natural threshold; it is only a threshold in the symmetric case, and only because we chose a symmetric link.\n\\(\\widehat{\\pi} = 0.5\\) arrives at a different \\(x\\beta\\) in each model. The open circles in panel (a) sit at \\(x\\beta\\) of 0, \\(-0.37\\), \\(-1.10\\), and \\(0.88\\). This is why the \\(\\tau = 0.5\\) classification threshold we use later is a statement about the link as much as about the data.\n\n\ncode\nkey %&gt;%\n  transmute(Link = link,\n            `Pr(y=1) at x\u03b2=0`        = p_at_0,\n            `x\u03b2 where Pr(y=1)=0.5`   = xb_half,\n            `x\u03b2 at steepest point`   = xb_peak,\n            `Pr(y=1) there`          = p_peak,\n            `slope there`            = me_peak) %&gt;%\n  knitr::kable(digits = 3,\n               caption = \"The three landmarks symmetry ties together, pulled apart.\")\n\n\n\nThe three landmarks symmetry ties together, pulled apart.\n\n\n\n\n\n\n\n\n\n\nLink\nPr(y=1) at x\u03b2=0\nx\u03b2 where Pr(y=1)=0.5\nx\u03b2 at steepest point\nPr(y=1) there\nslope there\n\n\n\n\nLogistic\n0.500\n0.000\n0.000\n0.500\n0.250\n\n\nCloglog\n0.632\n-0.366\n0.000\n0.632\n0.368\n\n\nScobit (\u03b1 = 0.5)\n0.707\n-1.099\n-0.693\n0.577\n0.192\n\n\nScobit (\u03b1 = 2)\n0.250\n0.881\n0.693\n0.444\n0.296"
  },
  {
    "objectID": "binaryextensions126.html#skewed-y-and-where-the-data-actually-are",
    "href": "binaryextensions126.html#skewed-y-and-where-the-data-actually-are",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Skewed \\(y\\), and where the data actually are",
    "text": "Skewed \\(y\\), and where the data actually are\nNow put the running example on top of that picture. Disputes occur in about 4.5% of dyad-years, and the baseline logit\u2019s fitted probabilities run from 0.002 to 0.22: not one of the 20,990 dyad-years gets a predicted probability above a quarter, and \\(x\\widehat{\\beta}\\) never gets closer to zero than \\(-1.26\\). The shaded band marked democratic peace in Figure\u00a01 is that entire estimation sample \u2014 every observation, all of it, off to the left.\nLook at where the band falls in panel (b). All four marginal-effect curves are pressed flat against the floor there. Every peak in the figure sits to the right of every observation in the data. So the model\u2019s claim about where cases are most responsive to a change in democracy is being made about a region of the covariate space that contains no dyad-years at all. That claim is not estimated; it is implied by functional form. Skewness in \\(y\\) is what does this \u2014 it pushes the whole estimation sample into one tail, and leaves the interesting part of the curve to be filled in by assumption.\nIt is tempting to conclude that the fix is an asymmetric link that drags the peak down into the tail where the data live. Panel (b) says that only half works. Cloglog moves the peak the wrong way, up to \\(\\widehat{\\pi} = 0.63\\). Scobit can move it down, but only so far: the probability at the steepest point is \\((1 + 1/\\alpha)^{-\\alpha}\\), which is bounded below by \\(1/e \\approx 0.37\\) no matter how large you make \\(\\alpha\\). None of these links can put peak sensitivity anywhere near a base rate of 0.045. What an asymmetric link genuinely buys you is a different shape in the tail \u2014 how fast \\(Pr(y=1)\\) climbs off the floor as \\(x\\beta\\) rises \u2014 and since the data are all in that tail, this is where the links can actually disagree with each other about the cases you have.\nThe scobit\u2019s \\(\\alpha\\) parameter is the empirical version of \u201cwhere is the transition between zeros and ones?\u201d \u2014 rather than asserting it sits at \\(\\widehat{\\pi} = 0.5\\), scobit estimates it. The model rarely appears in print, partly because the likelihood is sometimes ill-behaved (changes in the slope \\(\\beta\\) and in \\(\\alpha\\) can look similar to the optimizer) and partly because in most political-science data the substantive answer is similar to plain logit. Where the scobit\u2019s logic does push, cloglog usually does too, and cloglog is much easier to estimate."
  },
  {
    "objectID": "binaryextensions126.html#and-when-y-is-balanced",
    "href": "binaryextensions126.html#and-when-y-is-balanced",
    "title": "Diagnostics, Classification, and Fit",
    "section": "And when \\(y\\) is balanced",
    "text": "And when \\(y\\) is balanced\nRun the same exercise on the NAFTA model and the picture inverts. The House split 234\u2013200, the fitted probabilities run from 0.05 to 0.99, and the second shaded band in Figure\u00a01 \u2014 the range of \\(x\\widehat{\\beta}\\) in that model \u2014 covers every peak in panel (b) rather than sitting to the left of all of them.\nThat is the case where a symmetric link is not just harmless but appropriate. The logit\u2019s assumption is that the model is most sensitive around \\(\\widehat{\\pi} = 0.5\\), and here a third of the House actually sits there. The assumption is being made about a region of the covariate space that is densely populated, so it is doing work the data can speak to rather than filling in a gap by functional form. We come back to these members in the classification section, where they turn out to be the ones a whip would care about.\nPut the two bands together and you have the practical rule. Symmetry is a claim about where the action is. Check whether your data are anywhere near there before you worry about the link, and if they are not \u2014 as with any rare-events application \u2014 understand that you are extrapolating whichever link you pick."
  },
  {
    "objectID": "binaryextensions126.html#same-model-different-links",
    "href": "binaryextensions126.html#same-model-different-links",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Same model, different links",
    "text": "Same model, different links\nThe pragmatic check is to refit with multiple links and look at how much the substantive story changes.\n\n\ncode\nfit_logit   &lt;- dpm\nfit_probit  &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n                   family = binomial(link = \"probit\"),  data = dp)\nfit_cloglog &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n                   family = binomial(link = \"cloglog\"), data = dp)\n\nmodelsummary(\n  list(\"Logit\" = fit_logit, \"Probit\" = fit_probit, \"Cloglog\" = fit_cloglog),\n  coef_rename = c(\"border\" = \"Shared border\",\n                  \"deml\"   = \"Min. democracy\",\n                  \"lncaprat\" = \"Capabilities ratio (log)\",\n                  \"ally\"   = \"Allied\"),\n  gof_omit = \"RMSE|F\"\n)\n\n\n\n\n    \n\n    \n    \n      \n        \n        \n              \n                 \n                Logit\n                Probit\n                Cloglog\n              \n        \n        \n        \n                \n                  (Intercept)             \n                  -2.987   \n                  -1.653   \n                  -3.022   \n                \n                \n                                          \n                  (0.107)  \n                  (0.048)  \n                  (0.104)  \n                \n                \n                  Shared border           \n                  1.036    \n                  0.491    \n                  0.993    \n                \n                \n                                          \n                  (0.088)  \n                  (0.041)  \n                  (0.085)  \n                \n                \n                  Min. democracy          \n                  -0.070   \n                  -0.031   \n                  -0.068   \n                \n                \n                                          \n                  (0.007)  \n                  (0.003)  \n                  (0.007)  \n                \n                \n                  Capabilities ratio (log)\n                  -0.227   \n                  -0.101   \n                  -0.218   \n                \n                \n                                          \n                  (0.024)  \n                  (0.011)  \n                  (0.023)  \n                \n                \n                  Allied                  \n                  -0.842   \n                  -0.371   \n                  -0.806   \n                \n                \n                                          \n                  (0.081)  \n                  (0.038)  \n                  (0.077)  \n                \n                \n                  Num.Obs.                \n                  20990    \n                  20990    \n                  20990    \n                \n                \n                  AIC                     \n                  7024.3   \n                  7032.7   \n                  7026.1   \n                \n                \n                  BIC                     \n                  7064.1   \n                  7072.5   \n                  7065.9   \n                \n                \n                  Log.Lik.                \n                  -3507.152\n                  -3511.346\n                  -3508.056\n                \n        \n      \n    \n\n\n\nCoefficient magnitudes differ \u2014 logit and probit estimates are on different scales by construction, and cloglog is on yet another \u2014 but signs, significance, and predicted-probability conclusions track each other closely. That\u2019s the typical pattern. The substantive choice between links matters more for what the model implies out of sample than for what it says about the cases at hand.\n\n\n\n\n\n\nImportant Concept\n\n\n\nSymmetric vs.\u00a0asymmetric is a modeling choice with an unambiguous empirical content: where on the covariate space is the model most sensitive? A symmetric link puts that peak sensitivity at \\(\\widehat{\\pi}=0.5\\), at \\(x\\beta=0\\), and makes \\(\\widehat{\\pi}=0.5\\) arrive at \\(x\\beta=0\\) \u2014 all three at once. An asymmetric link separates them. With rare-event \\(y\\) none of these links can move peak sensitivity anywhere near the base rate, so the choice does not rescue you from extrapolating; what it changes is the shape of the curve in the tail where your data actually live, and that shows up out of sample rather than in the fitted values."
  },
  {
    "objectID": "binaryextensions126.html#who-classifies",
    "href": "binaryextensions126.html#who-classifies",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Who classifies",
    "text": "Who classifies\nWe mostly don\u2019t use these models for classification in political science, but classification is the native use of binomial models like the logit in many other places. A spam filter estimates \\(Pr(\\text{spam} \\mid \\text{message})\\) and then has to do something: the message goes to the inbox or to the spam folder, and there is no third option. A screening test estimates \\(Pr(\\text{disease} \\mid \\text{markers})\\) and the patient is called back or sent home. A bank estimates \\(Pr(\\text{default})\\) and extends the loan or doesn\u2019t. In each case somebody has to act on the probability, the action is binary, and the two kinds of error cost very different amounts \u2014 a spam message in your inbox is an annoyance, a real message in your spam folder is a missed job offer. That asymmetry in costs is the whole reason \\(\\tau\\) exists. The threshold is where you decide the probability is high enough to act on, and it belongs to the decision problem, not to the model.\nThe vocabulary comes from the same world. ROC stands for receiver operating characteristic, and the story usually told is that it originates with radar receiver operators in the Second World War, who had to decide whether a blip on the scope was enemy aircraft or flocks of geese (so signal or noise). Whether or not the anecdote is exact, the method it describes \u2014 try every threshold, plot the true-positive rate against the false-positive rate at every threshold, and read the quality of the model off the shape of the curve \u2014 was formalized after the war as signal detection theory (Green and Swets 1966). It moved into radiology and clinical decision-making in the 1970s, became standard equipment in machine learning, and reached political science late, largely through people arguing that our conflict models should be asked whether they predict anything at all (Ward, Greenhill, and Bakke 2010).\nSo why is this mostly not what we do? Because in most of our applications nobody acts on the individual case. We fit a democratic peace model to learn whether joint democracy is associated with fewer disputes; nobody is deciding what to do about dyad 4,712. The estimand is a coefficient or a marginal effect, and a model can be genuinely useful for that while being useless as a classifier. Keep that distinction in view for the rest of this section: a bad classifier is not automatically a bad model.\nBut \u201cmostly\u201d is not \u201cnever.\u201d Below we work two examples deliberately chosen to sit at opposite ends \u2014 a House roll-call vote, where \\(y\\) is nearly balanced and legislative leaders really were trying to sort members into categories, and the democratic peace data, where \\(y\\) is rare and nobody is sorting anything."
  },
  {
    "objectID": "binaryextensions126.html#confusion-matrix",
    "href": "binaryextensions126.html#confusion-matrix",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Confusion matrix",
    "text": "Confusion matrix\nLet\u2019s start by classifying the correct and incorrect predictions from a binomial model. To do this, we cross-tabulate observed \\(y\\) against predicted \\(\\widehat{y}\\) at a fixed threshold, \\(\\tau\\) - usually \\(\\tau\\) is fixed to .5, the symmetry point in the logit or probit CDFs. The result is the confusion matrix (I didn\u2019t make this up):\n\n\n\n\n\n\nPredicted.Positive\nPredicted.Negative\nRate\n\n\n\n\nObserved Positive (y = 1)\nTrue Positive (TP)\nFalse Negative (FN)\nTPR = TP / (TP + FN)\n\n\nObserved Negative (y = 0)\nFalse Positive (FP)\nTrue Negative (TN)\nFPR = FP / (FP + TN)\n\n\n\n\n\nTwo rates that get heavy use:\n\nTrue positive rate (TPR) = sensitivity. Of all the actual ones, what fraction does the model catch?\nFalse positive rate (FPR) = 1 \u2212 specificity. Of all the actual zeros, what fraction does the model mistakenly call ones?\n\nFrom these we build summary fit measures.\n\nPercent correctly predicted (PCP)\n\\[\n\\text{PCP} = \\frac{TP + TN}{N}\n\\]\nThe fraction of the sample classified correctly.\n\n\nProportional reduction of error (PRE)\nA null model that always predicts the modal category gets PMC (\u201cpercent in the modal category\u201d) correct. The PRE asks how much better the informed model does:\n\\[\n\\text{PRE} = \\frac{\\text{PCP} - \\text{PMC}}{1 - \\text{PMC}}\n\\]\nPRE goes to 1 for a perfect classifier and to 0 for a model that doesn\u2019t beat the null.\nThat is five acronyms in two pages, and they pile up fast. Keep this table somewhere you can glance at \u2014 the rest of the section restates them as it goes, but here they are in one place:\n\n\n\n\n\n\n\n\n\nstands for\nwhat it asks\n\n\n\n\nTP / FP / TN / FN\ntrue/false positive, true/false negative\nthe four cells of the confusion matrix\n\n\nTPR\ntrue positive rate, a.k.a. sensitivity\nof the real ones, what share did we catch?\n\n\nFPR\nfalse positive rate, a.k.a. 1 \u2212 specificity\nof the real zeros, what share did we wrongly call ones?\n\n\nPCP\npercent correctly predicted\nwhat share of the whole sample did we get right?\n\n\nPMC\npercent in the modal category\nwhat would we get right by always guessing the more common outcome?\n\n\nPRE\nproportional reduction of error\nhow much of the null model\u2019s error did the covariates remove?\n\n\n\nOnly the last one is really a measure of the model, because it is the only one that compares the model to an alternative."
  },
  {
    "objectID": "binaryextensions126.html#a-balanced-y-the-nafta-vote-1993",
    "href": "binaryextensions126.html#a-balanced-y-the-nafta-vote-1993",
    "title": "Diagnostics, Classification, and Fit",
    "section": "A balanced \\(y\\): the NAFTA vote, 1993",
    "text": "A balanced \\(y\\): the NAFTA vote, 1993\nOn 17 November 1993 the US House voted 234\u2013200 to approve NAFTA. The vote cut across both parties, which is exactly what makes it useful here: \\(\\bar{y} = 0.54\\), so there is no rare-events problem to fight through and the default \\(\\tau = 0.5\\) is not obviously silly. The model is the one estimated at the top of this file \u2014 a yes vote on party and the member\u2019s COPE score. Labor opposed NAFTA, so the negative COPE coefficient is the expected sign. The party coefficient deserves a second look, because it runs against the raw numbers: Democrats voted against NAFTA 153 to 101, yet the coefficient on democrat is positive and large. Both facts are true, and the model is telling you something the cross-tabulation cannot. Democrats were, on average, far more pro-labor, and it was the labor position that predicted a no vote. Holding the COPE score fixed \u2014 comparing a Democrat and a Republican with the same labor voting record \u2014 the Democrat was more likely to vote yes. This is the ordinary difference between a marginal association and a conditional one, and it is worth pausing on: the sign of a coefficient is a statement about a comparison you have to specify, not a summary of who voted which way.\n\n\ncode\np_nafta &lt;- predict(naftam, type = \"response\")\ny_nafta_50 &lt;- as.integer(p_nafta &gt;= 0.5)\n\ncm_nafta &lt;- confusionMatrix(factor(y_nafta_50, levels = c(0, 1)),\n                            factor(nafta$vote, levels = c(0, 1)),\n                            positive = \"1\")\ncm_nafta$table\n\n\n          Reference\nPrediction   0   1\n         0 160  77\n         1  40 157\n\n\ncode\ntibble(\n  PCP = unname(cm_nafta$overall[\"Accuracy\"]),\n  PMC = max(mean(nafta$vote), 1 - mean(nafta$vote)),\n  PRE = (unname(cm_nafta$overall[\"Accuracy\"]) -\n           max(mean(nafta$vote), 1 - mean(nafta$vote))) /\n        (1 - max(mean(nafta$vote), 1 - mean(nafta$vote)))\n) %&gt;%\n  knitr::kable(digits = 3, caption = \"PCP, PMC, PRE for the NAFTA model at $\\\\tau = 0.5$\")\n\n\n\nPCP, PMC, PRE for the NAFTA model at \\(\\tau = 0.5\\)\n\n\nPCP\nPMC\nPRE\n\n\n\n\n0.73\n0.539\n0.415\n\n\n\n\n\nBoth diagonals are populated \u2014 the model calls some members yes and some no, and gets most of both right. PCP (percent correctly predicted) is 0.73. PMC \u2014 the percent in the modal category, which is what a null model gets by always guessing \u201cyes\u201d \u2014 is 0.54. So PRE, the proportional reduction of error, is 0.42: the covariates removed about 42% of the errors the null model would have made. These numbers mean something here, and the reason they mean something is that \\(y\\) is balanced. Hold onto the comparison; the same three numbers computed on the democratic peace data in a moment will look better and say less."
  },
  {
    "objectID": "binaryextensions126.html#classification-with-a-purpose-whip-counts",
    "href": "binaryextensions126.html#classification-with-a-purpose-whip-counts",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Classification with a purpose: whip counts",
    "text": "Classification with a purpose: whip counts\nThis is where classification stops being an import from machine learning and becomes a question a political scientist might actually ask.\nIn the weeks before the vote, the leadership on both sides \u2014 and the Clinton White House, which was whipping hard for the treaty \u2014 ran whip counts: for each member, is this a yes, a no, or someone in play? That is a classification problem, and it is one where the threshold does real work, because a whip has finite resources. A committee assignment, a project in the district, an afternoon of the President\u2019s time: each can be spent once, and the question is on whom.\nNow look at where the model says those members are.\n\n\ncode\ntibble(\n  `in play (0.3 - 0.7)` = sum(p_nafta &gt;= 0.3 & p_nafta &lt;= 0.7),\n  `likely no (&lt; 0.3)`   = sum(p_nafta &lt; 0.3),\n  `likely yes (&gt; 0.7)`  = sum(p_nafta &gt; 0.7),\n  N                     = length(p_nafta)\n) %&gt;%\n  knitr::kable(caption = \"Members of the House by predicted probability of a yes vote.\")\n\n\n\nMembers of the House by predicted probability of a yes vote.\n\n\nin play (0.3 - 0.7)\nlikely no (&lt; 0.3)\nlikely yes (&gt; 0.7)\nN\n\n\n\n\n155\n128\n151\n434\n\n\n\n\n\n155 of 434 members \u2014 about a third of the House \u2014 have a predicted probability between 0.3 and 0.7. Those are the members worth a phone call. And notice what the symmetry section already told us about that region: it is precisely where the logit\u2019s marginal effect is largest. A member sitting at \\(\\widehat{\\pi} = 0.5\\) is the member whose probability moves most for a given inducement; the member at 0.95 was going to vote yes anyway, and the member at 0.05 is not reachable at any price the leadership can pay.\nSo for this application the symmetric logit\u2019s central assumption \u2014 that the model is most sensitive at \\(\\widehat{\\pi} = 0.5\\) \u2014 is defensible, and close to the substantive question being asked. The link and the problem line up."
  },
  {
    "objectID": "binaryextensions126.html#where-the-predicted-probabilities-actually-fall",
    "href": "binaryextensions126.html#where-the-predicted-probabilities-actually-fall",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Where the predicted probabilities actually fall",
    "text": "Where the predicted probabilities actually fall\nThe contrast with the running example is the whole point, and it is easiest to see by putting the two distributions of predicted probabilities side by side.\n\n\ncode\np_hat &lt;- predict(dpm, type = \"response\")   # fitted probabilities, democratic peace model\n\nphat_df &lt;- bind_rows(\n  tibble(model = \"NAFTA vote (y-bar = 0.54)\",          phat = p_nafta),\n  tibble(model = \"Democratic peace (y-bar = 0.045)\",   phat = p_hat)\n) %&gt;%\n  mutate(model = factor(model, levels = c(\"NAFTA vote (y-bar = 0.54)\",\n                                          \"Democratic peace (y-bar = 0.045)\")))\n\nggplot(phat_df, aes(phat)) +\n  geom_histogram(bins = 40, fill = \"#005A43\", color = NA) +\n  geom_vline(xintercept = 0.5, linetype = \"dashed\", color = \"grey20\") +\n  facet_wrap(~ model, scales = \"free_y\") +\n  scale_x_continuous(limits = c(0, 1)) +\n  labs(title = \"Where the fitted probabilities actually sit\",\n       subtitle = \"Dashed line is the default threshold, tau = 0.5.\",\n       x = expression(hat(pi)), y = \"members / dyad-years\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a02\n\n\n\n\n\nOn the left, the fitted probabilities spread across the whole unit interval and 45% of members land above 0.5, so a threshold at 0.5 partitions the sample into two usable groups. On the right, the entire distribution is jammed against the left wall: the largest fitted probability in the democratic peace model is 0.22, so no observation crosses 0.5 at all. Classifying at \\(\\tau = 0.5\\) there does not produce a bad partition; it produces no partition. That is the next section."
  },
  {
    "objectID": "binaryextensions126.html#democratic-peace-at-tau-0.5-the-rare-events-trap",
    "href": "binaryextensions126.html#democratic-peace-at-tau-0.5-the-rare-events-trap",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Democratic peace at \\(\\tau = 0.5\\) \u2014 the rare-events trap",
    "text": "Democratic peace at \\(\\tau = 0.5\\) \u2014 the rare-events trap\nThe dispute variable is rare. Let\u2019s see what happens at the default \\(\\tau = 0.5\\).\n\n\ncode\ny_hat_50 &lt;- as.integer(p_hat &gt;= 0.5)\n\ncm_50 &lt;- confusionMatrix(factor(y_hat_50, levels = c(0,1)),\n                         factor(dp$dispute, levels = c(0,1)),\n                         positive = \"1\")\ncm_50$table\n\n\n          Reference\nPrediction     0     1\n         0 20043   947\n         1     0     0\n\n\nAt \\(\\tau = 0.5\\) the model classifies almost nothing as a one. The base rate of dispute is roughly 4\u20135%, so with a symmetric logit, \\(\\widehat{\\pi}_i\\) rarely crosses 0.5. The percent correctly predicted (PCP) looks great \u2014 but only because predicting \u201cno dispute\u201d for everyone is right 95% of the time, and that is exactly what the percent in the modal category (PMC) measures:\n\n\ncode\ntibble(\n  PCP = unname(cm_50$overall[\"Accuracy\"]),\n  PMC = mean(dp$dispute == 0),\n  PRE = (unname(cm_50$overall[\"Accuracy\"]) - mean(dp$dispute == 0)) /\n        (1 - mean(dp$dispute == 0))\n) %&gt;%\n  knitr::kable(digits = 3, caption = \"PCP, PMC, PRE at $\\\\tau = 0.5$\")\n\n\n\nPCP, PMC, PRE at \\(\\tau = 0.5\\)\n\n\nPCP\nPMC\nPRE\n\n\n\n\n0.955\n0.955\n0\n\n\n\n\n\nPCP is high and PRE is near zero, and the second number is the one that matters: the proportional reduction of error compares the model to the null model, and here the model removes essentially none of the null model\u2019s mistakes. It is no better than always guessing \u201cno dispute.\u201d\nNotice what did the damage. Nothing about the coefficients changed between the NAFTA example and this one \u2014 the estimates are the same estimates we would report in a paper. What changed is that \\(\\widehat{\\pi}\\) never reaches the threshold, so every case falls on the same side of it. That is a fact about \\(\\tau\\) and the distribution of \\(\\widehat{\\pi}\\), not about whether the model has learned anything."
  },
  {
    "objectID": "binaryextensions126.html#democratic-peace-at-tau-0.1-lower-the-bar",
    "href": "binaryextensions126.html#democratic-peace-at-tau-0.1-lower-the-bar",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Democratic peace at \\(\\tau = 0.1\\) \u2014 lower the bar",
    "text": "Democratic peace at \\(\\tau = 0.1\\) \u2014 lower the bar\nDrop \\(\\tau\\) to where the model can actually classify some ones, and the picture changes:\n\n\ncode\ny_hat_10 &lt;- as.integer(p_hat &gt;= 0.10)\n\ncm_10 &lt;- confusionMatrix(factor(y_hat_10, levels = c(0,1)),\n                         factor(dp$dispute, levels = c(0,1)),\n                         positive = \"1\")\ncm_10$table\n\n\n          Reference\nPrediction     0     1\n         0 18343   660\n         1  1700   287\n\n\ncode\ntibble(\n  PCP = unname(cm_10$overall[\"Accuracy\"]),\n  TPR = unname(cm_10$byClass[\"Sensitivity\"]),\n  FPR = 1 - unname(cm_10$byClass[\"Specificity\"])\n) %&gt;%\n  knitr::kable(digits = 3, caption = \"PCP, TPR, FPR at $\\\\tau = 0.1$\")\n\n\n\nPCP, TPR, FPR at \\(\\tau = 0.1\\)\n\n\nPCP\nTPR\nFPR\n\n\n\n\n0.888\n0.303\n0.085\n\n\n\n\n\nPCP falls \u2014 we\u2019re now wrong about some of the zeros \u2014 but TPR, the true positive rate, rises sharply: of the dyad-years that really did see a dispute, the model now catches a meaningful share. The right \\(\\tau\\) depends on what we care about: in a peer-reviewed paper we usually don\u2019t pick one at all and let the ROC curve do the work.\n\n\n\n\n\n\nImportant Concept\n\n\n\nThe threshold \\(\\tau\\) is a decision-theoretic choice, not a statistical one. It depends on the cost ratio of false positives to false negatives \u2014 costs the model doesn\u2019t know about. Reporting fit at a single \\(\\tau\\) implicitly takes a stand on those costs."
  },
  {
    "objectID": "binaryextensions126.html#roc-curves",
    "href": "binaryextensions126.html#roc-curves",
    "title": "Diagnostics, Classification, and Fit",
    "section": "ROC curves",
    "text": "ROC curves\nROC stands for receiver operating characteristic \u2014 the phrase comes from the radar operators of the introduction, and \u201creceiver\u201d means the radar set, not the analyst. The idea is the one we have been sneaking up on: rather than pick a single threshold and report the classification it produces, let\u2019s do this for all values of \\(\\tau\\) across its whole range, record the pair of error rates at each value, and plot the result. Every point on an ROC curve is a threshold value of \\(\\tau\\).\nThe two axes have unhelpful names, so let\u2019s translate them into the radar language they came from.\n\nSensitivity = TPR = the detection rate. Of the cases that really are ones, what share does the model call ones? A radar with high sensitivity spots the incoming aircraft. Turn the threshold down far enough and you detect everything \u2014 you also call everything an aircraft, which is the catch.\nSpecificity = TN / (TN + FP) = 1 \u2212 FPR = the share of real zeros correctly left alone. Its complement, the false positive rate, is the false alarm rate: of the cases that really are zeros, what share does the model wrongly call ones? A radar with low specificity is constantly scrambling fighters to meet flocks of geese.\n\nThe tension between them is the point. You can always get the detection rate to 1 by lowering \\(\\tau\\) to zero, and you can always get the false alarm rate to 0 by raising \\(\\tau\\) to one. Neither is a detector. What you want to know is how good the trade off between the two is \u2014 how much detection you buy per unit of false alarm \u2014 and the shape of the ROC curve is the answer.\nWe plot the false alarm rate on the horizontal axis and the detection rate on the vertical, and we build the curves by hand from the pROC objects so the coordinates are visible:\n\n\ncode\nset.seed(20260907)   # chunk-level seed: this chunk simulates\n\n# pull (false alarm rate, detection rate) pairs out of a pROC object\nroc_df &lt;- function(r, label) {\n  tibble(model = label,\n         fpr = 1 - r$specificities,   # false alarm rate\n         tpr = r$sensitivities)       # detection rate\n}\n\n# four detectors of increasing quality on the same simulated outcome\nn  &lt;- 4000\ny  &lt;- rbinom(n, 1, 0.5)\nmk &lt;- function(d) plogis(rnorm(n, mean = d * y, sd = 1) - d / 2)\n\nscores &lt;- list(\"no better than chance\" = mk(0),\n               \"good\"                  = mk(0.75),\n               \"better\"                = mk(2),\n               \"perfect\"               = ifelse(y == 1, 0.999, 0.001))\n\nrocs    &lt;- imap(scores, ~ roc(y, .x, quiet = TRUE))\nlabels  &lt;- imap_chr(rocs, ~ sprintf(\"%s (AUC = %.2f)\", .y, auc(.x)))\ncurves  &lt;- imap_dfr(rocs, ~ roc_df(.x, labels[[.y]])) %&gt;%\n  mutate(model = factor(model, levels = labels))   # keep worst-to-best order, not alphabetical\n\n# three thresholds marked on the \"good\" curve: each tau is one point on the curve\nmarks &lt;- map_dfr(c(0.8, 0.5, 0.2), function(tau) {\n  yhat &lt;- as.integer(scores[[\"good\"]] &gt;= tau)\n  tibble(tau = tau,\n         fpr = mean(yhat[y == 0] == 1),\n         tpr = mean(yhat[y == 1] == 1))\n})\n\nggplot(curves, aes(fpr, tpr, color = model)) +\n  geom_abline(intercept = 0, slope = 1, color = \"grey60\", linetype = \"dashed\") +\n  geom_path(linewidth = 0.8) +\n  geom_point(data = marks, aes(fpr, tpr), inherit.aes = FALSE,\n             size = 2.6, color = \"grey15\") +\n  geom_text(data = marks, aes(fpr, tpr, label = paste0(\"tau = \", tau)),\n            inherit.aes = FALSE, hjust = -0.25, vjust = -1.1,\n            size = 3.0, color = \"grey15\") +\n  scale_color_manual(values = setNames(c(\"#BDBEBD\", \"#A7DA92\", \"#6CC24A\", \"#005A43\"),\n                                       labels)) +\n  coord_equal(xlim = c(0, 1.08), ylim = c(0, 1.06)) +\n  labs(title = \"What ROC curves look like, and what a point on one means\",\n       subtitle = \"Dots mark three thresholds on the 'good' curve. Lowering tau moves you up and to the right.\",\n       x = \"False alarm rate (FPR = 1 - specificity)\",\n       y = \"Detection rate (TPR = sensitivity)\",\n       color = NULL) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a03\n\n\n\n\n\nTake a look at the marked points on the middle curve first, because they illustrate the main idea. At \\(\\tau = 0.8\\) the model is cautious: it raises almost no false alarms (FPR of 0.03) but detects only 15% of the real ones. Drop to \\(\\tau = 0.5\\) and detection jumps to 64%, at the cost of a 35% false alarm rate. Drop again to \\(\\tau = 0.2\\) and you detect 97% of the ones \u2014 while falsely flagging 84% of the zeros. Same model, same coefficients, same fitted probabilities; three completely different classifiers. Sliding \\(\\tau\\) from 1 down to 0 traces out the whole curve, from the bottom-left corner to the top-right.\nThat is why the curve, rather than any single point on it, is the summary of the model. Once you see it this way the rest reads off directly:\n\nThe dashed 45\u00b0 line is a detector with no information: to detect 60% of the ones it must falsely flag 60% of the zeros. Any useful model lies above it.\nThe top-left corner is a perfect classifier \u2014 everything detected, nothing falsely flagged \u2014 so curves that bow harder toward the upper left are better.\nThe area under the curve (AUC) summarizes that bowing in one number. It has a clean interpretation with no threshold in it at all: AUC is the probability that a randomly chosen \\(y = 1\\) case gets a higher \\(\\widehat{\\pi}\\) than a randomly chosen \\(y = 0\\) case. It asks whether the model ranks the ones above the zeros, which is a different and more forgiving question than whether it classifies them correctly.\nAUC of 0.5 is chance, 1 is perfect, and 0.7\u20130.8 is typical for political-science models.\n\nNow the two running examples:\n\n\ncode\nroc_dp    &lt;- roc(dp$dispute, p_hat,   quiet = TRUE)\nroc_nafta &lt;- roc(nafta$vote, p_nafta, quiet = TRUE)\n\nbind_rows(roc_df(roc_nafta, sprintf(\"NAFTA vote (AUC = %.3f)\", auc(roc_nafta))),\n          roc_df(roc_dp,    sprintf(\"Democratic peace (AUC = %.3f)\", auc(roc_dp)))) %&gt;%\n  ggplot(aes(fpr, tpr, color = model)) +\n  geom_abline(intercept = 0, slope = 1, color = \"grey60\", linetype = \"dashed\") +\n  geom_path(linewidth = 0.8) +\n  scale_color_manual(values = setNames(\n    c(\"#005A43\", \"#6CC24A\"),\n    c(sprintf(\"NAFTA vote (AUC = %.3f)\", auc(roc_nafta)),\n      sprintf(\"Democratic peace (AUC = %.3f)\", auc(roc_dp))))) +\n  coord_equal() +\n  labs(title = \"ROC curves, both running examples\",\n       x = \"False alarm rate (FPR = 1 - specificity)\",\n       y = \"Detection rate (TPR = sensitivity)\",\n       color = NULL) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a04\n\n\n\n\n\nAnd here is the payoff of the threshold-free view. At \\(\\tau = 0.5\\) the democratic peace model looked worthless: PRE \u2014 the proportional reduction of error against a null model that always guesses the modal category \u2014 was essentially zero, and not a single case was classified as a dispute. Its AUC is 0.755, comfortably better than chance and squarely in the normal range for our field. The NAFTA model does better at 0.843, but the gap between the two is nothing like the gap their confusion matrices suggested. Almost all of that apparent difference was the threshold, not the model. This is the reason we report AUC rather than percent correctly predicted. It\u2019s the difference between evaluating the model at all thresholds versus a single threshold."
  },
  {
    "objectID": "binaryextensions126.html#comparing-models-via-roc",
    "href": "binaryextensions126.html#comparing-models-via-roc",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Comparing models via ROC",
    "text": "Comparing models via ROC\nA useful diagnostic: plot two nested models on the same ROC plane and ask whether the bigger model meaningfully improves discrimination.\n\n\ncode\ndpm_full   &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n                  family = binomial, data = dp)\ndpm_noborder &lt;- glm(dispute ~ deml + lncaprat + ally,\n                    family = binomial, data = dp)\n\nroc_full   &lt;- roc(dp$dispute, predict(dpm_full,   type = \"response\"), quiet = TRUE)\nroc_nb     &lt;- roc(dp$dispute, predict(dpm_noborder, type = \"response\"), quiet = TRUE)\n\nbind_rows(roc_df(roc_full, sprintf(\"with border (AUC = %.3f)\",  auc(roc_full))),\n          roc_df(roc_nb,   sprintf(\"no border (AUC = %.3f)\",    auc(roc_nb)))) %&gt;%\n  ggplot(aes(fpr, tpr, color = model)) +\n  geom_abline(intercept = 0, slope = 1, color = \"grey60\", linetype = \"dashed\") +\n  geom_path(linewidth = 0.8) +\n  scale_color_manual(values = setNames(\n    c(\"#005A43\", \"#6CC24A\"),\n    c(sprintf(\"with border (AUC = %.3f)\", auc(roc_full)),\n      sprintf(\"no border (AUC = %.3f)\",   auc(roc_nb))))) +\n  coord_equal() +\n  labs(title = \"Democratic peace, with and without shared border\",\n       x = \"False alarm rate (FPR = 1 - specificity)\",\n       y = \"Detection rate (TPR = sensitivity)\",\n       color = NULL) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a05\n\n\n\n\n\nThe two curves track each other closely. Border adds a small but visible bump in the low-false-alarm region, which is exactly where you want discrimination: cases the model is willing to call disputes that actually are disputes.\nIs that bump more than noise? DeLong\u2019s test asks precisely that. Its null hypothesis is that the two AUCs are equal. The word \u201ccorrelated\u201d in the output matters: both curves come from the same observations and the same observed \\(y\\), so the two AUC estimates are not independent draws and an ordinary two-sample comparison would be wrong. DeLong\u2019s test uses the covariance between them.\n\n\ncode\nroc.test(roc_full, roc_nb, method = \"delong\")\n\n\n\n    DeLong's test for two correlated ROC curves\n\ndata:  roc_full and roc_nb\nZ = 7.5264, p-value = 5.218e-14\nalternative hypothesis: true difference in AUC is not equal to 0\n95 percent confidence interval:\n 0.02245479 0.03826777\nsample estimates:\nAUC of roc1 AUC of roc2 \n  0.7549600   0.7245987 \n\n\nRead the result carefully, because it is a good example of a distinction this course keeps making. \\(Z = 7.53\\) with \\(p \\approx 5\\times10^{-14}\\): the AUC difference is about as far from zero as a difference gets, so we reject the null flatly. Border improves discrimination. But look at the confidence interval on the difference: 0.022 to 0.038. The improvement is somewhere between two and four AUC points, on a scale where 0.5 is a coin flip. With \\(N \\approx 21{,}000\\) we can detect a tiny improvement with enormous confidence, and the test tells us the improvement is real without telling us it is important.\nSo the test answers \u201cis this distinguishable from zero?\u201d and the interval answers \u201chow big is it?\u201d \u2014 and only the second question is the one a reader cares about. Report the interval. We will see the same pattern again in the information criteria below, where AIC and BIC both prefer the model with border by a margin that looks decisive while the AUC barely moves."
  },
  {
    "objectID": "binaryextensions126.html#two-types-of-separation-two-sets-of-symptoms",
    "href": "binaryextensions126.html#two-types-of-separation-two-sets-of-symptoms",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Two types of separation, two sets of symptoms",
    "text": "Two types of separation, two sets of symptoms\nThe distinction matters because the two look completely different when you run them, and only one of them announces itself.\n\nComplete separation. Some linear combination of \\(X\\) partitions the sample into ones and zeros perfectly, with room to spare on both sides. No coefficient in the model is pinned down.\nQuasi-complete separation. The split is perfect but tight \u2014 equality holds for at least some observations. The usual source is a single binary covariate with an empty cell. Typically only that one coefficient runs off to infinity and the rest of the model estimates normally.\n\nAn empty cell means exactly what it says, and it is worth being concrete because the phrase can sound like missing data. It is not. Cross-tabulate the covariate against \\(y\\) and count what lands in each of the four cells; one of them contains zero observations. The cases are all there and nothing is missing \u2014 it is just that this value of \\(x\\) never occurs alongside that value of \\(y\\):\n\n\n\n\n\\(y = 0\\)\n\\(y = 1\\)\n\n\n\n\n\\(x = 0\\)\n19,983\n947\n\n\n\\(x = 1\\)\n60\n0\n\n\n\nAll 60 observations with \\(x = 1\\) are zeros. Tell me \\(x = 1\\) and I know \\(y\\) with certainty, which is more than any finite coefficient can deliver: to drive \\(Pr(y = 1 \\mid x = 1)\\) to zero the logit needs \\(\\widehat{\\beta}_x = -\\infty\\). That table is not hypothetical \u2014 we build it from these data in a moment.\n\n\n\n\n\n\n\n\n\ncomplete\nquasi-complete\n\n\n\n\nconvergence\nfails \u2014 algorithm did not converge, hits the 25-iteration cap\nreports success, normal iteration count\n\n\nwarnings\nfitted probabilities numerically 0 or 1 occurred\noften none at all\n\n\ncoefficients\neverything enormous and unstable\none enormous coefficient; the rest look fine\n\n\nhow it reads\nobviously broken\na clean model with one insignificant variable\n\n\n\nThat last row is the problem. Complete separation is loud and you cannot miss it. Quasi-complete separation is quiet, and it misleads while it is quiet: an enormous coefficient divided by an even more enormous standard error gives \\(z \\approx 0\\) and \\(p \\approx 0.95\\), so the offending variable looks like a precisely estimated null. It is the opposite. The data are saying the effect is unbounded and the software is reporting it as nothing."
  },
  {
    "objectID": "binaryextensions126.html#the-case-youre-likely-to-encounter",
    "href": "binaryextensions126.html#the-case-youre-likely-to-encounter",
    "title": "Diagnostics, Classification, and Fit",
    "section": "The case you\u2019re likely to encounter",
    "text": "The case you\u2019re likely to encounter\nTextbook separation is complete separation. What you meet in dyadic conflict data is quasi-complete, and it usually arrives as a handful of cases \u2014 twenty, sixty, a couple hundred \u2014 where a covariate perfectly predicts \\(y\\), sitting inside a sample of twenty thousand. Let\u2019s build exactly that: a dummy flagging 60 dyad-years, none of which had a dispute. That \u201cflag\u201d dummy will perfectly predict \\(y=0\\).\n\n\ncode\nset.seed(20260907)   # chunk-level seed: this chunk uses sample()\n\ndp_flag &lt;- dp %&gt;% mutate(flag = 0L)\ndp_flag$flag[sample(which(dp_flag$dispute == 0), 60)] &lt;- 1L\n\ntable(dp_flag$flag, dp_flag$dispute, dnn = c(\"flag\", \"dispute\"))\n\n\n    dispute\nflag     0     1\n   0 19983   947\n   1    60     0\n\n\ncode\nm_flag &lt;- glm(dispute ~ border + deml + lncaprat + ally + flag,\n              family = binomial(link = \"logit\"), data = dp_flag)\n\ntibble(`N used` = nobs(m_flag), `converged?` = m_flag$converged,\n       iterations = m_flag$iter) %&gt;%\n  knitr::kable(caption = \"What glm reports about the fit.\")\n\n\n\nWhat glm reports about the fit.\n\n\nN used\nconverged?\niterations\n\n\n\n\n20990\nTRUE\n14\n\n\n\n\n\ncode\ntidy(m_flag) %&gt;%\n  knitr::kable(digits = 3, caption = \"The separated term is the `flag` row.\")\n\n\n\nThe separated term is the flag row.\n\n\nterm\nestimate\nstd.error\nstatistic\np.value\n\n\n\n\n(Intercept)\n-2.983\n0.107\n-27.964\n0.000\n\n\nborder\n1.034\n0.088\n11.787\n0.000\n\n\ndeml\n-0.070\n0.007\n-10.237\n0.000\n\n\nlncaprat\n-0.227\n0.024\n-9.461\n0.000\n\n\nally\n-0.840\n0.080\n-10.436\n0.000\n\n\nflag\n-12.318\n179.348\n-0.069\n0.945\n\n\n\n\n\nThe cell flag = 1, dispute = 1 is empty, so flag perfectly predicts a zero. Now read what glm() says about it. It reports converged = TRUE on the full 20,990 observations in an ordinary number of iterations, and prints no warning \u2014 not \u201cdid not converge,\u201d not \u201cfitted probabilities numerically 0 or 1.\u201d Every other coefficient is unchanged to three decimals. The flag row reads: coefficient about \\(-12\\), standard error about 180, \\(p \\approx 0.94\\).\nA reader \u2014 or a journal referee, or you at 2am \u2014 reads that row as \u201cflag doesn\u2019t matter.\u201d But what it really says is that the MLE for that coefficient is \\(-\\infty\\) and the optimizer stopped on the way there because the deviance stopped changing. There is no information in the data about how negative it is, so the standard error is arbitrary and the \\(p\\)-value is meaningless rather than large.\n\n\n\n\n\n\nImportant Concept\n\n\n\nThe reliable signature of quasi-complete separation is the coefficient and its standard error, not a warning message. Look for \\(|\\widehat{\\beta}|\\) above roughly 10 with a standard error one to three orders of magnitude larger. On the probability scale \\(|x\\beta| = 10\\) is already \\(\\widehat{\\pi} &lt; 0.0001\\); no substantive story in political science needs a logit coefficient of \\(-12\\). When you see one, you are looking at an empty cell, not an effect.\n\n\n\n\n\n\n\n\nR and Stata fail differently here\n\n\n\nStata\u2019s logit drops the perfectly-predicted observations and the offending variable, and says so: \u201cflag != 0 predicts failure perfectly; flag dropped and 60 obs not used.\u201d R\u2019s glm() does neither \u2014 it keeps every observation and reports a finite, enormous coefficient. Stata is loud and silently changes your estimation sample; R keeps the sample and silently reports nonsense. If you are moving a model between them, check \\(N\\): a Stata fit that dropped 60 observations is not on the same sample as the R fit beside it, so their log-likelihoods, AICs, and LR tests are not comparable."
  },
  {
    "objectID": "binaryextensions126.html#fixed-effects-as-a-major-source-of-separation",
    "href": "binaryextensions126.html#fixed-effects-as-a-major-source-of-separation",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Fixed effects as a major source of separation",
    "text": "Fixed effects as a major source of separation\nSeparation matters most where it arrives through an instinct every one of you has been trained to have: add fixed effects.\nThink about what a unit fixed effect does in a binary model. It is a dummy for each panel. If a panel never experiences the event \u2014 a dyad that never has a dispute, a country that never has a coup \u2014 then that dummy perfectly predicts \\(y = 0\\) within the panel. That is quasi-complete separation, by construction, once per quiet panel. And in event data many panels are quiet in the sense that the panel never fails (\\(y\\) never equals one).\n\n\ncode\ndp %&gt;%\n  group_by(dyad) %&gt;%\n  summarise(n = n(), events = sum(dispute), .groups = \"drop\") %&gt;%\n  summarise(dyads                     = n(),\n            `dyads with no dispute`   = sum(events == 0),\n            `dyads with variation`    = sum(events &gt; 0 & events &lt; n),\n            `obs in no-dispute dyads` = sum(n[events == 0])) %&gt;%\n  knitr::kable(caption = \"Dyad fixed effects would separate most of this sample.\")\n\n\n\nDyad fixed effects would separate most of this sample.\n\n\n\n\n\n\n\n\ndyads\ndyads with no dispute\ndyads with variation\nobs in no-dispute dyads\n\n\n\n\n827\n621\n202\n15412\n\n\n\n\n\n621 of 827 dyads never experience a dispute in this window, and they hold 15,412 of 20,990 dyad-years \u2014 73% of the data. Add dyad fixed effects and you have created 621 separated parameters at once.\nWhat happens next depends on what you typed, and none of the options is comfortable.\n\n\ncode\n# fixest drops the separated panels and reports doing so\nfe_fixest &lt;- feglm(dispute ~ deml + lncaprat + ally | dyad,\n                   family = binomial, data = dp)\n\npooled &lt;- glm(dispute ~ deml + lncaprat + ally,\n              family = binomial(link = \"logit\"), data = dp)\n\nfmt &lt;- function(b, n) c(sprintf(\"%.3f\", b), format(n, big.mark = \",\"))\n\ntibble(term      = c(\"deml\", \"lncaprat\", \"ally\", \"observations used\"),\n       pooled    = fmt(coef(pooled)[c(\"deml\", \"lncaprat\", \"ally\")],    nobs(pooled)),\n       `dyad FE` = fmt(coef(fe_fixest)[c(\"deml\", \"lncaprat\", \"ally\")], fe_fixest$nobs)) %&gt;%\n  knitr::kable(caption = \"Pooled logit vs. dyad fixed effects. Note the signs, and the N.\")\n\n\n\nPooled logit vs.\u00a0dyad fixed effects. Note the signs, and the N.\n\n\nterm\npooled\ndyad FE\n\n\n\n\ndeml\n-0.077\n0.048\n\n\nlncaprat\n-0.379\n0.245\n\n\nally\n-0.587\n-1.229\n\n\nobservations used\n20,990\n5,544\n\n\n\n\n\nTwo things to take away, and the second is worse than the first.\nThe sample quietly collapses. fixest::feglm() prints a note that 625 fixed effects and 15,446 observations were removed, and estimates on the 5,544 that remain \u2014 about a quarter of the data. survival::clogit() reaches the same place by a different route: its conditional likelihood conditions on the number of events within each panel, so panels with no variation contribute nothing and fall out. Plain glm() with dyad dummies does neither. It keeps all 20,990 rows, reports converged = TRUE, prints no warning, and hands back 621 dummy coefficients below \\(-10\\) (the smallest is \\(-21.9\\)).\nThese are all equally problematic though for different reasons. The models that don\u2019t drop the separated cases will report nonsense estimates. The models that do drop the separated cases are now estimated in a very non-random sample.\nAnd the substantive answer reverses. Look at the table: deml goes from \\(-0.070\\) pooled to \\(+0.048\\) with fixed effects, and lncaprat from \\(-0.227\\) to \\(+0.245\\). The democratic peace finding changes sign. Some of that is a real change of estimand \u2014 fixed effects ask a within-dyad question, answerable only among the 202 dyads that ever fight, which is not the population anyone means when they talk about the democratic peace. Some of it is the incidental-parameters problem, which biases fixed-effects logit in short panels. Untangling those two is a paper, not a lecture. The point for now is narrower: you typed one term, three-quarters of your data left, and your headline coefficient flipped sign \u2014 and depending on which function you used, nothing on your screen said so.\n\n\n\n\n\n\nThere is no within transformation here\n\n\n\nA natural thought: doesn\u2019t the fixed effect get swept out by de-meaning, the way it does in OLS? No. The within transformation is a linear-model device; subtracting panel means from \\(y\\) and \\(X\\) does not remove the fixed effect from a logit, because the link is nonlinear. That is exactly why conditional logit exists \u2014 conditioning on the panel\u2019s event count is the substitute for a within transformation \u2014 and why the incidental-parameters problem is a problem at all. If de-meaning worked, none of this would come up.\n\n\nSo: fixed effects in a binary-response model are not a free robustness check. Before you add them, cross-tabulate the panel identifier against \\(y\\) and find out how many panels are constant. If most are, you are not adding a control \u2014 you are choosing a different, much smaller sample and a different question."
  },
  {
    "objectID": "binaryextensions126.html#detecting-separation-in-practice",
    "href": "binaryextensions126.html#detecting-separation-in-practice",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Detecting separation in practice",
    "text": "Detecting separation in practice\nThree checks before you trust a binary-response model.\n\nCross-tab every binary and low-cardinality covariate against \\(y\\). Low cardinality just means the variable takes few distinct values \u2014 a dummy, a four-category region code, a panel identifier \u2014 as opposed to a continuous variable like the capability ratio that takes a different value in almost every row. Those are the ones you can cross-tabulate, and an empty cell is separation. This is the check that works, and the one people skip.\nScan the coefficient column for anything above about 10 in absolute value, then look at its standard error. Huge estimate over a huger standard error is the signature; a \\(p\\)-value near 1 on such a term is a symptom, not a finding.\nAsk directly. detectseparation::detect_separation() solves a linear program that answers the question exactly rather than by eye \u2014 and tells you which coefficients are infinite and in which direction.\n\n\n\ncode\n# fit with method = \"detect_separation\" instead of the usual IRLS: no estimates,\n# just a verdict on whether each MLE is finite\nglm(dispute ~ border + deml + lncaprat + ally,\n    family = binomial(link = \"logit\"), data = dp,\n    method = \"detect_separation\")\n\n\nImplementation: ROI | Solver: lpsolve \nSeparation: FALSE \nExistence of maximum likelihood estimates\n(Intercept)      border        deml    lncaprat        ally \n          0           0           0           0           0 \n0: finite value, Inf: infinity, -Inf: -infinity\n\n\ncode\nglm(dispute ~ border + deml + lncaprat + ally + flag,\n    family = binomial(link = \"logit\"), data = dp_flag,\n    method = \"detect_separation\")\n\n\nImplementation: ROI | Solver: lpsolve \nSeparation: TRUE \nExistence of maximum likelihood estimates\n(Intercept)      border        deml    lncaprat        ally        flag \n          0           0           0           0           0        -Inf \n0: finite value, Inf: infinity, -Inf: -infinity\n\n\nThe baseline model comes back Separation: FALSE with every coefficient marked 0, meaning finite. The model with flag comes back Separation: TRUE, and flag is marked -Inf while everything else stays finite \u2014 which is the precise statement that the other coefficients are fine and this one does not exist. Compare that to what glm() told us a moment ago, which was nothing. If you take one practical habit from this section, take this one: when a binary-response model surprises you, run it through detect_separation() before you try to interpret it."
  },
  {
    "objectID": "binaryextensions126.html#remedies",
    "href": "binaryextensions126.html#remedies",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Remedies",
    "text": "Remedies\nOnce you have found separation, the fix depends on what the variable is doing in the model.\n\nDrop the offending variable, if it shouldn\u2019t be there \u2014 mechanically related to \\(y\\), or an uninformative artifact of your sample.\nCombine or recode categories so the empty cell goes away.\nPenalized likelihood (Firth). Adds a Jeffreys-prior-like penalty that pulls estimates toward zero. logistf::logistf(). Slightly biased toward zero but always finite, and inference works.\nBayesian estimation with a weakly informative prior \u2014 \\(N(0, 2.5^2)\\) on \\(\\beta\\) makes the posterior proper. arm::bayesglm(), rstanarm::stan_glm(), brms::brm().\n\nFirth is the usual first move for political-science data. Run it on the separated model from above:\n\n\ncode\nm_firth &lt;- logistf(dispute ~ border + deml + lncaprat + ally + flag, data = dp_flag)\n\n# logistf returns PROFILE penalized-likelihood CIs (ci.lower/ci.upper) -- the right\n# interval under the penalty, since the Wald SE is untrustworthy on a separated term\ntibble(term      = names(coef(m_firth)),\n       glm_est   = unname(coef(m_flag)),\n       glm_se    = unname(sqrt(diag(vcov(m_flag)))),\n       firth_est = unname(coef(m_firth)),\n       firth_lo  = unname(m_firth$ci.lower),\n       firth_hi  = unname(m_firth$ci.upper)) %&gt;%\n  knitr::kable(digits = 3,\n               caption = \"Ordinary glm vs. Firth, with Firth's profile-likelihood 95% CIs.\")\n\n\n\nOrdinary glm vs.\u00a0Firth, with Firth\u2019s profile-likelihood 95% CIs.\n\n\nterm\nglm_est\nglm_se\nfirth_est\nfirth_lo\nfirth_hi\n\n\n\n\n(Intercept)\n-2.983\n0.107\n-2.980\n-3.191\n-2.773\n\n\nborder\n1.034\n0.088\n1.033\n0.862\n1.206\n\n\ndeml\n-0.070\n0.007\n-0.070\n-0.084\n-0.057\n\n\nlncaprat\n-0.227\n0.024\n-0.227\n-0.274\n-0.180\n\n\nally\n-0.840\n0.080\n-0.839\n-0.998\n-0.682\n\n\nflag\n-12.318\n179.348\n-1.648\n-6.489\n0.306\n\n\n\n\n\nThe Firth estimate on flag is large but finite, and the other coefficients barely move \u2014 the penalty concentrates where the data are pathological. Read the inference off the profile-likelihood interval, not the Wald standard error: on a separated term the sampling distribution is nowhere near normal, so the Wald interval is misleading while the profile interval is finite and honest.\nOne caution you\u2019ll have seen in Rainey (2016). A separated coefficient carries no information from the data, so whatever finite number a remedy reports is coming from the penalty or the prior. Firth\u2019s penalty is one choice and a \\(N(0, 2.5^2)\\) prior is another; they need not agree, and the substantive quantity of interest can move a lot depending on which you pick."
  },
  {
    "objectID": "binaryextensions126.html#compare-on-the-democratic-peace-data",
    "href": "binaryextensions126.html#compare-on-the-democratic-peace-data",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Compare on the democratic peace data",
    "text": "Compare on the democratic peace data\n\n\ncode\nfit_firth_full &lt;- logistf(dispute ~ border + deml + lncaprat + ally, data = dp)\n\ntibble(\n  term       = names(coef(fit_logit)),\n  logit_est  = unname(coef(fit_logit)),\n  logit_se   = unname(sqrt(diag(vcov(fit_logit)))),\n  firth_est  = unname(coef(fit_firth_full)),\n  firth_se   = unname(sqrt(diag(vcov(fit_firth_full))))\n) %&gt;%\n  knitr::kable(digits = 3,\n               caption = \"Plain logit vs. Firth-penalized logit on the full democratic peace data.\")\n\n\n\nPlain logit vs.\u00a0Firth-penalized logit on the full democratic peace data.\n\n\nterm\nlogit_est\nlogit_se\nfirth_est\nfirth_se\n\n\n\n\n(Intercept)\n-2.987\n0.107\n-2.983\n0.107\n\n\nborder\n1.036\n0.088\n1.035\n0.088\n\n\ndeml\n-0.070\n0.007\n-0.070\n0.007\n\n\nlncaprat\n-0.227\n0.024\n-0.227\n0.024\n\n\nally\n-0.842\n0.081\n-0.841\n0.080\n\n\n\n\n\nWith \\(N \\approx 20{,}000\\) and roughly 900 events, the Firth correction barely moves anything. This is the virtue of living in asymptopia. The exercise matters more for a researcher working on \\(N = 500\\) with 20 events \u2014 there, the Firth estimates can shift meaningfully relative to plain logit.\n\n\n\n\n\n\nImportant Concept\n\n\n\nFirth\u2019s penalty does double duty: it solves separation (preventing infinite estimates) and rare-events bias (correcting finite-sample intercept bias) in one move. For binary-response work on small-to-medium-N political-science data, \u201cfit it with logistf first and check whether anything changes\u201d is a reasonable default workflow."
  },
  {
    "objectID": "binaryextensions126.html#precision-and-recall",
    "href": "binaryextensions126.html#precision-and-recall",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Precision and recall",
    "text": "Precision and recall\nOne more tool belongs here, because it exists for exactly this situation. ROC and AUC can flatter a model badly when one class is abundant, and the reason is entirely in the denominators.\nGo back to the confusion matrix. Recall is the true positive rate under another name \u2014 \\(TP/(TP+FN)\\), the share of real disputes the model catches, which is the ROC\u2019s vertical axis. Precision is a quantity the ROC never shows you at all: \\(TP/(TP+FP)\\), the share of the cases you flagged that turned out to be real.\nWhy the difference matters: at \\(\\tau = 0.1\\) this model flags 1,987 dyad-years, of which 287 are disputes and 1,700 are false alarms. Divide those 1,700 false alarms by the 20,043 true zeros and you get an FPR of 0.085 \u2014 under 9%, and the ROC curve looks healthy. Divide the same 1,700 by the 1,987 cases you actually flagged and precision is 0.144 \u2014 about six false alarms for every dispute you catch. Same model, same threshold, same errors. The ROC is dividing by a number that is large precisely because \\(y\\) is rare.\nPut it the other way and the rule is easy to remember: precision and recall never use \\(TN\\); the ROC does. When 95% of your data sits in that one cell, that is the whole difference.\nThe precision-recall curve sweeps \\(\\tau\\) exactly the way the ROC does, and plots precision against recall. AUC-PR is the area under it. It also goes by average precision, and that name is literal \u2014 walk down the cases sorted by \\(\\widehat{\\pi}\\), note the precision each time you hit a real dispute, and average those numbers.\n\n\ncode\n# sort by predicted probability, then accumulate: precision and recall at every\n# possible threshold are just running totals down the sorted list\nord  &lt;- order(p_hat, decreasing = TRUE)\nysrt &lt;- dp$dispute[ord]\ntp   &lt;- cumsum(ysrt)\n\npr_df &lt;- tibble(precision = tp / seq_along(ysrt),\n                recall    = tp / sum(ysrt),\n                tau       = p_hat[ord])\n\n# average precision: precision weighted by the gain in recall at each step\nauc_pr    &lt;- sum(diff(c(0, pr_df$recall)) * pr_df$precision)\nbase_rate &lt;- mean(dp$dispute)\n\n# the (recall, precision) pair at three specific thresholds\npr_marks &lt;- map_dfr(c(0.2, 0.1, 0.05), function(tt) {\n  yh &lt;- as.integer(p_hat &gt;= tt)\n  tibble(tau       = tt,\n         recall    = sum(yh == 1 & dp$dispute == 1) / sum(dp$dispute == 1),\n         precision = sum(yh == 1 & dp$dispute == 1) / max(sum(yh == 1), 1))\n})\n\nggplot(pr_df, aes(recall, precision)) +\n  geom_hline(yintercept = base_rate, linetype = \"dashed\", color = \"grey40\") +\n  annotate(\"text\", x = 0.60, y = base_rate + 0.02, hjust = 0, size = 3.1,\n           color = \"grey15\", label = \"chance = the base rate, 0.045\") +\n  geom_path(color = \"#005A43\", linewidth = 0.8) +\n  geom_point(data = pr_marks, size = 2.6, color = \"grey15\") +\n  geom_text(data = pr_marks, aes(label = paste0(\"tau = \", tau)),\n            hjust = -0.2, vjust = -0.8, size = 3.0, color = \"grey15\") +\n  coord_cartesian(xlim = c(0, 1), ylim = c(0, 0.5)) +\n  labs(title = \"Precision-recall curve, democratic peace logit\",\n       subtitle = sprintf(\"AUC-PR = %.3f, against a chance baseline of %.3f\",\n                          auc_pr, base_rate),\n       x = \"Recall (= TPR = sensitivity): share of real disputes caught\",\n       y = \"Precision: share of flagged cases that were real\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a06\n\n\n\n\n\nTwo things to know before reading it. Chance is not 0.5. A coin flip achieves precision equal to the base rate at every level of recall, so the chance line here sits at 0.045 and an AUC-PR has to be judged against the base rate rather than against a fixed reference. And the curve always ends at (1, base rate): set \\(\\tau = 0\\), flag every case, and precision is by definition the share of ones in the data.\nThis model\u2019s AUC-PR is 0.116 against a chance baseline of 0.045 \u2014 about two and a half times better than guessing, and a very different-looking number from the AUC of 0.755. Both are correct, and they answer different questions. AUC asks whether the model ranks disputes above non-disputes, and it does that respectably. AUC-PR asks how clean a set of flagged cases you could assemble, and the answer is: not very clean at all.\nWhich one you should care about depends on whether anyone is going to act on the flags. For conflict forecasting \u2014 the literature that brought this machinery into political science (Ward, Greenhill, and Bakke 2010) \u2014 precision is the operative quantity, because a watchlist that is 86% false alarms is expensive to act on. For the coefficient-and-marginal-effect work most of us do, neither curve is really the estimand. The practical reason to know what AUC-PR is comes up when you are reading someone else\u2019s paper: when a rare-events model reports an impressive AUC, remember how much of that number the abundant zeros are paying for."
  },
  {
    "objectID": "binaryextensions126.html#the-trinity-of-tests",
    "href": "binaryextensions126.html#the-trinity-of-tests",
    "title": "Diagnostics, Classification, and Fit",
    "section": "The trinity of tests",
    "text": "The trinity of tests\nFor nested models \u2014 where the constrained model\u2019s regressors are a strict subset of the unconstrained model\u2019s, on the same sample \u2014 there are three asymptotically equivalent tests.\nThey are three readings of the same likelihood geometry we built in the likelihood notes: the LLR compares the height of the log-likelihood at two points; the Wald measures the distance of \\(\\widehat\\beta_1\\) from \\(\\beta_0\\) scaled by the curvature at the maximum (the information, \\((-H)^{-1}\\)); and the LM reads the slope \u2014 the score \u2014 at the constrained estimate. Height, curvature, slope: the same three objects from the machine, measured three ways.\n\nLog-likelihood ratio (LLR)\nEstimate the constrained model \\(M_0\\) and the unconstrained model \\(M_1\\). Compare the heights of their log-likelihoods:\n\\[\n\\chi^2_{\\text{LLR}} = -2\\left[\\ln\\mathcal{L}(M_0) - \\ln\\mathcal{L}(M_1)\\right]\n\\]\ndistributed \\(\\chi^2\\) with \\(k_1 - k_0\\) degrees of freedom under \\(H_0: M_0\\). Simple to compute, requires fitting both models.\n\n\nWald\nCompute the distance between the unconstrained estimate \\(\\widehat{\\beta}_1\\) and the constrained value \\(\\beta_0\\), weighted by the curvature of the log-likelihood at \\(\\widehat{\\beta}_1\\) (the inverse information matrix). Requires only the unconstrained model. Tests whether \\(\\widehat{\\beta}_1\\) is \u201cfar enough\u201d from \\(\\beta_0\\) relative to its sampling variability.\n\n\nLagrange multiplier (LM, score)\nEvaluate the score of the unconstrained log-likelihood at the constrained estimate \\(\\beta_0\\). If the score is far from zero, the constrained model is leaving information on the table. Requires only the constrained model \u2014 useful when the unconstrained model is expensive to fit.\nIn practice, LLR is the most common in political science, Wald is what software usually defaults to when reporting per-coefficient \\(z\\)-tests, and LM is rare.\n\n\nCompare the democratic peace models\nThe LLR test is worth doing by hand once, because there is almost nothing to it. Both models are already fit, so the whole test is two log-likelihoods, a subtraction, and one lookup in a \\(\\chi^2\\) table.\n\n\ncode\nll0 &lt;- logLik(dpm_noborder)   # constrained model: border excluded\nll1 &lt;- logLik(dpm_full)       # unconstrained model: border included\n\nllr &lt;- -2 * (as.numeric(ll0) - as.numeric(ll1))\ndf  &lt;- attr(ll1, \"df\") - attr(ll0, \"df\")   # difference in number of estimated parameters\npv  &lt;- pchisq(llr, df = df, lower.tail = FALSE)   # upper tail: chi-squared is one-sided\n\ntibble(`ln L, no border`   = sprintf(\"%.3f\", as.numeric(ll0)),\n       `ln L, with border` = sprintf(\"%.3f\", as.numeric(ll1)),\n       `chi-squared`       = sprintf(\"%.2f\", llr),\n       df                  = df,\n       `p-value`           = format(pv, digits = 3, scientific = TRUE)) %&gt;%\n  knitr::kable(caption = \"LR test computed by hand: does adding shared border improve fit?\")\n\n\n\nLR test computed by hand: does adding shared border improve fit?\n\n\nln L, no border\nln L, with border\nchi-squared\ndf\np-value\n\n\n\n\n-3579.534\n-3507.152\n144.76\n1\n2.42e-33\n\n\n\n\n\ncode\n# the same test from the packaged functions:\n# anova(dpm_noborder, dpm_full, test = \"LRT\")\n# lmtest::lrtest(dpm_noborder, dpm_full)\n\n\nFour lines of arithmetic. logLik() pulls the maximized log-likelihood off each fitted model \u2014 the height of the hill at its summit, the quantity we examined in the maximization notes \u2014 and the df attribute carries the number of parameters that got it there, so the degrees of freedom are just the difference. The one-sided pchisq() call is the whole test.\nIn your own work use the packaged version \u2014 commented out above \u2014 because anova() and lmtest::lrtest() also check that the models are genuinely nested and fit on the same rows, which is the mistake that actually bites people. But it\u2019s worth understanding how simple this is to compute (by hand).\nThe \\(\\chi^2\\) of 144.8 on 1 degree of freedom gives a \\(p\\)-value of about \\(2\\times10^{-33}\\) \u2014 border meaningfully improves fit, even though the ROC bump was small. That divergence is informative: the LLR test cares about likelihood gain, which a few high-leverage observations can drive; the AUC cares about ranking across the full sample. Both are valid; they answer different questions."
  },
  {
    "objectID": "binaryextensions126.html#information-criteria",
    "href": "binaryextensions126.html#information-criteria",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Information criteria",
    "text": "Information criteria\nWhen models are non-nested, the trinity doesn\u2019t apply. The information criteria do \u2014 they trade the maximized log-likelihood (the same quantity we recovered off the fitted model in the maximization notes) against a penalty for the number of parameters \\(k\\).\n\\[\n\\text{AIC} = -2\\ln\\mathcal{L} + 2k\n\\]\n\\[\n\\text{BIC} = -2\\ln\\mathcal{L} + \\ln(N)\\, k\n\\]\nLower is better. AIC has a fixed penalty per parameter; BIC\u2019s penalty scales with sample size, so it pushes harder against complexity in large samples.\nRaftery (1995) gives a rule of thumb for interpreting \\(|\\Delta\\text{BIC}|\\):\n\n0\u20132: weak evidence\n2\u20136: positive\n6\u201310: strong\n\\(&gt; 10\\): very strong\n\n\n\ncode\ntibble(\n  model = c(\"no border\", \"with border\"),\n  loglik = c(logLik(dpm_noborder), logLik(dpm_full)),\n  AIC    = c(AIC(dpm_noborder),    AIC(dpm_full)),\n  BIC    = c(BIC(dpm_noborder),    BIC(dpm_full))\n) %&gt;%\n  mutate(dAIC = AIC - min(AIC),\n         dBIC = BIC - min(BIC)) %&gt;%\n  knitr::kable(digits = 2,\n               caption = \"AIC/BIC comparison, democratic peace with and without border.\")\n\n\n\nAIC/BIC comparison, democratic peace with and without border.\n\n\nmodel\nloglik\nAIC\nBIC\ndAIC\ndBIC\n\n\n\n\nno border\n-3579.53\n7167.07\n7198.87\n142.76\n134.81\n\n\nwith border\n-3507.15\n7024.30\n7064.06\n0.00\n0.00\n\n\n\n\n\nBoth AIC and BIC prefer the larger model. The BIC gap is well past Raftery\u2019s \u201cvery strong\u201d threshold, so the verdict is unambiguous despite the modest AUC improvement."
  },
  {
    "objectID": "binaryextensions126.html#footnotes",
    "href": "binaryextensions126.html#footnotes",
    "title": "Diagnostics, Classification, and Fit",
    "section": "Footnotes",
    "text": "Footnotes\n\n\nI used Claude code to make figures 1 and 3.\u21a9\ufe0e\nsee what I did there?\u21a9\ufe0e"
  },
  {
    "objectID": "ex22026.html",
    "href": "ex22026.html",
    "title": "Exercise #2 \u2014 Write the Maximizer",
    "section": "",
    "text": "Please answer the following questions. All answers should be coded in R and submitted as a rendered PDF or HTML built in Quarto (or R Markdown / LaTeX). Turn in both your .qmd/.R source and the rendered document on Brightspace, so I can run what you ran.\nFor this assignment the only R library you need is tidyverse, for data management and plotting. There is no modeling package and no optimizer in this assignment \u2014 no glm inside your estimator, no optim, no maxLik. You will call glm twice, in questions 9 and 10, and only ever to check an answer you have already produced yourself.\nThis is the assignment where you build the machine. Up to now you have written a log-likelihood and we\u2019ve climbed it in class; here I\u2019m asking you to write the functions that estimate the unknowns. When you are done you will have a working maximum likelihood estimator that you wrote yourself, and \u2014 more to the point \u2014 you will have the four pieces every estimator you ever write again is made of.\nThe math you need is in the maximization notes and the likelihood notes. The code is not. Finding the right formula in the notes and turning it into a function is the assignment.\nOn AI tools. Per the course policy: if you get stuck coding you may use AI to get unstuck, but you must cite and annotate it \u2014 what you asked, why, and which lines are the bot\u2019s. A bot can easily write a Newton-Raphson logit. What it cannot do is sit in my office and tell me why the step is \\((X'WX)^{-1}X'(y-\\pi)\\) rather than something else, or which of your four functions is broken when the estimates come out wrong. I will ask. The checks in Part II are there so that you can find your own errors."
  },
  {
    "objectID": "ex22026.html#notes",
    "href": "ex22026.html#notes",
    "title": "Exercise #2 \u2014 Write the Maximizer",
    "section": "Notes",
    "text": "Notes\n\nThe maximization notes have the update rule, the score, and the Hessian for the logit. The likelihood notes have where the standard errors come from and why the curvature at the peak is the right thing to invert.\nWork with each other \u2014 that is essential \u2014 but the work you turn in must be your own."
  },
  {
    "objectID": "ex12026.html",
    "href": "ex12026.html",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood",
    "section": "",
    "text": "Please answer the following questions. All answers should be coded in R and submitted as a rendered PDF built in Quarto (or R Markdown) or LaTeX. Please turn in both your source (.qmd/.R) and the rendered document on Brightspace, so I can run exactly what you ran. Everything should be instantly replicable \u2014 running your code should reproduce every figure, table, and number.\nFor this assignment, the only R libraries you need (should use) are\nThis assignment is aimed at developing coding skills and habits, an understanding of probability distributions and their relationship to data, and some basic data management. It also reviews the logit and probit and prediction methods.\nAbout the figures. Questions 1\u20134 each show you a figure and ask you to replicate it. I mean replicate: same curves, same axes, same labeling, same look. Part of what you\u2019re learning this term is how to produce a figure someone else would be willing to publish, and the fastest way to learn that is to reproduce one and notice every decision that went into it \u2014 the palette, the linetypes, the axis labels, where the legend sits, what the title claims. The prediction plots you build in question 10 should meet the same standard.\nWork with each other \u2014 that\u2019s essential \u2014 but the work you turn in must be your own.\nOn AI tools. Per the course policy: if you get stuck coding, you may use a bot to get unstuck, but you must cite and annotate it (what you asked, why, and which lines are the bot\u2019s), and expect to explain your work. Don\u2019t start with a bot \u2014 the point of the exercise is what you come to understand by writing the code."
  },
  {
    "objectID": "ex12026.html#notes",
    "href": "ex12026.html#notes",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood",
    "section": "Notes",
    "text": "Notes\nThe CES project site is https://cces.gov.harvard.edu. Here\u2019s an article about the \u201cAmerican Voter Bot\u201d project: https://www.nytimes.com/2020/01/20/opinion/twitter-democratic-debate.html"
  },
  {
    "objectID": "predictionOLS26.html",
    "href": "predictionOLS26.html",
    "title": "Prediction Methods in the Linear Model",
    "section": "",
    "text": "You have already seen these prediction methods in the linear model \u2014 quantities of interest from OLS three ways: predictions at means, average effects, and simulation. This page builds them in a detailed way organized around those three methods, and then around ways to measure uncertainty in any of these methods. It is the companion to the nonlinear prediction methods slides; the two are meant to be read together.\nPreviewing the toolkit in OLS is useful because OLS is the simple case. In the linear model the \u201clink\u201d between the linear prediction \\(x\\widehat\\beta\\) and the quantity of interest is the identity function \u2014 \\(\\widehat y = x\\widehat\\beta\\). In the linear model, we don\u2019t have to transform the linear prediction by the link function; the linear prediction is the quantity of interest.\n\nend-point transformation is just \\(\\widehat y \\pm 1.96 \\, se(\\widehat y)\\);\nthe maximum-likelihood standard error \\(\\sqrt{\\mathrm{diag}(\\mathbf{X V X'})}\\) is exactly the \\(se(\\widehat y)\\) that predict() produces;\nthe delta-method standard error of \\(\\widehat y\\) equals that same number;\nthe confidence interval is symmetric.\n\nNote each of these as we go. In MLE, the link is a nonlinear function that performs important functions: the transformation by \\(F\\) bounds the interval and bends it, and the two standard errors part ways. But the techniques described below transport directly from the linear case to the nonlinear case.\n\n\n\n\n\n\nThe process, up front\n\n\n\nEvery quantity of interest below \u2014 and in every model we meet the rest of the semester \u2014 comes from the same sequence:\n\nestimate the model;\nchoose an approach \u2014 at-means (hold the other covariates at central tendencies, vary the \\(x\\) of interest) or average effects (set the \\(x\\) of interest for the whole estimation sample, predict every case, average);\nbuild the linear prediction \\(x\\widehat\\beta\\) for the interesting values of \\(x\\);\ntransform through the link (in OLS the link is the identity, so this step does nothing);\nattach uncertainty by an analytic standard error, by simulation, or by a bootstrap;\nform the interval and plot.\n\nSteps 2 (the approach) and 5 (the technique) are independent choices: any approach pairs with any technique. Keeping them separate organizes everything that follows."
  },
  {
    "objectID": "predictionOLS26.html#the-estimation-sample",
    "href": "predictionOLS26.html#the-estimation-sample",
    "title": "Prediction Methods in the Linear Model",
    "section": "The estimation sample",
    "text": "The estimation sample\nBoth approaches \u2014 average effects especially \u2014 need the rows the model was actually fit on, after listwise deletion. na.action(m1) returns the indices R dropped; flag them and keep the rest. It is worth checking how many cases you actually keep, every time.\n\n\ncode\nitt$used &lt;- TRUE\nitt$used[na.action(m1)] &lt;- FALSE\nestdata &lt;- itt %&gt;% filter(used == \"TRUE\")\n\n# central tendencies of the estimation sample, reused throughout the file\np1med  &lt;- median(estdata$p1)          # Polity held at its median (16 =&gt; polity2 = 5)\np2med  &lt;- p1med^2                     # the matching quadratic value\ngdpmed &lt;- median(estdata$loggdp)      # log GDP per capita at its median\npromed &lt;- median(estdata$lagprotest)  # protests at their median\n\ndatasummary(scarring + lagRA + civilwar + lagprotest + p1 + loggdp ~\n              (Mean + Median + SD + Min + Max),\n            data = estdata, title = \"ITT estimation sample\")\n\n\n\n\n    \n\n    \n    \n      \n        \n        ITT estimation sample\n              \n                 \n                Mean\n                Median\n                SD\n                Min\n                Max\n              \n        \n        \n        \n                \n                  scarring  \n                  8.21 \n                  5.00 \n                  11.15\n                  0.00\n                  130.00\n                \n                \n                  lagRA     \n                  0.14 \n                  0.00 \n                  0.35 \n                  0.00\n                  1.00  \n                \n                \n                  civilwar  \n                  0.05 \n                  0.00 \n                  0.21 \n                  0.00\n                  1.00  \n                \n                \n                  lagprotest\n                  3.22 \n                  1.00 \n                  4.61 \n                  0.00\n                  37.00 \n                \n                \n                  p1        \n                  13.63\n                  16.00\n                  6.52 \n                  1.00\n                  21.00 \n                \n                \n                  loggdp    \n                  8.30 \n                  8.25 \n                  1.27 \n                  5.28\n                  11.04 \n                \n        \n      \n    \n\n\n\ncode\ncat(\"N used:\", nrow(estdata), \"of\", nrow(itt), \"country-years\\n\")\n\n\nN used: 899 of 2128 country-years\n\n\nWe keep 899 of 2,128 country-years \u2014 the lags and the missing protest data cost us a lot of rows. The model we interpret is the model on the cases we kept, and here that is well under half the data. It\u2019s worth noting that this is a terrible missingness rate and not something we could tolerate in a real research project - missing data are never random; the amount of non-randomness here is bad."
  },
  {
    "objectID": "predictionOLS26.html#the-ml-standard-error-of-the-linear-prediction",
    "href": "predictionOLS26.html#the-ml-standard-error-of-the-linear-prediction",
    "title": "Prediction Methods in the Linear Model",
    "section": "The ML standard error of the linear prediction",
    "text": "The ML standard error of the linear prediction\nThe most direct measure of uncertainty is the standard error of the linear prediction \\(X\\widehat\\beta\\). It comes from the variance\u2013covariance matrix of \\(\\widehat\\beta\\):\n\\[\n\\mathrm{Var}(X\\widehat\\beta) = \\mathbf{X}\\,\\mathbf{V}\\,\\mathbf{X}',\n\\]\nan \\(N \\times N\\) matrix, where \\(\\mathbf{V} = \\widehat{\\mathrm{Var}}(\\widehat\\beta) =\\) vcov(m1). Its diagonal holds the variances of the \\(N\\) predictions, so\n\\[\nse(X\\widehat\\beta) = \\sqrt{\\mathrm{diag}(\\mathbf{X}\\,\\mathbf{V}\\,\\mathbf{X}')},\n\\]\nan \\(N \\times 1\\) vector. In the MLE class this \\(\\mathbf{V}\\) is the \\((-H)^{-1}\\) from the likelihood notes; in OLS it is the familiar \\(\\widehat\\sigma^2 (X'X)^{-1}\\). Same formula, and it produces exactly the standard error predict() reports. Here it is by hand, on the at-means protest grid, checked against predict():\n\n\ncode\n# design matrix for the at-means grid (protest varies, others held)\nXg &lt;- model.matrix(~ lagRA + civilwar + lagprotest + p1 + p2 + loggdp, data = oosdata)\nV  &lt;- vcov(m1)\n\nxb     &lt;- as.vector(Xg %*% coef(m1))        # linear prediction x'b (= y-hat in OLS)\nse_lin &lt;- sqrt(diag(Xg %*% V %*% t(Xg)))    # ML SE of x'b, by hand\n\n# check against predict()'s SE\ndata.frame(lagprotest = oosdata$lagprotest,\n           se_byhand  = se_lin,\n           se_predict = predict(m1, newdata = oosdata, se.fit = TRUE)$se.fit)[1:5, ]\n\n\n  lagprotest se_byhand se_predict\n1          0 0.6379299  0.6379299\n2          1 0.6063378  0.6063378\n3          2 0.5832546  0.5832546\n4          3 0.5697154  0.5697154\n5          4 0.5664052  0.5664052\n\n\nThey agree to machine precision: predict\u2019s standard errors are this \\(\\sqrt{\\mathrm{diag}(XVX')}\\) computation. Please note the value of understanding basic matrix algebra for doing this. You might ask why you\u2019d ever need to do this - some estimation packages in R do not have predict functions, or have sufficiently opaque functions that computing your own quantities is preferable."
  },
  {
    "objectID": "predictionOLS26.html#delta-method-standard-errors",
    "href": "predictionOLS26.html#delta-method-standard-errors",
    "title": "Prediction Methods in the Linear Model",
    "section": "Delta-method standard errors",
    "text": "Delta-method standard errors\nWhen do we use the ML standard error, and when the delta method? Use the ML standard error \u2014 \\(\\sqrt{\\mathrm{diag}(\\mathbf{X V X'})}\\) \u2014 for the linear prediction itself, or for a quantity we can reach by transforming it: build the interval on the linear scale and map the endpoints through the link (a predicted probability from a logit or probit works this way). Use the delta method when the quantity of interest is a nonlinear function of the coefficients that we cannot reach by transforming a single linear prediction \u2014 a marginal effect that combines several coefficients, a ratio of coefficients, or a probability in a model like multinomial logit, where the quantity is not monotone in any one \\(x\\widehat\\beta\\). In the linear model, for \\(\\widehat y\\) itself the two agree exactly, and the reason is worth seeing.\n\n\n\n\n\n\nML or delta \u2014 which, and when?\n\n\n\nML standard error \u2014 \\(\\sqrt{\\mathrm{diag}(\\mathbf{X V X'})}\\) \u2014 for the linear prediction \\(x\\widehat\\beta\\) itself, or for a quantity we reach by transforming it monotonically (build the interval on the linear scale, then map the endpoints through the link).\nDelta method \u2014 for a nonlinear function of the coefficients we cannot reach that way: a marginal effect that combines several coefficients (the polynomial below), a ratio of coefficients, or a probability that is not monotone in any one \\(x\\widehat\\beta\\) (multinomial logit).\nIn OLS, for \\(\\widehat y\\) the two produce the same number.\n\n\nGreene (2012) gives the delta method\u2019s general form for a transformation \\(F\\),\n\\[\n\\mathrm{Var}\\!\\left[F(X\\widehat\\beta)\\right] = f(x'\\widehat\\beta)^2 \\; \\mathbf{x}'\\,\\mathbf{V}\\,\\mathbf{x},\n\\]\nwhere \\(f\\) is the derivative of \\(F\\). In OLS the quantity of interest is the linear prediction, so \\(F\\) is the identity, \\(f = 1\\), and the delta standard error of \\(\\widehat y\\) is just \\(\\sqrt{\\mathbf{x}'\\mathbf{V}\\mathbf{x}}\\) \u2014 the same se_lin we just built. In the linear model, delta and ML give the same number, because there is no nonlinear link to differentiate. In MLE, \\(f\\) is the link\u2019s PDF (0.25 at the peak for the logit, 0.399 for the probit), and the two standard errors part ways.\nWhere the delta method earns its keep within OLS is a nonlinear function of the coefficients \u2014 a linear combination whose standard error must carry the covariances between estimates. This is not a prediction sweep; we generate the quantities of interest over protest. It is a short worked example of how we get the standard error of a linear combination, and the quadratic Polity term is the natural vehicle for it. The marginal effect of Polity is not a single coefficient but\n\\[\n\\frac{\\partial \\, \\widehat y}{\\partial \\, \\text{polity}} = \\widehat\\beta_{p1} + 2\\,\\widehat\\beta_{p2}\\,\\text{polity},\n\\]\nand its standard error carries the covariance term:\n\\[\nse = \\sqrt{\\mathrm{Var}(\\widehat\\beta_{p1}) + 4\\,\\text{polity}^2\\,\\mathrm{Var}(\\widehat\\beta_{p2}) + 4\\,\\text{polity}\\,\\mathrm{Cov}(\\widehat\\beta_{p1}, \\widehat\\beta_{p2})}.\n\\]\nThe variances and the covariance come straight from \\(\\mathbf{V} =\\) vcov(m1):\n\n\ncode\nb &lt;- coef(m1); V &lt;- vcov(m1)\n\n# Two small functions, each taking a Polity value p (or a whole vector of them).\n# me_polity() returns the marginal effect b_p1 + 2*b_p2*p; se_polity() returns\n# its delta-method standard error. Both read the coefficient vector b and the\n# covariance matrix V from the two lines just above -- these are NOT passed in as\n# arguments, so the functions use whatever b and V currently hold. We call each\n# below on the vector `poly`, so they return one value per Polity value.\nme_polity &lt;- function(p) b[\"p1\"] + 2*b[\"p2\"]*p                 # marginal effect at polity = p\nse_polity &lt;- function(p) sqrt(V[\"p1\",\"p1\"] + 4*p^2*V[\"p2\",\"p2\"] +\n                              4*p*V[\"p1\",\"p2\"])                # its delta-method SE\n\npoly &lt;- seq(0, 20, 2)\ndata.frame(polity = poly,\n           marg_effect = as.numeric(me_polity(poly)),\n           se          = as.numeric(se_polity(poly))) %&gt;%\n  mutate(lb = marg_effect - 1.96*se, ub = marg_effect + 1.96*se)\n\n\n   polity marg_effect         se         lb        ub\n1       0 -0.01601086 0.35706137 -0.7158512 0.6838294\n2       2 -0.01607049 0.29739200 -0.5989588 0.5668178\n3       4 -0.01613011 0.23824773 -0.4830957 0.4508354\n4       6 -0.01618973 0.18014647 -0.3692768 0.3368973\n5       8 -0.01624935 0.12455648 -0.2603801 0.2278813\n6      10 -0.01630898 0.07711472 -0.1674538 0.1348359\n7      12 -0.01636860 0.06123592 -0.1363910 0.1036538\n8      14 -0.01642822 0.09446583 -0.2015812 0.1687248\n9      16 -0.01648784 0.14652148 -0.3036699 0.2706943\n10     18 -0.01654746 0.20343080 -0.4152718 0.3821769\n11     20 -0.01660709 0.26205044 -0.5302259 0.4970118\n\n\ncode\n# ---- MORE EFFICIENT -----------------------------------\n# marginaleffects::slopes() computes this marginal effect AND its delta-method\n# SE from the model object, with no hand-written covariance formula:\n#\n#   library(marginaleffects)\n#   slopes(m1, variables = \"p1\", newdata = datagrid(p1 = seq(0, 20, 2)))\n#\n# WHY it's better: you never transcribe the \"b1 + 2*b2*x\" derivative or the\n# variance formula, so there's nothing to get wrong -- marginaleffects\n# differentiates the prediction numerically and pulls the covariances from V\n# itself. The by-hand version is here so the covariance term is visible: that\n# middle 4*p*Cov(b1,b2) piece is exactly what a naive SE(b1) + SE(2*b2*x) would\n# miss.\n\n\nLook at the output. The marginal effect of Polity hovers around zero and its interval covers zero everywhere, because neither Polity term is doing anything in this model. That is the point, not a defect: the procedure for the standard error of a linear combination is identical whether or not the effect is real \u2014 we compute it the same way for a null result and a strong one. And notice what the covariance term buys us. Dropping it \u2014 pretending \\(\\mathrm{Cov}(\\widehat\\beta_{p1}, \\widehat\\beta_{p2}) = 0\\) \u2014 gives the wrong standard error, because in a polynomial the two coefficients are strongly correlated by construction."
  },
  {
    "objectID": "predictionOLS26.html#confidence-intervals-by-end-point-transformation",
    "href": "predictionOLS26.html#confidence-intervals-by-end-point-transformation",
    "title": "Prediction Methods in the Linear Model",
    "section": "Confidence intervals by end-point transformation",
    "text": "Confidence intervals by end-point transformation\nEither standard error can build the interval. Take the point estimate plus and minus 1.96 times the ML or the delta standard error, transforming the endpoints if the quantity is transformed:\n\\[\nF(X\\widehat\\beta - 1.96\\,se) \\;\\le\\; F(X\\widehat\\beta) \\;\\le\\; F(X\\widehat\\beta + 1.96\\,se).\n\\]\nThe choice between the two matters only for a nonlinear quantity. An interval built on the linear predictor with the ML standard error and then transformed stays inside the natural bounds \u2014 a probability stays in \\([0,1]\\) \u2014 and comes out asymmetric. An interval built directly on the transformed quantity with the delta standard error is symmetric, and can fall outside those bounds. In OLS neither issue arises: \\(F\\) is the identity, so the interval is the symmetric \\(\\widehat y \\pm 1.96\\,se\\) from the at-means figure, and the ML and delta standard errors are the same number to begin with. In a nonlinear model \\(F\\) is the link, and the difference between the two intervals is real.\n\n\n\n\n\n\nEither standard error builds a confidence interval\n\n\n\nTake the point estimate \\(\\pm\\,1.96 \\times se\\) with either the ML or the delta standard error, transforming the endpoints if the quantity is transformed. The choice matters only for a nonlinear quantity: the ML interval (built on the linear scale, then transformed) stays in bounds and is asymmetric; the delta interval (built on the response) is symmetric and can leave the bounds. In OLS they coincide."
  },
  {
    "objectID": "predictionOLS26.html#simulating-the-parameters-the-expected-value-confidence-interval",
    "href": "predictionOLS26.html#simulating-the-parameters-the-expected-value-confidence-interval",
    "title": "Prediction Methods in the Linear Model",
    "section": "Simulating the parameters \u2014 the expected value (confidence interval)",
    "text": "Simulating the parameters \u2014 the expected value (confidence interval)\nTreat \\(\\widehat\\beta\\) as the mean of a multivariate normal and \\(\\mathbf{V}\\) as its variance, draw 10,000 coefficient vectors, and for each value of protest form the 10,000 linear predictions and take the percentiles.\n\n\ncode\nB &lt;- data.frame(rmvnorm(n = 10000, mean = coef(m1), sigma = vcov(m1)))\ncolnames(B) &lt;- c(\"b0\", \"b_ra\", \"b_cw\", \"b_pro\", \"b_p1\", \"b_p2\", \"b_gdp\")\n\nsim.exp &lt;- data.frame(lb = numeric(0), med = numeric(0), ub = numeric(0), pr = numeric(0))\n\nfor (i in seq(0, 37, 1)) {\n  xbR &lt;- quantile(B$b0 + B$b_ra*0 + B$b_cw*0 + B$b_pro*i +\n                  B$b_p1*p1med + B$b_p2*p2med + B$b_gdp*gdpmed,\n                  probs = c(.025, .5, .975))\n  sim.exp[i + 1, 1:4] &lt;- data.frame(t(xbR), i)\n}\n\nsim.exp %&gt;% head()\n\n\n        lb      med       ub pr\n1 4.172945 5.420639 6.665929  0\n2 4.475042 5.661991 6.850677  1\n3 4.757268 5.904290 7.036988  2\n4 5.029860 6.145498 7.259408  3\n5 5.283568 6.387826 7.493155  4\n6 5.516782 6.633641 7.752399  5\n\n\ncode\n# ---- MORE EFFICIENT -----------------------------------\n# Draw with MASS::mvrnorm, build all the linear predictions as one matrix\n# multiply instead of looping, and skip the data.frame-row assignment:\n#\n#   sims &lt;- MASS::mvrnorm(10000, coef(m1), vcov(m1))       # 10000 x k\n#   grid &lt;- seq(0, 37, 1)\n#   Xg   &lt;- model.matrix(~ lagRA + civilwar + lagprotest + p1 + p2 + loggdp,\n#             data = data.frame(lagRA = 0, civilwar = 0, lagprotest = grid,\n#                               p1 = p1med, p2 = p2med, loggdp = gdpmed))\n#   XB   &lt;- Xg %*% t(sims)                                 # length(grid) x 10000\n#   ci   &lt;- t(apply(XB, 1, quantile, c(.025, .5, .975)))   # one row per protest value\n#\n# WHY it's better: the matrix multiply replaces the loop and the fragile\n# \"i + 1\" indexing, and building x'b from model.matrix means you never re-type\n# the linear predictor by hand (where a dropped term hides). The manual loop is\n# here because writing x'b out term-by-term makes it obvious what's being\n# simulated."
  },
  {
    "objectID": "predictionOLS26.html#simulating-the-quantity-of-interest-the-predicted-value-prediction-interval",
    "href": "predictionOLS26.html#simulating-the-quantity-of-interest-the-predicted-value-prediction-interval",
    "title": "Prediction Methods in the Linear Model",
    "section": "Simulating the quantity of interest \u2014 the predicted value (prediction interval)",
    "text": "Simulating the quantity of interest \u2014 the predicted value (prediction interval)\nSame draws of the parameters, but now we also add the fundamental uncertainty: for each simulated \\(x\\beta^{*}\\) we add a disturbance draw \\(\\varepsilon^{*} \\sim N(0, \\widehat\\sigma^2)\\), where \\(\\widehat\\sigma\\) is the residual standard error, and take percentiles of the predicted value \\(\\widetilde y = x\\beta^{*} + \\varepsilon^{*}\\).\n\n\ncode\nsigma_hat &lt;- sigma(m1)   # residual standard error -- the scatter around the line\n\nsim.pred &lt;- data.frame(lb = numeric(0), med = numeric(0), ub = numeric(0), pr = numeric(0))\n\nfor (i in seq(0, 37, 1)) {\n  xstar &lt;- B$b0 + B$b_pro*i + B$b_p1*p1med + B$b_p2*p2med + B$b_gdp*gdpmed  # binaries = 0\n  ystar &lt;- xstar + rnorm(length(xstar), mean = 0, sd = sigma_hat)          # add fundamental noise\n  q &lt;- quantile(ystar, probs = c(.025, .5, .975))\n  sim.pred[i + 1, 1:4] &lt;- data.frame(t(q), i)\n}\n\nsim.pred %&gt;% head()\n\n\n         lb      med       ub pr\n1 -14.60143 5.400084 25.34124  0\n2 -14.50950 5.690950 25.64214  1\n3 -14.19927 6.174480 26.09840  2\n4 -14.17129 5.990618 26.02624  3\n5 -13.31731 6.505752 26.59697  4\n6 -13.13051 6.534256 26.45667  5\n\n\nNow put the two intervals on one plot, and overlay what predict() gives directly \u2014 the simulation should reproduce the analytic confidence and prediction intervals.\n\n\ncode\n# analytic confidence and prediction intervals straight from predict()\ncheck &lt;- data.frame(lagprotest = oosdata$lagprotest,\n  predict(m1, newdata = oosdata, interval = \"confidence\"),\n  predict(m1, newdata = oosdata, interval = \"prediction\")) %&gt;%\n  rename(ci_lb = lwr, ci_ub = upr, pi_lb = lwr.1, pi_ub = upr.1)\n\nggplot() +\n  # simulated prediction interval (wide) and confidence interval (narrow)\n  geom_ribbon(data = sim.pred, aes(x = pr, ymin = lb, ymax = ub),\n              fill = bu_lightgreen, alpha = .25) +\n  geom_ribbon(data = sim.exp,  aes(x = pr, ymin = lb, ymax = ub),\n              fill = bu_green, alpha = .35) +\n  geom_line(data = sim.exp, aes(x = pr, y = med), color = bu_green, linewidth = 1) +\n  # analytic checks, dashed, in dark ink so they read on the bands\n  geom_line(data = check, aes(lagprotest, ci_lb), color = bu_black, linetype = \"dashed\") +\n  geom_line(data = check, aes(lagprotest, ci_ub), color = bu_black, linetype = \"dashed\") +\n  geom_line(data = check, aes(lagprotest, pi_lb), color = bu_black, linetype = \"dotted\") +\n  geom_line(data = check, aes(lagprotest, pi_ub), color = bu_black, linetype = \"dotted\") +\n  geom_hline(yintercept = 0, color = bu_black, linewidth = .3) +\n  annotate(\"text\", x = 2, y = 11, label = \"confidence interval\\n(expected value)\",\n           hjust = 0, size = 3, color = bu_green) +\n  annotate(\"text\", x = 20, y = 26, label = \"prediction interval\\n(predicted value)\",\n           hjust = 0, size = 3, color = bu_black) +\n  labs(title = \"Two simulations: expected value vs. predicted value\",\n       subtitle = \"solid/dark band = confidence interval; pale band = prediction interval; dashed/dotted = predict()\",\n       x = \"Protests (t-1)\", y = \"Scarring torture\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\nThe simulated bands land on the analytic ones \u2014 the dashed confidence bounds sit on the dark band, the dotted prediction bounds on the pale one. That is the payoff: the two \u201ctypes of simulation\u201d are not two techniques for the same quantity, they are two quantities. The confidence interval is our uncertainty about the average; the prediction interval is our uncertainty about a case, and it is much wider because it carries \\(\\widehat\\sigma\\).\nAnd look where the prediction interval goes \u2014 below zero, for a variable that counts torture allegations and cannot be negative. OLS does not know scarring is a bounded count, so it puts real probability mass on impossible values. Nothing we do to the uncertainty technique fixes that; the fix is a different model, one whose link function keeps predictions non-negative (see count models in a few weeks)"
  },
  {
    "objectID": "predictionOLS26.html#simulating-combinations-of-binary-variables",
    "href": "predictionOLS26.html#simulating-combinations-of-binary-variables",
    "title": "Prediction Methods in the Linear Model",
    "section": "Simulating combinations of binary variables",
    "text": "Simulating combinations of binary variables\nSimulation also lets us compare discrete scenarios and see the whole predicted distribution, not just a point and an interval. Here are the four combinations of restricted access and civil war, holding protest, Polity, and GDP at their medians, each as a density of simulated expected values.\n\n\ncode\n# xb_scen() builds the simulated linear prediction for one access/war scenario.\n# Its arguments are ra (restricted access, 0/1) and cw (civil war, 0/1). The\n# other pieces come from the environment: the simulated coefficient columns\n# B$... were drawn in the simulation chunk above, and the held medians\n# promed/p1med/p2med/gdpmed in the estimation-sample chunk. It returns a full\n# column of 10,000 simulated predictions. We call it four times, once per\n# combination of ra and cw:\nxb_scen &lt;- function(ra, cw) {\n  B$b0 + B$b_ra*ra + B$b_cw*cw + B$b_pro*promed +\n  B$b_p1*p1med + B$b_p2*p2med + B$b_gdp*gdpmed\n}\n\ncombos &lt;- data.frame(ra0cw0 = xb_scen(0, 0), ra1cw0 = xb_scen(1, 0),\n                     ra0cw1 = xb_scen(0, 1), ra1cw1 = xb_scen(1, 1))\n\nggplot(combos) +\n  geom_density(aes(ra0cw0), fill = bu_green,      alpha = .3) +\n  geom_density(aes(ra1cw0), fill = bu_lightgreen, alpha = .3) +\n  geom_density(aes(ra0cw1), fill = bu_palegreen,  alpha = .3) +\n  geom_density(aes(ra1cw1), fill = bu_grey,       alpha = .4) +\n  annotate(\"text\", x = 5.7,  y = .55, label = \"open access,\\nno civil war\",   color = bu_black, size = 3) +\n  annotate(\"text\", x = 13.6, y = .45, label = \"open access,\\ncivil war\",      color = bu_black, size = 3) +\n  annotate(\"text\", x = 16.8, y = .35, label = \"restricted access,\\nno civil war\", color = bu_green, size = 3) +\n  annotate(\"text\", x = 24.7, y = .30, label = \"restricted access,\\ncivil war\",    color = bu_black, size = 3) +\n  labs(title = \"Simulated expected scarring for four scenarios\",\n       x = \"Expected scarring torture\", y = \"density\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\nFour clearly separated distributions. Restricted access shifts the whole distribution right by about eleven allegations; civil war by about eight; together they stack. And because we have the full simulated distribution, not just a point, we can read directly the probability that, say, a restricted-access country in civil war has higher expected scarring than an open one at peace \u2014 the overlap between two of these densities. That is the flexibility King, Tomz, and Wittenberg were after, and it is why simulation is the default when the quantity gets complicated."
  },
  {
    "objectID": "ex12026answers.html",
    "href": "ex12026answers.html",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "",
    "text": "Answer key for Exercise #1. The code is folded \u2014 open it to see how a piece was built."
  },
  {
    "objectID": "ex12026answers.html#cdfs-and-pdfs",
    "href": "ex12026answers.html#cdfs-and-pdfs",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "1. CDFs and PDFs",
    "text": "1. CDFs and PDFs\n\n1. Please replicate this figure. What do the PDFs describe? What do the CDFs describe?\n\n\n\ncode\nz  &lt;- seq(-4, 4, length.out = 1000)\ndz &lt;- z[2] - z[1]\n\n# Normal: density written by hand, then accumulated to get the CDF. The\n# trapezoid rule averages consecutive heights. This is Phi(z) - Phi(-4); the\n# mass below -4 is about .00003.\nnormal_pdf &lt;- (1 / sqrt(2 * pi)) * exp(-z^2 / 2)\nnormal_cdf &lt;- c(0, cumsum((head(normal_pdf, -1) + tail(normal_pdf, -1)) / 2 * dz))\n\ndists &lt;- tibble(\n  z,\n  Logistic = 1 / (1 + exp(-z)),\n  Normal   = normal_cdf,\n  Cauchy   = 0.5 + atan(z) / pi\n) %&gt;%\n  pivot_longer(-z, names_to = \"distribution\", values_to = \"cdf\") %&gt;%\n  mutate(distribution = factor(distribution, levels = c(\"Logistic\", \"Normal\", \"Cauchy\")))\n\ndensities &lt;- tibble(\n  z,\n  Logistic = exp(-z) / (1 + exp(-z))^2,\n  Normal   = normal_pdf,\n  Cauchy   = 1 / (pi * (1 + z^2))\n) %&gt;%\n  pivot_longer(-z, names_to = \"distribution\", values_to = \"pdf\") %&gt;%\n  mutate(distribution = factor(distribution, levels = c(\"Logistic\", \"Normal\", \"Cauchy\")))\n\np_cdf &lt;- ggplot(dists, aes(z, cdf, color = distribution, linetype = distribution)) +\n  geom_line(linewidth = 0.9) +\n  scale_color_manual(values = series_colors) +\n  scale_linetype_manual(values = series_lines) +\n  labs(title = \"CDFs\", x = expression(x*beta), y = \"Pr(y = 1)\",\n       color = NULL, linetype = NULL) +\n  theme_bu() +\n  theme(legend.position = \"bottom\", plot.title = element_text(size = 12))\n\np_pdf &lt;- ggplot(densities, aes(z, pdf, color = distribution, linetype = distribution)) +\n  geom_line(linewidth = 0.9) +\n  scale_color_manual(values = series_colors) +\n  scale_linetype_manual(values = series_lines) +\n  labs(title = \"PDFs\", x = expression(x*beta), y = \"density\",\n       color = NULL, linetype = NULL) +\n  theme_bu() +\n  theme(legend.position = \"bottom\", plot.title = element_text(size = 12))\n\n(p_cdf + p_pdf) +\n  plot_layout(guides = \"collect\") +\n  plot_annotation(\n    title = \"Logistic, normal, and Cauchy distributions\",\n    subtitle = \"The CDF maps the linear predictor into [0,1]; the PDF is its slope\",\n    theme = theme_bu()\n  ) &\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure\u00a01\n\n\n\n\n\nThe CDF returns \\(Pr(y = 1)\\) at any value of \\(x\\beta\\) \u2014 probability accumulated up to that point. It takes the real line, where \\(x\\beta\\) lives, and squashes it into \\([0,1]\\), where a probability has to live.\nThe PDF is its derivative: how fast the probability changes at a given \\(x\\beta\\). Tallest where the CDF is steepest, near zero in the tails where the CDF has flattened. A marginal effect is a slope, so this is the curve we want later."
  },
  {
    "objectID": "ex12026answers.html#three-links",
    "href": "ex12026answers.html#three-links",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "2. Three links",
    "text": "2. Three links\n\n2. Please replicate this figure \u2014 it overlays the CDFs of the logistic, the complementary log-log (cloglog), and the log-log. Comment on the differences among the three.\n\n\n\ncode\nlinks &lt;- tibble(\n  z,\n  Logistic = 1 / (1 + exp(-z)),\n  Cloglog  = 1 - exp(-exp(z)),\n  Loglog   = exp(-exp(-z))\n) %&gt;%\n  pivot_longer(-z, names_to = \"link\", values_to = \"p\") %&gt;%\n  mutate(link = factor(link, levels = c(\"Logistic\", \"Cloglog\", \"Loglog\")))\n\nggplot(links, aes(z, p, color = link, linetype = link)) +\n  geom_line(linewidth = 0.9) +\n  geom_hline(yintercept = 0.5, color = bu_grey, linetype = \"dashed\", linewidth = 0.4) +\n  scale_color_manual(values = series_colors) +\n  scale_linetype_manual(values = series_lines) +\n  labs(title = \"Logistic, cloglog, and loglog CDFs\",\n       x = expression(x*beta), y = \"Pr(y = 1)\",\n       color = NULL, linetype = NULL) +\n  theme_bu() +\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure\u00a02\n\n\n\n\n\nThe logistic is symmetric: \\(\\Lambda(-z) = 1 - \\Lambda(z)\\), so it crosses .5 at \\(x\\beta = 0\\) and approaches 1 the way it approaches 0, run backwards. The cloglog and loglog are asymmetric, and they mirror each other rather than themselves. The cloglog, \\(1 - \\exp(-\\exp(z))\\), leaves zero gradually then rushes to one, crossing .5 at about \\(-0.37\\); the loglog does the reverse, crossing at about \\(+0.37\\).\nThat asymmetry is why we keep them. When ones are rare the action is in the bottom left, and that is where the three disagree most: a link that leaves zero slowly puts more mass down there than the logit does at the same \\(x\\beta\\), one that leaves abruptly puts less. We are asking the data to tell very small probabilities apart, so the shape of that tail does real work. Hence the cloglog in rare-events work and in discrete-time hazards, where it is also the exact discrete-time analogue of a constant underlying hazard."
  },
  {
    "objectID": "ex12026answers.html#intercept-shifts",
    "href": "ex12026answers.html#intercept-shifts",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "3. Intercept shifts",
    "text": "3. Intercept shifts\n\n3. Please replicate this figure \u2014 it overlays the logistic CDF with intercepts of \\(-1\\), \\(0\\), and \\(+1\\). Please describe what happens as the intercept changes. Then compare these intercept shifts to intercept shifts in the linear model: how are the two similar, and how are they different?\n\n\n\ncode\nshifts &lt;- tibble(\n  z,\n  `Intercept = -1` = 1 / (1 + exp(-(z - 1))),\n  `Intercept = 0`  = 1 / (1 + exp(-z)),\n  `Intercept = +1` = 1 / (1 + exp(-(z + 1)))\n) %&gt;%\n  pivot_longer(-z, names_to = \"intercept\", values_to = \"p\") %&gt;%\n  mutate(intercept = factor(intercept,\n                            levels = c(\"Intercept = -1\", \"Intercept = 0\", \"Intercept = +1\")))\n\nggplot(shifts, aes(z, p, color = intercept, linetype = intercept)) +\n  geom_line(linewidth = 0.9) +\n  geom_hline(yintercept = 0.5, color = bu_grey, linetype = \"dashed\", linewidth = 0.4) +\n  scale_color_manual(values = series_colors) +\n  scale_linetype_manual(values = series_lines) +\n  labs(title = \"Logistic CDFs under intercept shifts\",\n       x = expression(x*beta), y = \"Pr(y = 1)\",\n       color = NULL, linetype = NULL) +\n  theme_bu() +\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure\u00a03\n\n\n\n\n\nChanging the intercept slides the curve sideways and does nothing else. Raise it by one and the curve moves one unit left, so the probability reaches one-half one unit earlier. Steepness and tails are identical across all three.\nSame as the linear model: the intercept sets the level and leaves the slopes alone. Add \\(c\\) and every \\(x\\beta\\) goes up by \\(c\\). On the \\(x\\beta\\) scale the logit is linear, so the two behave identically there.\nDifferent from the linear model on the scale we care about. In OLS the prediction is \\(x\\beta\\), so a one-unit shift moves predicted \\(y\\) one unit, everywhere, for everybody. In the logit the prediction is \\(\\Lambda(x\\beta)\\), so the same shift moves the probability a lot in the steep middle and hardly at all in the flat tails \u2014 off the figure, about .23 at \\(x\\beta = 0\\) and about .03 at \\(x\\beta = -4\\). Constant on the \\(x\\beta\\) scale, not constant on the probability scale. That is why a logit coefficient is not a marginal effect."
  },
  {
    "objectID": "ex12026answers.html#the-skewed-logit-scobit",
    "href": "ex12026answers.html#the-skewed-logit-scobit",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "4. The skewed logit (scobit)",
    "text": "4. The skewed logit (scobit)\n\n4. Please replicate this figure \u2014 it overlays the skewed logit (scobit) CDF at shape parameters \\(\\alpha = 1\\), \\(\\alpha = 2\\), and \\(\\alpha = 0.5\\). Please describe what happens as the shape parameter changes. How are these curves similar to, and different from, the logistic CDF in question 3?\n\n\n\ncode\nscobit &lt;- tibble(\n  z,\n  `alpha = 1`   = 1 / (1 + exp(-z))^1,\n  `alpha = 2`   = 1 / (1 + exp(-z))^2,\n  `alpha = 0.5` = 1 / (1 + exp(-z))^0.5\n) %&gt;%\n  pivot_longer(-z, names_to = \"shape\", values_to = \"p\") %&gt;%\n  mutate(shape = factor(shape, levels = c(\"alpha = 1\", \"alpha = 2\", \"alpha = 0.5\")))\n\nggplot(scobit, aes(z, p, color = shape, linetype = shape)) +\n  geom_line(linewidth = 0.9) +\n  geom_hline(yintercept = 0.5, color = bu_grey, linetype = \"dashed\", linewidth = 0.4) +\n  scale_color_manual(values = series_colors,\n                     labels = c(expression(alpha == 1), expression(alpha == 2),\n                                expression(alpha == 0.5))) +\n  scale_linetype_manual(values = series_lines,\n                        labels = c(expression(alpha == 1), expression(alpha == 2),\n                                   expression(alpha == 0.5))) +\n  labs(title = \"Skewed logit (scobit) CDFs\",\n       x = expression(x*beta), y = \"Pr(y = 1)\",\n       color = NULL, linetype = NULL) +\n  theme_bu() +\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure\u00a04\n\n\n\n\n\nAt \\(\\alpha = 1\\) the scobit is the logit \u2014 raise \\((1 + e^{-z})^{-1}\\) to the first power and you have written \\(\\Lambda(z)\\). The logit is the special case, so \u201cis the logit good enough here?\u201d becomes a hypothesis about a single number rather than a matter of taste.\nMove \\(\\alpha\\) off 1 and the curve bends, which is a different operation from question 3. An intercept shift is a rigid translation \u2014 same curve, new location. Changing \\(\\alpha\\) deforms it: the steepest point moves off \\(Pr(y = 1) = .5\\) and the tails stop mirroring each other. At \\(\\alpha = 2\\) the curve hugs zero longer and is steepest below .5; at \\(\\alpha = 0.5\\) it lifts off early and is steepest above .5.\nNagler\u2019s motivation: the logit forces the largest marginal effects to land at \\(Pr(y = 1) = .5\\), for everyone, always, by construction. That is an assumption, and a strong one. If the people most responsive to \\(x\\) are not the ones sitting at fifty-fifty \u2014 for turnout, the argument is that they are not \u2014 you want that location estimated rather than imposed. That is what \\(\\alpha\\) does."
  },
  {
    "objectID": "ex12026answers.html#simulate-a-binary-outcome",
    "href": "ex12026answers.html#simulate-a-binary-outcome",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "5. Simulate a binary outcome",
    "text": "5. Simulate a binary outcome\n\n5. Please write code that simulates data for a binary \\(y\\) variable from the equation \\(x\\beta = \\beta_0 + \\beta_1 x_1 + \\beta_2 x_2\\), with \\(\\beta_0 = -1\\), \\(\\beta_1 = 0.5\\), and \\(\\beta_2 = -0.5\\), with a logit link. Let \\(x_1\\) be continuous and \\(x_2\\) be binary. The output should be a data frame of 1,000 observations containing a binary \\(y\\), a constant, and the two \\(x\\) variables.\n\nThe data-generating process, stated before any code is written:\n\\[\n\\begin{aligned}\nx_{1i} &\\sim \\text{Normal}(0,1), \\qquad x_{2i} \\sim \\text{Bernoulli}(0.5) \\\\\nx_i\\beta &= -1 + 0.5\\,x_{1i} - 0.5\\,x_{2i} \\\\\n\\pi_i &= \\Lambda(x_i\\beta) \\;=\\; \\frac{1}{1 + e^{-x_i\\beta}} \\\\\ny_i &\\sim \\text{Bernoulli}(\\pi_i)\n\\end{aligned}\n\\]\nThe third line is the link: take \\(x_i\\beta\\), free to be any real number, and hand back a probability. Write \\(\\Phi(x_i\\beta)\\) instead and you have simulated a probit; write \\(1 - \\exp(-\\exp(x_i\\beta))\\) and you have a cloglog. Nothing above or below that line changes.\nThe last two lines are the model. The systematic part produces a probability, one per observation; the stochastic part turns it into a zero or a one with a coin weighted by \\(\\pi_i\\). Two observations with identical \\(x\\)\u2019s get identical \\(\\pi\\)\u2019s and can still get different \\(y\\)\u2019s. That is why we estimate \\(\\pi\\) rather than \\(y\\).\n\n\ncode\nset.seed(8675309)\n\nn  &lt;- 1000\nx1 &lt;- rnorm(n)                # continuous\nx2 &lt;- rbinom(n, 1, 0.5)       # binary\n\nxb &lt;- -1 + 0.5 * x1 - 0.5 * x2\n\n# The logit link, written out rather than calling plogis(). Call it `pr`, not\n# `pi` -- in R, `pi` is already 3.14159..., and overwriting it breaks any later\n# line that needs the real one.\npr &lt;- 1 / (1 + exp(-xb))\ny  &lt;- rbinom(n, 1, pr)        # the coin flip: y is random even given pr\n\nsim &lt;- tibble(y, const = 1, x1, x2)\n\nc(n = n, share_ones = round(mean(sim$y), 3), mean_pr = round(mean(pr), 3))\n\n\n         n share_ones    mean_pr \n  1000.000      0.239      0.236 \n\n\nAbout 0.239 of the observations are ones, close to the mean of the \\(\\pi_i\\)\u2019s, 0.236, as it should be. Both sit below one-half, which is what the \\(-1\\) intercept gets us: at \\(x_1 = 0\\), \\(x_2 = 0\\) the probability is \\(\\Lambda(-1) = 0.269\\).\nWe also have the true \\(\\beta = (-1, 0.5, -0.5)\\), because we chose it. In real data we never do, which is why simulated data is the place to test an estimator like this."
  },
  {
    "objectID": "ex12026answers.html#the-likelihood-and-log-likelihood",
    "href": "ex12026answers.html#the-likelihood-and-log-likelihood",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "6. The likelihood and log-likelihood",
    "text": "6. The likelihood and log-likelihood\n\n6. Please write (in proper math notation, not code) the likelihood and log-likelihood functions for a binary \\(y\\) variable using the logit as the link function. Describe the parts of the log-likelihood and what each is doing.\n\nStart with the general form; it is the same for every binary model, and only one piece changes. Each observation contributes the probability of the outcome we observed for it: \\(\\pi_i\\) when \\(y_i = 1\\), \\(1 - \\pi_i\\) when \\(y_i = 0\\). Putting \\(y_i\\) and \\(1 - y_i\\) in the exponents writes that as one term \u2014 one exponent is always zero, so one factor always drops out.\n\\[\n\\begin{aligned}\nL(\\beta \\mid y) &= \\prod_{i=1}^{n} \\pi_i^{\\,y_i}\\,(1 - \\pi_i)^{\\,1 - y_i} \\\\\n\\ell(\\beta \\mid y) &= \\ln L(\\beta \\mid y)\n                    = \\sum_{i=1}^{n}\\Big[\\, y_i \\ln \\pi_i + (1 - y_i)\\ln(1 - \\pi_i)\\Big]\n\\end{aligned}\n\\]\nwith \\(\\pi_i = F(x_i\\beta)\\) for whatever link \\(F\\) we chose. For the logit, \\(\\pi_i = \\Lambda(x_i\\beta) = 1/(1 + e^{-x_i\\beta})\\), and\n\\[\n\\begin{aligned}\nL(\\beta \\mid y) &= \\prod_{i=1}^{n} \\Lambda(x_i\\beta)^{\\,y_i}\\,\n                   \\big[1 - \\Lambda(x_i\\beta)\\big]^{\\,1 - y_i} \\\\\n\\ell(\\beta \\mid y) &= \\sum_{i=1}^{n} \\Big[\\, y_i \\ln \\Lambda(x_i\\beta)\n                      + (1 - y_i)\\ln\\big(1 - \\Lambda(x_i\\beta)\\big) \\Big]\n\\end{aligned}\n\\]\nNote where the link sits \u2014 inside both terms and nowhere else. Swap \\(\\Lambda\\) for \\(\\Phi\\) and you have the probit; exchange it for \\(1 - \\exp(-\\exp(\\cdot))\\) and you have the cloglog."
  },
  {
    "objectID": "ex12026answers.html#the-log-likelihood-in-r",
    "href": "ex12026answers.html#the-log-likelihood-in-r",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "7. The log-likelihood in R",
    "text": "7. The log-likelihood in R\n\n7. Please write the log-likelihood function from above in R code, as a function. It should take as arguments a vector of parameters, a matrix of \\(X\\) variables, and a \\(y\\) variable, and return a single number (the log-likelihood). Using the data from question #5 for \\(X\\) and \\(y\\), evaluate the log-likelihood at the true \\(\\beta\\)\u2019s from question #5; the \\(\\beta\\)\u2019s from a glm logit of the data and equation from question #5; \\(\\beta\\) values of 0, 0, 0; and \\(\\beta\\) values of 1, -.5, .5. Present the log-likelihoods in a professional table and comment.\n\n\n\ncode\n# Build xb, transform it through the link to get probabilities, sum the two\n# terms. Three arguments in, one number out.\nlogit_ll &lt;- function(beta, X, y) {\n  xb &lt;- as.vector(X %*% beta)\n  p  &lt;- 1 / (1 + exp(-xb))\n  sum(y * log(p) + (1 - y) * log(1 - p))\n}\n\nX &lt;- cbind(const = 1, x1 = sim$x1, x2 = sim$x2)\n\nb_hat &lt;- coef(glm(y ~ x1 + x2, family = binomial(link = \"logit\"), data = sim))\n\nll_table &lt;- tibble(\n  `beta` = c(\"truth (-1, 0.5, -0.5)\",\n             \"glm logit estimate\",\n             \"all zeros (0, 0, 0)\",\n             \"sign-flipped (1, -0.5, 0.5)\"),\n  `log-likelihood` = c(\n    logit_ll(c(-1, 0.5, -0.5), X, sim$y),\n    logit_ll(b_hat,            X, sim$y),\n    logit_ll(c(0, 0, 0),       X, sim$y),\n    logit_ll(c(1, -0.5, 0.5),  X, sim$y)\n  )\n)\n\nknitr::kable(ll_table, digits = 3, align = \"lr\",\n             caption = \"The logit log-likelihood evaluated at four values of beta\")\n\n\n\nThe logit log-likelihood evaluated at four values of beta\n\n\nbeta\nlog-likelihood\n\n\n\n\ntruth (-1, 0.5, -0.5)\n-521.555\n\n\nglm logit estimate\n-521.374\n\n\nall zeros (0, 0, 0)\n-693.147\n\n\nsign-flipped (1, -0.5, 0.5)\n-1296.000\n\n\n\n\n\nglm returns (-1.014, 0.526, -0.434) when the true parameters are \\((-1, 0.5, -0.5)\\).\nThe function prefers the glm estimate \u2014 highest of the four, with the truth second by about a tenth of a point. The estimate beats the truth, which might suprise you, but it shouldn\u2019t. The log-likelihood is a statement about this sample, and \\(\\hat\\beta\\) is by construction the value that makes these thousand observations most probable. The true \\(\\beta\\) generated the sample but does not fit it best, because the sample carries noise too. Re-run with a different seed and \\(\\hat\\beta\\) moves around the truth; let \\(n\\) grow and the two will converge.\nNote that we have not maximized anything. We wrote a function and evaluated it at four points, and picking the best of four is not estimation. Finding the \\(\\beta\\) that beats every other \\(\\beta\\) is the problem, and it is what maximization is for."
  },
  {
    "objectID": "ex12026answers.html#logit-and-probit-on-the-ces-data",
    "href": "ex12026answers.html#logit-and-probit-on-the-ces-data",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "8. Logit and probit on the CES data",
    "text": "8. Logit and probit on the CES data\n\n8. Please estimate logit and probit models predicting vote choice as a function of whatever variables you think are relevant. Present a professional table of the estimates in side-by-side columns and discuss. Explain your thinking about the models \u2014 why did you include these variables, and what did you expect?\n\nBinary vote choice \u2014 Biden = 1, Trump = 0, everyone else missing \u2014 predicted by age, an indicator for white respondents, and a pro-choice indicator.\nWhy these three. The abortion item is the best available proxy for the cultural dimension that dominated the 2020 vote, so I expect the largest effect by far, positive. Race has been the most durable cleavage in American voting for fifty years, so white respondents less likely to vote Biden. Age I include as a control and as a continuous variable over which to plot predictions.\nWe do not drop the missing cases by hand; glm does the listwise deletion, because we need to recover exactly which rows it used when we predict in question 10.\n\n\ncode\nces &lt;- read_csv(here::here(\"data\", \"ces.csv\"), show_col_types = FALSE)\n\n# Recode: a binary vote, and two binary predictors. Anything that isn't a Biden\n# or Trump vote -- didn't vote, other, not sure -- is missing, not zero.\ncesa &lt;- ces %&gt;%\n  mutate(\n    vote  = case_when(votechoice == \"Joe Biden (Democrat)\"         ~ 1,\n                      votechoice == \"Donald J. Trump (Republican)\" ~ 0,\n                      .default = NA_real_),\n    white = if_else(race == \"White\", 1, 0),\n    pro   = if_else(prochoice == \"Support\", 1, 0)\n  )\n\nm_logit  &lt;- glm(vote ~ age + white + pro, family = binomial(link = \"logit\"),\n                data = cesa)\nm_probit &lt;- glm(vote ~ age + white + pro, family = binomial(link = \"probit\"),\n                data = cesa)\n\nmodal &lt;- function(x) as.numeric(names(which.max(table(x))))\nmodal(cesa$pro)\n\n\n[1] 1\n\n\ncode\nmodelsummary(list(\"Logit\" = m_logit, \"Probit\" = m_probit),\n             stars = TRUE, gof_map = c(\"nobs\", \"aic\", \"logLik\"),\n             coef_rename = c(\"age\" = \"Age\", \"white\" = \"White\",\n                             \"pro\" = \"Pro-choice\"),\n             title = \"Vote for Biden, 2020 CES: logit and probit estimates\")\n\n\n\n\n    \n\n    \n    \n      \n        \n        Vote for Biden, 2020 CES: logit and probit estimates\n              \n                 \n                Logit\n                Probit\n              \n        \n        + p &lt; 0.1, * p &lt; 0.05, ** p &lt; 0.01, *** p &lt; 0.001\n        \n                \n                  (Intercept)\n                  0.408***  \n                  0.159***  \n                \n                \n                             \n                  (0.051)   \n                  (0.029)   \n                \n                \n                  Age        \n                  -0.014*** \n                  -0.008*** \n                \n                \n                             \n                  (0.001)   \n                  (0.000)   \n                \n                \n                  White      \n                  -1.081*** \n                  -0.591*** \n                \n                \n                             \n                  (0.033)   \n                  (0.018)   \n                \n                \n                  Pro-choice \n                  2.851***  \n                  1.707***  \n                \n                \n                             \n                  (0.026)   \n                  (0.014)   \n                \n                \n                  Num.Obs.   \n                  43225     \n                  43225     \n                \n                \n                  AIC        \n                  40007.9   \n                  40058.4   \n                \n                \n                  Log.Lik.   \n                  -19999.930\n                  -20025.177\n                \n        \n      \n    \n\n\n\nThe two models tell the same story, as they must \u2014 two links on the same binomial likelihood, so they agree on signs, significance, and which predictor matters most. What differs is scale: the logit estimates run larger by a factor of about 1.8, which is not a finding but an artifact of the two distributions\u2019 variances. The standard logistic has variance \\(\\pi^2/3 \\approx 3.29\\), the standard normal has variance 1, and \\(\\sqrt{3.29} = 1.81\\)."
  },
  {
    "objectID": "ex12026answers.html#odds-ratios",
    "href": "ex12026answers.html#odds-ratios",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "9. Odds ratios",
    "text": "9. Odds ratios\n\n9. Please compute odds ratios for the variables of interest in the logit model, and discuss them. What do they tell you?\n\nExponentiating a logit coefficient turns it into an odds ratio, and it works only for the logit, because the logit link is the log of the odds:\n\\[\n\\ln\\!\\left(\\frac{\\pi_i}{1 - \\pi_i}\\right) = x_i\\beta\n\\qquad \\Longrightarrow \\qquad\n\\frac{\\pi_i}{1 - \\pi_i} = e^{x_i\\beta}.\n\\]\nIncrease \\(x_k\\) by one unit and the odds get multiplied by \\(e^{\\beta_k}\\). The intervals below exponentiate the endpoints of the interval on the coefficient scale \u2014 the same end-point logic as the predicted probabilities in question 10, for the same reason: form the interval where the sampling distribution is symmetric, then transform.\n\n\ncode\nb  &lt;- coef(m_logit)\nse &lt;- sqrt(diag(vcov(m_logit)))\n\ntibble(\n  term       = names(b),\n  coefficient = b,\n  `odds ratio` = exp(b),\n  lower       = exp(b - 1.96 * se),\n  upper       = exp(b + 1.96 * se)\n) %&gt;%\n  filter(term != \"(Intercept)\") %&gt;%\n  mutate(across(where(is.numeric), \\(x) round(x, 3)))\n\n\n# A tibble: 3 x 5\n  term  coefficient `odds ratio`  lower  upper\n  &lt;chr&gt;       &lt;dbl&gt;        &lt;dbl&gt;  &lt;dbl&gt;  &lt;dbl&gt;\n1 age        -0.014        0.986  0.984  0.987\n2 white      -1.08         0.339  0.318  0.362\n3 pro         2.85        17.3   16.5   18.2  \n\n\nRead them as multipliers on the odds, with 1 as the null value rather than 0. Pro-choice multiplies the odds of a Biden vote by about 17; white respondents are 66% less likely to vote for Biden than non-whites (1-.339)."
  },
  {
    "objectID": "ex12026answers.html#predicted-probabilities-and-intervals",
    "href": "ex12026answers.html#predicted-probabilities-and-intervals",
    "title": "Exercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood (Answers)",
    "section": "10. Predicted probabilities and intervals",
    "text": "10. Predicted probabilities and intervals\n\n10. Please generate predicted probabilities and intervals from both models and present professional plots \u2014 separately, one plot for each model. For the logit, please use average effects; for the probit, please use at-mean effects. For both, the \\(x\\) axis should be a continuous variable, and each plot should show two lines, one for each value of a binary variable of interest. Please write the code for these yourself \u2014 that is, don\u2019t call a package that produces the predictions or marginal effects for you. Write a couple of sentences describing/interpreting your figures (as you would in a journal article).\n\nBoth confidence intervals come from the delta method, in the form the prediction notes give it:\n\\[\n\\mathrm{Var}\\!\\left[F(x\\widehat\\beta)\\right] \\;=\\; f(x\\widehat\\beta)^2\\;\\mathbf{x}'\\mathbf{V}\\mathbf{x},\n\\]\nwith \\(F\\) the link and \\(f\\) its derivative \u2014 the link\u2019s PDF. Notice that the standard error of a predicted probability is the standard error of the linear prediction, scaled by the height of the PDF at that point. The first piece is one you already have \u2014 it is \\(\\sqrt{\\mathrm{diag}(\\mathbf{XVX}')}\\), the same ML standard error predict() reports for \\(x\\widehat\\beta\\). The second is \\(\\phi(x\\widehat\\beta)\\) for the probit and \\(\\lambda(x\\widehat\\beta) = \\pi(1-\\pi)\\) for the logit.\nThe average effects plot needs one more step because we\u2019re computing the average over the entire sample. There we compute a probability for every respondent and average them, so there is no single row and no single height \u2014 every observation has its own. The PDF cannot come out front as a scalar so must be inside the average computation.\n\\[\n\\bar{\\mathbf{g}} \\;=\\; \\frac{1}{n}\\sum_{i=1}^{n} f(x_i\\widehat\\beta)\\,\\mathbf{x}_i,\n\\qquad\nse = \\sqrt{\\bar{\\mathbf{g}}'\\,\\mathbf{V}\\,\\bar{\\mathbf{g}}}.\n\\]\n\nLogit: average effects\n\n\ncode\n# Reconstruct the estimation sample: na.action() returns the rows glm dropped\n# for missingness, so `esample` is exactly the data the model was fit on.\ncesa$used &lt;- TRUE\ncesa$used[na.action(m_logit)] &lt;- FALSE\nesample &lt;- cesa %&gt;% filter(used == TRUE)\n\nb &lt;- coef(m_logit)      # beta-hat\nV &lt;- vcov(m_logit)      # its estimated sampling variance -- the delta method's input\n\nage_grid &lt;- seq(20, 90, by = 5)\n\n# Average effects with a delta-method interval, in four steps.\n#   quantity:  pbar(beta) = (1/n) * sum_i Lambda(x_i beta)\n#   gradient:  g = (1/n) * sum_i lambda(x_i b) * x_i, one per coefficient\n# lambda() is the logistic PDF, so each row of X is weighted by how responsive\n# that respondent's own probability is.\navg_delta &lt;- function(a, pro_value) {\n  cf      &lt;- esample            # counterfactual COPY -- never overwrite esample\n  cf$age  &lt;- a                  # (1) everyone is this age ...\n  cf$pro  &lt;- pro_value          #     ... and holds this abortion position\n\n  X    &lt;- model.matrix(~ age + white + pro, data = cf)\n  xb   &lt;- as.vector(X %*% b)\n  p    &lt;- plogis(xb)            # every respondent's predicted probability\n  dens &lt;- p * (1 - p)           # lambda(xb): the logistic PDF, from question 1\n\n  g  &lt;- colMeans(dens * X)                       # (2) gradient of the average\n  se &lt;- sqrt(as.vector(t(g) %*% V %*% g))        # (3) delta-method SE: g' V g\n\n  tibble(age = a, pro = pro_value,\n         p  = mean(p),\n         lb = mean(p) - 1.96 * se,               # (4) symmetric, probability scale\n         ub = mean(p) + 1.96 * se)\n}\n\navg_effects &lt;- map_dfr(c(0, 1), function(pv) map_dfr(age_grid, avg_delta, pro_value = pv)) %&gt;%\n  mutate(pro = factor(pro, levels = c(0, 1),\n                      labels = c(\"Opposes abortion rights\", \"Pro-choice\")))\n\nggplot(avg_effects, aes(age, p, color = pro, fill = pro)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), alpha = 0.3, linewidth = 0.3) +\n  geom_line(linewidth = 0.9) +\n  labs(title = \"Predicted probability of a Biden vote, by age and abortion position\",\n       subtitle = \"Logit average effects, 95% delta-method intervals\",\n       x = \"Age\", y = \"Pr(Biden vote)\", color = NULL, fill = NULL) +\n  scale_color_manual(values = bu_two) +\n  scale_fill_manual(values = bu_two) +\n  theme_bu() +\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\n\n\n\nProbit: at-means effects\nOne constructed respondent instead of the whole sample, every covariate we are not varying held at its central tendency \u2014 the mode for white, since the mean of a dummy describes nobody. The delta method here is the same calculation with \\(n = 1\\), so we can check it against predict(type = \"response\", se.fit = TRUE), which computes this standard error by this method. I recommend using the se.fit = TRUE standard errors in at-mean predictions to save yourselves the trouble of computing them, but want you to see how and to have code with which to do it. Please note that this packaged method will not make sense with average predictions.\n\n\ncode\nb_p &lt;- coef(m_probit)\nV_p &lt;- vcov(m_probit)\n\n# Central tendencies from the estimation sample, so they describe what the model saw.\nwhite_mode &lt;- as.numeric(names(sort(table(esample$white), decreasing = TRUE))[1])\n\n# Out-of-sample data: ONE respondent, walked across the age grid at each value of pro.\nmem &lt;- expand_grid(age = age_grid, pro = c(0, 1)) %&gt;%\n  mutate(white = white_mode)\n\n# One row per scenario, so the PDF is a scalar and factors out:\n#   se(p-hat) = phi(xb) * sqrt(x' V x)\n# That second factor is the ML standard error of the linear prediction.\nXm     &lt;- model.matrix(~ age + white + pro, data = mem)\nxb     &lt;- as.vector(Xm %*% b_p)\nse_lin &lt;- sqrt(diag(Xm %*% V_p %*% t(Xm)))          # (2) SE of xb\nse     &lt;- dnorm(xb) * se_lin                        # (3) scaled by the PDF\n\nat_means &lt;- mem %&gt;%\n  mutate(p  = pnorm(xb),\n         lb = pnorm(xb) - 1.96 * se,                # (4)\n         ub = pnorm(xb) + 1.96 * se,\n         pro = factor(pro, levels = c(0, 1),\n                      labels = c(\"Opposes abortion rights\", \"Pro-choice\")))\n\n# Check the hand calculation against R's. stopifnot() is silent when it passes\n# and stops the render when it fails.\nstopifnot(isTRUE(all.equal(se, predict(m_probit, newdata = mem, type = \"response\",\n                                       se.fit = TRUE)$se.fit,\n                           check.attributes = FALSE))) \n\n\n\n\ncode\nggplot(at_means, aes(age, p, color = pro, fill = pro)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), alpha = 0.3, linewidth = 0.3) +\n  geom_line(linewidth = 0.9) +\n  labs(title = \"Predicted probability of a Biden vote, by age and abortion position\",\n       subtitle = \"Probit at-means effects, 95% delta-method intervals\",\n       x = \"Age\", y = \"Pr(Biden vote)\", color = NULL, fill = NULL) +\n  scale_color_manual(values = bu_two) +\n  scale_fill_manual(values = bu_two) +\n  theme_bu() +\n  theme(legend.position = \"bottom\")\n\n\n\n\n\n\n\n\n\nThe two plots look almost the same, and they should \u2014 two links on the same binomial likelihood, and these predictions never get near the tails of the CDFs where the links part ways.\nThe vertical distance between the two lines is the effect of the abortion itemin probability at each age \u2014 about .591 at age 20, .586 at age 90. Nearly constant, because over this age range \\(x\\beta\\) moves too little for the lines to curve much at all. Out in the flat tails the same shift would be worth a fraction of that.\n\n\nWhat you would actually write\nThis text could refer to either of the plots, though I\u2019ll talk about the logit:\n\nFigure 1 plots the predictions from the logit model. The most striking effect is the difference in vote choice depending on whether a voter is pro-choice or not. Pro-choice voters are 55 to 59 percentage points more likely to vote for Biden across the range of age. Vote choice varies relatively little across age, a total of about a 17 point decline over the entire range. Both of the predictions lines remain strictly above (pro-choice) or below (anti-abortion) the \\(Pr(y=.5)\\) threshold, suggesting even a 17 point change (over 70 years) is not likely to shift a hypothetical voter from one candidate to the other. The mild negative slope suggests both groups become slightly less likely to support Biden as age increases; older voters mildly prefer Trump in this particular election."
  },
  {
    "objectID": "buildtemplate26.html",
    "href": "buildtemplate26.html",
    "title": "Build Your Own Estimator",
    "section": "",
    "text": "You have built the binomial machine \u2014 a likelihood, a parameterization, a link, a maximizer. It does not care what \\(y\\) is: swap the distribution and the link and every other part stays put. Today you\u2019ll swap them out, and you build the whole thing by hand before using a canned optimizer.\nWe\u2019ll do this working in groups and using real data. I am not telling you which distribution to use and I am not writing the log-likelihood for you. Look at \\(y\\); the likelihood notes provide guidance you\u2019ll need.\n\n\n\n\n\n\nWhat you do\n\n\n\nNine chunks are marked TODO. Fill them, then render. If it renders and the self-check tables are all TRUE, you built it. That is the whole exercise.\nUntil you fill them in, the test tables below read FALSE and the results are empty. That is what an unfinished build looks like, not a broken page.\nThe TODO chunks are yours. Nothing else is. The setup, the test tables, and the self-check are the file\u2019s, and they are what tell you whether the thing works \u2014 editing them to turn a FALSE into a TRUE only removes your own instrument. If a check is red and you are sure your code is right, come find me.\n\n\nTo get your own copy, run this in the console \u2014 it lands in your working directory under the right name:\ndownload.file(\"https://raw.githubusercontent.com/clavedark/MLEfall26/main/buildtemplate26.qmd\",\n              \"buildtemplate26.qmd\")\n\n\n\n\n\n\nThe recipe\n\n\n\n\nObserve \\(y\\); consider its distribution; write down its PDF/PMF.\nWrite the joint probability of the data.\nRewrite as a likelihood.\nSimplify \u2014 take logs.\nParameterize \u2014 make the distribution\u2019s parameter a function of \\(x\\beta\\).\nChoose a link that respects the parameter\u2019s bounds."
  },
  {
    "objectID": "buildtemplate26.html#why-beta-mathbf0-makes-this-easy",
    "href": "buildtemplate26.html#why-beta-mathbf0-makes-this-easy",
    "title": "Build Your Own Estimator",
    "section": "Why \\(\\beta = \\mathbf{0}\\) makes this easy",
    "text": "Why \\(\\beta = \\mathbf{0}\\) makes this easy\nSet every coefficient to zero so that \\(x_i\\beta = 0\\) for every observation, and so \\(\\lambda_i = e^0 = 1\\) \u2014 the rate is 1 for every case. Put that into the three expressions you derived in Step 2 and each one collapses to something simple:\n\\[\n\\begin{aligned}\n\\ell(\\mathbf{0}) &= \\sum_{i=1}^{n}\\big[\\,y_i(x_i\\beta) - e^{x_i\\beta} - \\ln(y_i!)\\,\\big]\n   \\;=\\; \\sum_{i=1}^{n}\\big[\\,0 - 1 - \\ln(y_i!)\\,\\big]\n   \\;=\\; -n - \\sum_{i=1}^{n}\\ln(y_i!) \\\\[6pt]\ns(\\mathbf{0}) &= X'(y - \\lambda) \\;=\\; X'(y - \\mathbf{1}) \\\\[6pt]\nH(\\mathbf{0}) &= -X'\\Lambda X \\;=\\; -X'IX \\;=\\; -X'X .\n\\end{aligned}\n\\]\nEvery one of those is a single line of R, and none needs the general case (coded in your functions): the log-likelihood loses its first two terms because \\(y_i \\cdot 0 = 0\\) and \\(e^0 = 1\\); the score is the fitted values replaced by a column of ones; the Hessian\u2019s weight matrix becomes the identity and drops out."
  },
  {
    "objectID": "buildtemplate26.html#the-two-sides-of-each-test",
    "href": "buildtemplate26.html#the-two-sides-of-each-test",
    "title": "Build Your Own Estimator",
    "section": "The two sides of each test",
    "text": "The two sides of each test\nEach test compares two things that should be identical:\n\nyour function, called at \\(\\beta = \\mathbf{0}\\) \u2014 mle_ll(b0, X, y), and the same for the other two. You do use what you wrote; that is the point of testing it;\nthe closed form above, coded directly, in one line.\n\nThe right-hand side is the independent check. It has to be computed a different way from the function, or the test compares your code to itself.\n\n# TODO 5: code the three closed forms above, one line each. NA is a\n#   placeholder -- replace it. What each should come back as:\n#     ll_direct    -- one number\n#     score_direct -- a k-vector\n#     hess_direct  -- a k x k matrix\nll_direct    &lt;- NA\nscore_direct &lt;- NA\nhess_direct  &lt;- NA\n\n\n\ncode\ntibble(\n  # one row per function: right dimensions, and the right numbers?\n  fn                = c(\"mle_ll\", \"mle_score\", \"mle_hessian\"),\n  `right dimensions`           = c(length(mle_ll(b0, X, y)) == 1,\n                                   length(mle_score(b0, X, y)) == k,\n                                   identical(dim(mle_hessian(b0, X, y)), c(k, k))),\n  `matches direct calculation` = c(isTRUE(all.equal(mle_ll(b0, X, y), ll_direct)),\n                                   isTRUE(all.equal(as.vector(mle_score(b0, X, y)),\n                                                    as.vector(score_direct))),\n                                   isTRUE(all.equal(as.vector(mle_hessian(b0, X, y)),\n                                                    as.vector(hess_direct))))\n) %&gt;%\n  knitr::kable(caption = \"Each piece at beta = 0, against the closed form\")\n\n\n\nEach piece at beta = 0, against the closed form\n\n\nfn\nright dimensions\nmatches direct calculation\n\n\n\n\nmle_ll\nFALSE\nFALSE\n\n\nmle_score\nFALSE\nFALSE\n\n\nmle_hessian\nFALSE\nFALSE\n\n\n\n\n\nOne more test. Everything above is evaluated at \\(\\beta = \\mathbf{0}\\), where the parameter is 1 for every case \u2014 so a function that is only correct at zero passes it. A Hessian that drops the weight entirely is exactly right at \\(\\beta = \\mathbf{0}\\) and wrong everywhere else, and the table above will not notice.\nSo test the pieces against each other, away from zero. Your score is the derivative of your log-likelihood, and your Hessian is the derivative of your score. A numerical derivative of one should reproduce the other, at any \\(\\beta\\) you like, and no closed form is needed.\n\n\ncode\nb1 &lt;- c(1, 0.5, 0.5, 0.05, 0.1)     # any beta that is not zero\n\n# central difference: ( f(b + e) - f(b - e) ) / 2e, one column per coefficient\nnumderiv &lt;- function(f, b) {\n  sapply(seq_along(b), function(j) {\n    e &lt;- rep(0, length(b)); e[j] &lt;- 1e-5\n    (f(b + e, X, y) - f(b - e, X, y)) / 2e-5\n  })\n}\n\ntibble(\n  check = c(\"mle_score matches the numerical derivative of mle_ll\",\n            \"mle_hessian matches the numerical derivative of mle_score\"),\n  pass  = c(isTRUE(all.equal(as.vector(mle_score(b1, X, y)),\n                             as.vector(numderiv(mle_ll, b1)), tolerance = 1e-4)),\n            isTRUE(all.equal(as.vector(mle_hessian(b1, X, y)),\n                             as.vector(numderiv(mle_score, b1)), tolerance = 1e-4)))\n) %&gt;%\n  knitr::kable(caption = \"The same three pieces, tested away from zero\")\n\n\n\nThe same three pieces, tested away from zero\n\n\ncheck\npass\n\n\n\n\nmle_score matches the numerical derivative of mle_ll\nFALSE\n\n\nmle_hessian matches the numerical derivative of mle_score\nFALSE\n\n\n\n\n\nIf the first table is green and this one is not, the piece it names works at zero but at no other value \u2014 look at the weight.\nTwo things to check by eye before you trust the tables. Is the score anywhere near zero? It should not be \u2014 \\(\\beta = \\mathbf{0}\\) is a long way from the maximum and the score only vanishes at the top; what you are testing here is arithmetic, not optimality. And the diagonal of your Hessian should be negative everywhere. Say in a sentence why that has to be true at a maximum, and what a positive element would do to your standard errors.\n(The table compares with all.equal rather than == because these are floating-point numbers, and wraps it in isTRUE because all.equal returns a description of the difference rather than FALSE when things do not match. as.vector on both sides strips names and dimensions so only the numbers are compared.)"
  },
  {
    "objectID": "mlesyllabus26.html",
    "href": "mlesyllabus26.html",
    "title": "MLE Syllabus",
    "section": "",
    "text": "Dave Clark\n \u00a0 LNG 57; LN2430\n \u00a0 dclark@binghamton.edu\n \u00a0 clavedark\noffice hours: M 1:30-3:30pm and by appt.\n\n\n\n\n\n \u00a0 Fall 2026\n \u00a0 Wednesday\n \u00a0 11:30am-2:30pm\n \u00a0 Bing SSEL - LNG 332"
  },
  {
    "objectID": "mlesyllabus26.html#seminar-description",
    "href": "mlesyllabus26.html#seminar-description",
    "title": "MLE Syllabus",
    "section": "Seminar Description",
    "text": "Seminar Description\nThis 4 credit hour seminar is a survey of maximum likelihood methods and their applications to empirical political questions. It presumes students have a detailed and intuitive knowledge of least squares, probability theory, basic skills in scalar and matrix algebra, and a basic understanding of calculus. The course will deal mainly in understanding the principles of maximum likelihood estimation, under what conditions we move away from least squares, and what particular models are appropriate given observed data. The seminar will focus on application and interpretation of ML models and linking theory to statistical models. The course emphasizes coding and data viz in R.\nThe class meets one time per week for three hours. My office hours are designed to be homework help hours where I\u2019ll work in the grad lab with any of you who are working on the exercises. The most productive pathway for this class is for you to get in the habit of working together, and those office hours are a good time for this."
  },
  {
    "objectID": "mlesyllabus26.html#course-purpose",
    "href": "mlesyllabus26.html#course-purpose",
    "title": "MLE Syllabus",
    "section": "Course Purpose",
    "text": "Course Purpose\nThis seminar fulfills the advanced quantitative methods requirement in the Ph.D.\u00a0curriculum. The method of maximum likelihood underlies a majority of quantitative models in Political Science; this class teaches students to be astute consumers of such models, and how to implement and interpret ML models. These are crucial skills for dissertations in Political Science, and for producing publishable quantitative research."
  },
  {
    "objectID": "mlesyllabus26.html#learning-objectives",
    "href": "mlesyllabus26.html#learning-objectives",
    "title": "MLE Syllabus",
    "section": "Learning Objectives",
    "text": "Learning Objectives\nStudents will encounter an array of maximum likelihood models in this course. By the end of the course, students will have mastered the theory of maximum likelihood sufficient to write and program likelihood functions in ; they will be able to choose, estimate, and interpret appropriate models, model specifications, and model evaluation tools given their data; and they will be able to produce sophisticated quantities of interest (e.g.\u00a0predicted probabilities, expected values, confidence intervals) via a variety of techniques including simulation and end point transformation. Students will also be able to present model findings verbally and graphically."
  },
  {
    "objectID": "mlesyllabus26.html#resources",
    "href": "mlesyllabus26.html#resources",
    "title": "MLE Syllabus",
    "section": " Resources",
    "text": "Resources\nThere are lots of good, free  resources online. Here are a few:\n\nModern Statistics with R\nR for Data Science\nR Markdown: The Definitive Guide\nR Graphics Cookbook\nAdvanced R\nR Markdown Cookbook\nData Science:A First Introduction\nThe Big Book of R"
  },
  {
    "objectID": "mlesyllabus26.html#class-meetings-office-hours-assignments",
    "href": "mlesyllabus26.html#class-meetings-office-hours-assignments",
    "title": "MLE Syllabus",
    "section": "Class Meetings, Office Hours, Assignments",
    "text": "Class Meetings, Office Hours, Assignments\nThe course will meet this fall entirely in-person in the Social Science Experiment Lab on Wednesdays 11:30am-2:30pm.\nOffice hours are Mondays 1:30pm-3:30pm. I\u2019ll hold these in the grad work room to help with your assignments. For an appointment, email me and we\u2019ll sort out a time.\nThis syllabus, slides, etc. can be found on the course website. Assignments will be posted weekly to Brightspace. All assignments should be turned in on Brightspace - please submit ::\n\nPDFs generated from LaTeX or R Markdown (Quarto).\nannotated R scripts.\nwhere necessary, data.\n\nAssignments should be instantly replicable - running the code file should produce all models, tables, plots, etc."
  },
  {
    "objectID": "mlesyllabus26.html#reading",
    "href": "mlesyllabus26.html#reading",
    "title": "MLE Syllabus",
    "section": "Reading",
    "text": "Reading\nThe reading material for the course is important because it often demonstrates application of various MLE models; seeing how folks apply these and how they motivate their applications is really informative, something you cannot miss. We often won\u2019t directly discuss the readings, but don\u2019t let that imply they\u2019re not important. If I get the sense we\u2019re not keeping up with reading, expect the syllabus to change to incorporate quizzes or other accountability measures.\nReading for the course will consist of several books and articles (listed by week below). The books listed below also have Amazon links - you\u2019ll find most of these cheaper used online.\n\nRequired\n\nBox-Steffensmeier, Janet and Jones, Brad. 2004. Event History Modeling. Cambridge. ISBN 0521546737\nJ. Scott Long. 1997. Regression Models for Categorical and Limited Dependent Variables. Sage Publications Inc.\u00a0ISBN 0803973748\nWard, Michael D. and John S. Ahlquist. 2018 Maximum Likelihood for Social Science. Cambridge. ISBN 978-1316636824.\n\n\n\nRecommended\nUseful, but not required (though some required reading in the first one):\n\nGary King. 1998. Unifying Political Methodology. University of Michigan Press. ISBN 0472085549\n\n\nGary King\u2019s book is regarded as seminal in developing ML applications in political science. Scott Long\u2019s is a similarly accessible treatment of a host of ML models and applications (and the Stata book is a great applied companion). Together, these two books are probably the most important on the syllabus as they are both accessible, but comprehensive and technical enough to be useful. Ward and Ahlquist\u2019s book is a newer overview of applied ML in a political science setting. Box-Steffensmeier and Jones is a thorough and accessible treatment of hazard models in a variety of empirical settings.\n\n\nAdditional Resources\nOther useful books include:\n\nCameron, A. Colin and Trivedi, Pravin K. 1998. Regression Analysis of Count Data. Cambridge. ISBN 0521635675\nMaddala, Gregory. 1983. Limited Dependent and Qualitative Variables in Econometrics. Cambridge. ISBN 0521338255\nPaul D Allison - Event History Analysis : Regression for Longitudinal Event Data. Sage Publications Inc.\u00a0ISBN 0803920555\nTim Futing Liao - Interpreting Probability Models : Logit, Probit, and Other Generalized Linear Models. Sage Publications Inc.\u00a0ISBN 0803949995\nJohn H Aldrich and Forrest D Nelson - Linear Probability, Logit, and Probit Models. Sage Publications Inc.\u00a0ISBN 0803921330\nFred C Pampel - Logistic Regression : A Primer. Sage Publications Inc.\u00a0ISBN 0761920102\nVani Kant Borooah - Logit and Probit : Ordered and Multinomial Models. Sage Publications Inc.\u00a0ISBN 0761922423\nScott R Eliason - Maximum Likelihood Estimation : Logic and Practice. Sage Publications Inc.\u00a0ISBN 0803941072\nRichard Breen - Regression Models : Censored, Sample Selected, or Truncated Data. Sage Publications Inc.\u00a0ISBN 0803957106\nKrishnan Namboodiri - Matrix Algebra : An Introduction. ISBN 0803920520"
  },
  {
    "objectID": "mlesyllabus26.html#course-requirements-and-grades",
    "href": "mlesyllabus26.html#course-requirements-and-grades",
    "title": "MLE Syllabus",
    "section": "Course Requirements and Grades",
    "text": "Course Requirements and Grades\nThe seminar requires the following:\n\nProblem sets - 60% total\nMechanism papers - 40%\n\nPlease note that all written assignments must be submitted as PDFs either compiled in LaTeX or in R markdown (Quarto).\nYou\u2019ll complete a series of problem sets, mostly applied. How many will depend on how things move along during the term (roughly 8-10). Regarding the problem sets - the work you turn in for the problem sets should clearly be your own, but I urge you to work together - doing so is a great way to learn and to overcome problems.\nThe mechanism papers are a series of three short papers you\u2019ll write during the semester aimed at learning to identify and describe causal mechanisms, then at producing a causal mechanism. More on these early in the term.\nA word about completeness - attempt everything. To receive a passing grade in the course, you must finish all elements of the course, so all problem sets, all exams, papers, etc. To complete an element, you must at least attempt all parts of the element - so if a problem set has 10 problems, you must attempt all 10 or the assignment is incomplete, you\u2019ve not completed every element of the course, and you cannot pass. I realize there may be problems you have trouble with and even get wrong, but you must try - the bottom line is don\u2019t turn in incomplete work.\nGrades will be assigned on the following scale:\n\n\n\n\n\n\n\n\n\n\n\nGrade\nRange\nGrade\nRange\n\n\n\n\nA\n94-100%\nC+\n77-79%\n\n\nA-\n90-93%\nC\n73-76%\n\n\nB+\n87-89%\nC-\n70-72%\n\n\nB\n83-86%\nD\n60-69%\n\n\nB-\n80-82%\nF\n&lt;60%"
  },
  {
    "objectID": "mlesyllabus26.html#course-policies",
    "href": "mlesyllabus26.html#course-policies",
    "title": "MLE Syllabus",
    "section": "Course Policies",
    "text": "Course Policies\n\nAttendance\nAttendance is expected, and is essential if you\u2019re to succeed in this class.\n\n\nAcademic Integrity\nIdeas are the currency in academic exchange, so acknowledging where ideas come from is important. Acknowledging the sources of ideas also helps us identify an idea\u2019s lineage which can be important for understanding how that line of thought has developed, and toward promoting future growth. As graduate students, you should have a good understanding of academic honesty and best practices. Here are details of Binghamton\u2019s honesty policy.\n\n\nArtificial Intelligence and Coding Tools\nGenerative AI can act as an incredible accelerator for research, for coding and data management in particular. Such tools are only as useful as your own mastery of coding, data management, and modeling.\nDo you need to be able to write code when AI can do it for you? Yes. Three reasons:\n\nUnderstanding. Code and the act of coding are the windows to seeing and understanding data, models, and predictions \u2014 how data produce models, which in turn produce predictions. Code is an explicit connection between a data matrix and a model, and facilitates your understanding of the what the model produces. AI can\u2019t hand you that understanding; you build it by doing the work yourself.\nCriticism. AI will produce code. If you can code, you can read it, critique it, and evaluate its output to judge whether it\u2019s right. If you can\u2019t code, you can\u2019t do any of that \u2014 you\u2019re left trusting output you have no way to check.\nInquiry. If you don\u2019t understand the code, you probably don\u2019t understand the data and the model, and you\u2019ll not write a very good query to the AI in the first place. AI is only as good as its user. You should always know things the AI does not, in terms of your argument, your data, your model, and your code.\n\nSo here is the policy for this course. Do not use generative AI to produce code. If you get stuck coding, you may use AI to get unstuck - this is equivalent to consulting \u201cstackexchange\u201d or \u201cstackoverflow\u201d to solve code problems and get unstuck. Anytime you consult AI, you must cite and annotate it specifically: what you asked, why you asked it, and which parts of your code came from the AI. To this end, expect assessment where I ask you to show that you understand the data, code, models, and predictions you are producing. A bot can\u2019t fake that understanding.\nYou should not use generative AI tools for writing in this course. Learning to express your ideas about politics, about causal relations, about data and models, and explaining your findings are core skills for the Ph.D.\u00a0Please learn to do these things without any use of generative AI."
  },
  {
    "objectID": "mlesyllabus26.html#course-schedule",
    "href": "mlesyllabus26.html#course-schedule",
    "title": "MLE Syllabus",
    "section": "Course Schedule",
    "text": "Course Schedule\nWe meet Wednesdays. Two dates are dark: fall break (Oct 14) and Thanksgiving (Nov 25).\n\nPart I \u2014 Foundations\nWeek 1, Aug 19 \u2013 Binary \\(y\\) Variables: probit/logit, first estimation, and interpretation\nRequired\n\nWard & Ahlquist, 2018. Maximum Likelihood for Social Science. Chapters 1, 2, 4\nJ. Scott Long. 1997. Regression Models for Categorical and Limited Dependent Variables. Chapter 3.\n\nRecommended\n\nHanmer and Kalkan (2013)\nMood (2010)\nPierskalla and Hollenbach (2013)\n\nWeek 2, Aug 26 \u2013 Likelihood Theory: the log-likelihood, score, Hessian, information\nRequired\n\nGary King. 1998. Unifying Political Methodology. Chapters 1\u20134\nJ. Scott Long. 1997. Chapters 1\u20132.\n\nRecommended\n\nEliason (1993)\n\nWeek 3, Sep 2 \u2013 MLE Mechanics and Building Your Own Model: grid search, Newton\u2013Raphson, optim, and programming an estimator\nRequired\n\nWard & Ahlquist, 2018. Chapters 5, 6, 7\n\n\n\nPart II \u2014 Binomial extensions, and a causal lens\nWeek 4, Sep 9 \u2013 Binomial Extensions I \u2014 Diagnostics, Classification, and Fit: quantities of interest (review), symmetry, classification and ROC, separation, rare events\nRequired\n\nWard & Ahlquist, 2018. Chapter 6 (quantities of interest)\nGary King. 1998. Chapter 4 (simulation of quantities of interest)\nJ. Scott Long. 1997. Chapter 5.\nG. King and Zeng (2001)\nRainey (2016b)\n\nRecommended\n\nNagler (1994)\nRainey (2024)\nC. Zorn (2005)\nClark, Nordstrom, and Reed (2008)\nGary King and Roberts (2015)\nGreenhill, Ward, and Sacks (2011)\nEsarey and Pierce (2012)\nWard, Greenhill, and Bakke (2010)\nRainey (2014)\n\nWeek 5, Sep 16 \u2013 Binomial Extensions II \u2014 Discrete-Time Hazards: the logit as a hazard, person-period (BTSCS) data, time dependence\nRequired\n\nWard & Ahlquist, 2018. Chapter 11\nBeck, Katz, and Tucker (1998)\nCarter and Signorino (2010)\n\nRecommended\n\nMetzger and Jones (2022)\nWilson and Butler (2007)\nSvolik (2008)\n\nWeek 6, Sep 23 \u2013 Binomial Extensions III \u2014 Interactions: multiplicative terms, compression, and marginal effects in nonlinear models\nRequired\n\nBrambor, Clark, and Golder (2006)\nWilliam D. Berry, Golder, and Milton (2012)\nWilliam D. Berry, DeMeritt, and Esarey (2010)\nHainmueller, Mummolo, and Xu (2019)\nZhirnov, Moral, and Sedashov (2023)\n\nRecommended\n\nMize (2019)\nRainey (2016a)\nLiu, Liu, and Xu (2025)\n\nWeek 7, Sep 30 \u2013 Causal Mechanics and Identification: potential outcomes, estimands (ATE/ATT), propensity scores, g-computation\nRequired\n\nKeele (2015)\n(Hern\u00e1n and Robins 2020, chaps. 1\u20133 and 13) (potential outcomes, the g-formula)\n\nRecommended\n\nGary King and Nielsen (2019)\nIacus, King, and Porro (2012)\nBlackwell (2013)\nRoberts, Stewart, and Nielsen (2020)\nKam and Palmer (2008)\nHenderson and Chatfield (2011)\n\n\n\nPart III \u2014 Counts and durations as one family\nWeek 8, Oct 7 \u2013 Event Counts I: Poisson, rates and offsets, equidispersion\nRequired\n\nWard & Ahlquist, 2018. Chapter 10\nJ. Scott Long. 1997. Chapter 8.1\u20138.2.\nFordham (1998)\n\nRecommended\n\nGowa (1998)\nBrandt and Williams (2001)\nFogarty and Monogan (2014)\n\nOct 14 \u2013 no class, fall break\nWeek 9, Oct 21 \u2013 Event Counts II: dispersion and zeros \u2014 negative binomial, zero-inflation, split populations\nRequired\n\nJ. Scott Long. 1997. Chapter 8.3\u20138.7.\nClark (2003)\nBagozzi (2015)\nCasiraghi and Cusumano (2024)\n\nRecommended\n\nC. J. W. Zorn (1998)\nBagozzi et al. (2015)\n\nWeek 10, Oct 28 \u2013 Count \\(\\leftrightarrow\\) Hazard Equivalence: the same event as a count and as a duration\nRequired\n\nJanet Box-Steffensmeier and Brad Jones. 2004. Event History Modeling. Chapters 1\u20132\nBox-Steffensmeier, Arnold, and Zorn (1997)\nAlt, King, and Signorino (2001)\n\nWeek 11, Nov 4 \u2013 Continuous-Time Hazards: parametric and semi-parametric duration models (exponential, Weibull, Cox)\nRequired\n\nBox-Steffensmeier and Jones. 2004. Chapters 3\u20135\nC. J. W. Zorn (2000)\n\nRecommended\n\nLicht (2011)\nKropko and Harden (2020)\nHarden and Kropko (2019)\nBox-Steffensmeier, De Boef, and Joyce (2007)\nBagozzi et al. (2019)\n\n\n\nPart IV \u2014 Choice, and synthesis\nWeek 12, Nov 11 \u2013 Choice I: multinomial and conditional logit, and prediction\nRequired\n\nJ. Scott Long. 1997. Chapter 6.\nWard & Ahlquist, 2018. Chapter 9\nAlvarez and Nagler (1998)\nPaolino (2021)\n\nRecommended\n\nHainmueller, Hopkins, and Yamamoto (2014)\nSignorino (2003)\n\nWeek 13, Nov 18 \u2013 Choice II: IIA \u2014 assessing it, and how much to worry; alternatives (multivariate probit) and the conditional\u2013fixed-effects equivalence\nRequired\n\nDow and Endersby (2004)\nClark and Reed (2005)\n\nRecommended\n\nLacy and Burden (1999)\nC. J. W. Zorn (1996)\nGlasgow (2001)\nBeck (2020)\nAbramson, Ko\u00e7ak, and Magazinnik (2022)\n\nNov 25 \u2013 no class, Thanksgiving\nWeek 14, Dec 2 \u2013 catch up"
  },
  {
    "objectID": "code.html",
    "href": "code.html",
    "title": "Exercises & Code",
    "section": "",
    "text": "Posted as they\u2019re assigned. Turn in both your source (.qmd/.R) and the rendered document on Brightspace. The data these use are in the Data section below.\n\nExercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood \u00b7 answer key\nExercise #2 \u2014 Write the Maximizer \u00b7 answer key\nBuild Your Own Estimator \u2014 the in-class build template. Read it here, but work in the .qmd: run\ndownload.file(\"https://raw.githubusercontent.com/clavedark/MLEfall26/main/buildtemplate26.qmd\",\n              \"buildtemplate26.qmd\")\nand it lands in your working directory. It reads its data straight from this repository, so you do not need a local data/ folder. The test tables read FALSE until you fill in the TODOs."
  },
  {
    "objectID": "code.html#exercises",
    "href": "code.html#exercises",
    "title": "Exercises & Code",
    "section": "",
    "text": "Posted as they\u2019re assigned. Turn in both your source (.qmd/.R) and the rendered document on Brightspace. The data these use are in the Data section below.\n\nExercise #1 \u2014 Distributions, Simulation, and the Log-Likelihood \u00b7 answer key\nExercise #2 \u2014 Write the Maximizer \u00b7 answer key\nBuild Your Own Estimator \u2014 the in-class build template. Read it here, but work in the .qmd: run\ndownload.file(\"https://raw.githubusercontent.com/clavedark/MLEfall26/main/buildtemplate26.qmd\",\n              \"buildtemplate26.qmd\")\nand it lands in your working directory. It reads its data straight from this repository, so you do not need a local data/ folder. The test tables read FALSE until you fill in the TODOs."
  },
  {
    "objectID": "code.html#code",
    "href": "code.html#code",
    "title": "Exercises & Code",
    "section": "Code",
    "text": "Code\nStandalone R scripts, for slides that have one.\n\npredictionOLS26.R \u2014 R script from the linear model prediction slides.\nbinarymodels26.R \u2014 R script from the binary models slides. Reads dp.dta, below."
  },
  {
    "objectID": "code.html#data",
    "href": "code.html#data",
    "title": "Exercises & Code",
    "section": "Data",
    "text": "Data\n\nCES 2020 (ces.csv) \u2014 individual-level responses from the 2020 Cooperative Election Study, reached through the \u201cAmerican Voter Bot\u201d project; used in exercises #1 and #2. GitHub won\u2019t preview a file this size \u2014 click Download raw file. Variable descriptions are in the codebook. Source: Cooperative Election Study, Harvard University. CES project site.\nDemocratic peace (dp.dta) \u2014 dyad-year data, 1951\u20131985, 20,990 rows: militarized dispute onset (dispute) with a shared border, the capability ratio, the lower of the two Polity scores (deml), an alliance indicator, and joint IGO memberships. Used in the binary models, likelihood, and maximization slides. Stata format \u2014 read it with haven::read_dta(). Click Download raw file. Source: Oneal, John R., and Bruce M. Russett. 1997. \u201cThe Classic Liberals Were Right: Democracy, Interdependence, and Conflict, 1950-1985.\u201d International Studies Quarterly 41(2): 267-294.\nITT data (ITT.csv) \u2014 Ill-Treatment and Torture allegation counts by country-year, merged with World Bank (WDI) and Polity covariates; used in the predictionOLS.R script. Source: Conrad, Courtenay R., Jillienne Haglund, and Will H. Moore. 2013. \u201cDisaggregating Torture Allegations: Introducing the Ill-Treatment and Torture (ITT) Country-Year Data.\u201d International Studies Perspectives 14(2): 199-220. ITT project page."
  },
  {
    "objectID": "ex22026answers.html",
    "href": "ex22026answers.html",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "",
    "text": "Answer key for Exercise #2. The code is folded \u2014 open it to see how a piece was built.\nThe four functions below are the whole assignment. Everything after them is checking that they are right, which is most of the work in practice and all of the work students skip."
  },
  {
    "objectID": "ex22026answers.html#simulate-a-known-truth",
    "href": "ex22026answers.html#simulate-a-known-truth",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "1. Simulate a known truth",
    "text": "1. Simulate a known truth\n\n1. Simulate \\(n = 2{,}000\\) observations of a binary \\(y\\) from a logit DGP with one continuous covariate, one binary covariate, and an intercept.\n\n\\[\n\\begin{aligned}\nx_{1i} &\\sim \\text{Normal}(0,1), \\qquad x_{2i} \\sim \\text{Bernoulli}(0.5) \\\\\nx_i\\beta &= -0.5 + 0.75\\,x_{1i} - 1\\,x_{2i} \\\\\n\\pi_i &= \\Lambda(x_i\\beta) \\;=\\; \\frac{1}{1 + e^{-x_i\\beta}} \\\\\ny_i &\\sim \\text{Bernoulli}(\\pi_i)\n\\end{aligned}\n\\]\n\n\ncode\nset.seed(20260902)\n\nn     &lt;- 2000\ntruth &lt;- c(`(Intercept)` = -0.5, x1 = 0.75, x2 = -1)\n\nx1 &lt;- rnorm(n)\nx2 &lt;- rbinom(n, 1, 0.5)\n\nxb &lt;- truth[1] + truth[2] * x1 + truth[3] * x2\npr &lt;- 1 / (1 + exp(-xb))\ny  &lt;- rbinom(n, 1, pr)\n\nsim &lt;- tibble(y, x1, x2)\nX   &lt;- model.matrix(~ x1 + x2, data = sim)\n\nc(n = n, share_ones = round(mean(sim$y), 3))\n\n\n         n share_ones \n     2e+03      3e-01 \n\n\nAbout 0.3 of the observations are ones."
  },
  {
    "objectID": "ex22026answers.html#the-log-likelihood-the-score-and-the-hessian",
    "href": "ex22026answers.html#the-log-likelihood-the-score-and-the-hessian",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "2\u20134. The log-likelihood, the score, and the Hessian",
    "text": "2\u20134. The log-likelihood, the score, and the Hessian\n\n2. logit_ll(beta, X, y). 3. logit_score(beta, X, y). 4. logit_hessian(beta, X, y).\n\n\\[\n\\ell(\\beta) = \\sum_{i=1}^{n}\\Big[y_i \\ln \\pi_i + (1-y_i)\\ln(1-\\pi_i)\\Big],\n\\qquad\n\\nabla\\ell(\\beta) = X'(y - \\pi),\n\\qquad\nH(\\beta) = -X'WX,\n\\]\nwith \\(\\pi_i = \\Lambda(x_i\\beta)\\) and \\(W = \\mathrm{diag}\\big(\\pi_i(1-\\pi_i)\\big)\\).\n\n\ncode\n# Build xb, transform to probabilities, sum the two terms. One number out.\nlogit_ll &lt;- function(beta, X, y) {\n  p &lt;- 1 / (1 + exp(-as.vector(X %*% beta)))\n  sum(y * log(p) + (1 - y) * log(1 - p))\n}\n\n# The score, X'(y - pi). Comes back as a k-vector, one per coefficient.\nlogit_score &lt;- function(beta, X, y) {\n  p &lt;- 1 / (1 + exp(-as.vector(X %*% beta)))\n  as.vector(t(X) %*% (y - p))\n}\n\n# The Hessian, -X'WX, with W = diag(pi(1-pi)). `X * w` scales each column of X\n# by the weights, which is what X'WX needs and avoids building the n x n W.\nlogit_hessian &lt;- function(beta, X, y) {\n  p &lt;- 1 / (1 + exp(-as.vector(X %*% beta)))\n  -t(X) %*% (X * (p * (1 - p)))\n}\n\n\nThe X * (p * (1 - p)) construction is the one place students reliably get stuck. \\(W\\) is an \\(n \\times n\\) diagonal matrix in the notation, and writing it that way works on the simulated data and then tries to allocate a 43,000 \u00d7 43,000 matrix on the CES data. Recognizing that \\(X'WX\\) only ever uses the diagonal \u2014 so the weights can scale the rows of \\(X\\) directly \u2014 is the practical version of the same formula.\nWhy the Hessian is \\(k \\times k\\). Because the coefficients are not estimated separately. The off-diagonal element for \\(\\beta_1\\) and \\(\\beta_2\\) says how the slope in one direction changes as you move in the other \u2014 how much what we learn about one coefficient depends on the value of another. That is what makes a single Newton step update the whole vector at once rather than each coefficient on its own, and it is why the inverse of \\(-H\\) has covariances in it and not just variances."
  },
  {
    "objectID": "ex22026answers.html#the-estimator",
    "href": "ex22026answers.html#the-estimator",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "5. The estimator",
    "text": "5. The estimator\n\n5. fit_logit(X, y, tol, maxit) \u2014 start, compute score and Hessian, step, record, check, then standard errors.\n\nThe update rule, from the maximization notes:\n\\[\n\\beta^{(t+1)} \\;=\\; \\beta^{(t)} + (X'WX)^{-1}X'(y-\\pi)\n\\;=\\; \\beta^{(t)} - H\\big(\\beta^{(t)}\\big)^{-1}\\nabla\\ell\\big(\\beta^{(t)}\\big).\n\\]\n\n\ncode\nfit_logit &lt;- function(X, y, tol = 1e-8, maxit = 50) {\n\n  beta      &lt;- rep(0, ncol(X))   # start at zero\n  hist      &lt;- list()\n  converged &lt;- FALSE\n\n  for (it in 1:maxit) {\n\n    g &lt;- logit_score(beta, X, y)\n    H &lt;- logit_hessian(beta, X, y)\n\n    # solve(-H, g) solves the system directly, rather than forming solve(-H)\n    # and multiplying. Faster, and better conditioned.\n    step &lt;- solve(-H, g)\n\n    # Record before stepping, so row `it` describes the guess just evaluated.\n    hist[[it]] &lt;- c(iter     = it,\n                    loglik   = logit_ll(beta, X, y),\n                    max_grad = max(abs(g)),\n                    max_step = max(abs(step)))\n\n    beta &lt;- beta + step\n\n    if (max(abs(step)) &lt; tol) { converged &lt;- TRUE; break }\n  }\n\n  # The loop's H was computed one step before the final beta, so recompute the\n  # curvature at beta-hat.\n  H_hat &lt;- logit_hessian(beta, X, y)\n  V     &lt;- solve(-H_hat)          # -H is the observed information\n\n  list(coef       = setNames(beta, colnames(X)),\n       se         = setNames(sqrt(diag(V)), colnames(X)),\n       vcov       = V,\n       loglik     = logit_ll(beta, X, y),\n       iterations = it,\n       converged  = converged,\n       history    = as_tibble(do.call(rbind, hist)))\n}\n\n\nStarting values. Zero, which says every coefficient is null and every \\(\\pi_i\\) is one-half. The binomial log-likelihood is globally concave, so there is one hill and one top and it does not matter much where we start \u2014 but zero is the honest default, and it is the value whose log-likelihood we can compute by hand as a check.\nConvergence. The assignment prescribes the step-size rule \u2014 stop when the largest absolute element of the step falls below tol \u2014 so that is what this key uses, and it is what the maximization notes use.\nWorth knowing for later, though not asked for here: of the three candidates, the score is the most principled, because the maximum is defined by the derivative being zero, while \u201cthe step got small\u201d and \u201cthe log-likelihood stopped moving\u201d are statements about how the algorithm is behaving rather than about where we are. On a well-conditioned, globally concave problem like this one all three fire within an iteration of each other. On a badly scaled likelihood the step can go small because the curvature is enormous rather than because we have arrived, and there the difference starts to matter."
  },
  {
    "objectID": "ex22026answers.html#testing-the-log-likelihood",
    "href": "ex22026answers.html#testing-the-log-likelihood",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "6. Testing the log-likelihood",
    "text": "6. Testing the log-likelihood\nAt \\(\\beta = \\mathbf{0}\\) every observation contributes \\(\\ln(0.5)\\), one or zero alike, so\n\\[\n\\ell(\\mathbf{0}) = \\sum_{i=1}^{n} \\ln(0.5) = n\\ln(0.5).\n\\]\n\n\ncode\ntibble(quantity = c(\"logit_ll at beta = 0\", \"n * log(0.5)\"),\n       value    = c(logit_ll(b_zero, X, sim$y), n * log(0.5)))\n\n\n# A tibble: 2 x 2\n  quantity              value\n  &lt;chr&gt;                 &lt;dbl&gt;\n1 logit_ll at beta = 0 -1386.\n2 n * log(0.5)         -1386."
  },
  {
    "objectID": "ex22026answers.html#testing-the-score",
    "href": "ex22026answers.html#testing-the-score",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "7. Testing the score",
    "text": "7. Testing the score\nThe score is \\(X'(y - \\pi)\\), and at \\(\\beta = \\mathbf{0}\\) that is \\(X'(y - 0.5)\\). Element by element: the intercept column is all ones, so its entry is \\(\\sum_i y_i - n/2\\); a covariate column \\(x_j\\) gives \\(\\sum_i x_{ij}(y_i - 0.5)\\).\n\n\ncode\nby_hand &lt;- c(sum(sim$y) - n / 2,\n             sum(sim$x1 * (sim$y - 0.5)),\n             sum(sim$x2 * (sim$y - 0.5)))\n\ntibble(term       = colnames(X),\n       from_fn    = logit_score(b_zero, X, sim$y),\n       by_hand    = by_hand,\n       difference = logit_score(b_zero, X, sim$y) - by_hand)\n\n\n# A tibble: 3 x 4\n  term        from_fn by_hand difference\n  &lt;chr&gt;         &lt;dbl&gt;   &lt;dbl&gt;      &lt;dbl&gt;\n1 (Intercept)   -401    -401           0\n2 x1             267.    267.          0\n3 x2            -288    -288           0\n\n\nElement by element, not as a total. A score can be right in two places and wrong in the third, and a sum would hide it: flip the sign on the x1 element and this table shows x1 off by twice its own value while the intercept and x2 still agree to the last decimal."
  },
  {
    "objectID": "ex22026answers.html#testing-the-hessian",
    "href": "ex22026answers.html#testing-the-hessian",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "8. Testing the Hessian",
    "text": "8. Testing the Hessian\nAt \\(\\beta = \\mathbf{0}\\) every weight \\(\\pi_i(1-\\pi_i)\\) is \\(0.5 \\times 0.5 = 0.25\\). A constant factors out of the matrix product, so the whole Hessian is\n\\[\nH(\\mathbf{0}) \\;=\\; -0.25\\,X'X.\n\\]\n\n\ncode\nH_fn   &lt;- logit_hessian(b_zero, X, sim$y)\nH_hand &lt;- -0.25 * t(X) %*% X\n\nc(matrices_agree      = isTRUE(all.equal(unname(H_fn), unname(H_hand))),\n  intercept_is_neg_qn = isTRUE(all.equal(unname(H_fn[1, 1]), -0.25 * n)),\n  diagonal_negative   = all(diag(H_fn) &lt; 0))\n\n\n     matrices_agree intercept_is_neg_qn   diagonal_negative \n               TRUE                TRUE                TRUE \n\n\n\n\ncode\nround(H_fn, 3)\n\n\n            (Intercept)       x1       x2\n(Intercept)     -500.00   -0.680 -248.500\nx1                -0.68 -509.209    8.649\nx2              -248.50    8.649 -248.500\n\n\nThe intercept\u2019s own diagonal entry is \\(-0.25n = -500\\), because the intercept column is all ones and \\(X'X\\) counts the sample.\nWhy the diagonal has to be negative. A maximum curves downward. The diagonal entry for \\(\\beta_j\\) is how fast the slope in the \\(\\beta_j\\) direction is falling as \\(\\beta_j\\) increases; at a peak it must be falling, so the entry is negative. The standard errors come from inverting \\(-H\\), and \\(-H\\) has a positive diagonal only if \\(H\\) has a negative one. A positive entry in \\(H\\) would give a negative variance and sqrt() would hand back NaN \u2014 which is what separation looks like from inside the algorithm."
  },
  {
    "objectID": "ex22026answers.html#testing-the-whole-estimator",
    "href": "ex22026answers.html#testing-the-whole-estimator",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "9. Testing the whole estimator",
    "text": "9. Testing the whole estimator\n\n\ncode\nfit_sim &lt;- fit_logit(X, sim$y)\nm_glm   &lt;- glm(y ~ x1 + x2, family = binomial(link = \"logit\"), data = sim)\n\noptions(pillar.sigfig = 7)   # or the two columns round into agreement\n\ntibble(term         = colnames(X),\n       truth        = as.vector(truth),\n       hand         = fit_sim$coef,\n       glm          = coef(m_glm),\n       `hand - glm` = fit_sim$coef - coef(m_glm),\n       `(hand - truth)/se` = (fit_sim$coef - truth) / fit_sim$se)\n\n\n# A tibble: 3 x 6\n  term        truth       hand        glm  `hand - glm` `(hand - truth)/se`\n  &lt;chr&gt;       &lt;dbl&gt;      &lt;dbl&gt;      &lt;dbl&gt;         &lt;dbl&gt;               &lt;dbl&gt;\n1 (Intercept) -0.5  -0.5333638 -0.5333638 -1.102785e-12          -0.4852052\n2 x1           0.75  0.7012762  0.7012762  3.417933e-12          -0.8556023\n3 x2          -1    -0.9009602 -0.9009602 -3.560818e-12           0.9330644\n\n\n\n\ncode\ntibble(term         = colnames(X),\n       hand_se      = fit_sim$se,\n       glm_se       = sqrt(diag(vcov(m_glm))),\n       difference   = fit_sim$se - sqrt(diag(vcov(m_glm))))\n\n\n# A tibble: 3 x 4\n  term           hand_se     glm_se  difference\n  &lt;chr&gt;            &lt;dbl&gt;      &lt;dbl&gt;       &lt;dbl&gt;\n1 (Intercept) 0.06876222 0.06876219 2.638044e-8\n2 x1          0.05694676 0.05694670 6.151835e-8\n3 x2          0.1061447  0.1061446  7.054059e-8\n\n\nAgainst glm: the coefficients agree to about twelve decimal places and the standard errors to about seven. That is what a correct implementation looks like, and the exact Hessian is why the agreement is this tight \u2014 a general-purpose optimizer working from an approximated Hessian does not get this close.\nAgainst the truth: the three ratios are -0.49, -0.86, 0.93 \u2014 all comfortably inside one standard error. That is the right comparison and the right result. They will never agree to five decimals, because \\(\\hat\\beta\\) is computed from 2,000 draws and the truth is not.\nWhat size gap should worry you. If the estimator is correct each ratio is approximately standard normal, and since we react to the largest of \\(k\\) of them, the relevant probability is \\(1 - (1-p)^k\\). With three coefficients: \\(\\lvert z \\rvert\\) over 2 turns up in about one run in eight, over 3 in one in 124, over 4 in one in 5,300, and over 6 in one in 170 million.\nTwo different answers hide in that column, and full credit needs only the first. At 3, go look \u2014 not because it is proof, but because the decision is lopsided: checking working code costs ten minutes, and not checking broken code costs a reported result. At 6, luck is off the list; the explanation is in the code.\nThe better answer, and the one worth rewarding when a student finds it: re-run with a different seed. Sampling noise moves; a bug sits still. Eight runs of the correct estimator here give max \\(\\lvert z \\rvert\\) between 0.77 and 1.72 \u2014 never near 2. A student who proposes this has understood that the ratio is a statement about repeated samples, which is the whole point of the question.\nThe distinction students should take away: this is the arithmetic of a \\(z\\) statistic put to a different use. We are not testing a claim about the world, because we know \\(\\beta\\) \u2014 we chose it. The hypothesis on trial is that the code is right.\n\n\ncode\nfit_sim$history %&gt;%\n  mutate(loglik   = round(loglik, 4),\n         max_grad = signif(max_grad, 4),\n         max_step = signif(max_step, 4))\n\n\n# A tibble: 6 x 4\n   iter    loglik     max_grad     max_step\n  &lt;dbl&gt;     &lt;dbl&gt;        &lt;dbl&gt;        &lt;dbl&gt;\n1     1 -1386.294 4.01    e+ 2 6.727   e- 1\n2     2 -1106.335 5.163   e+ 1 2.022   e- 1\n3     3 -1095.908 5.414   e+ 0 2.56    e- 2\n4     4 -1095.779 7.77    e- 2 3.743   e- 4\n5     5 -1095.779 1.66    e- 5 7.951   e- 8\n6     6 -1095.779 7.198000e-13 3.725000e-15\n\n\nThe log-likelihood climbs fast and then stops moving \u2014 nearly all of the gain is in the first two or three steps. The score falls toward zero, and it is the score, not the log-likelihood, that tells you how close you are: the log-likelihood looks converged several iterations before the gradient does, because near the top the surface is flat and large moves in \\(\\beta\\) buy almost no height."
  },
  {
    "objectID": "ex22026answers.html#the-ces-model",
    "href": "ex22026answers.html#the-ces-model",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "10. The CES model",
    "text": "10. The CES model\n\n10. Estimate a real model with your own estimator: vote choice on age, white, and pro-choice.\n\n\n\ncode\nces &lt;- read_csv(here::here(\"data\", \"ces.csv\"), show_col_types = FALSE)\n\n# Recode, then listwise deletion by hand -- before building X and y, so the\n# two objects describe the same rows.\ncesa &lt;- ces %&gt;%\n  mutate(vote  = case_when(votechoice == \"Joe Biden (Democrat)\"         ~ 1,\n                           votechoice == \"Donald J. Trump (Republican)\" ~ 0,\n                           .default = NA_real_),\n         white = if_else(race == \"White\", 1, 0),\n         pro   = if_else(prochoice == \"Support\", 1, 0)) %&gt;%\n  select(vote, age, white, pro) %&gt;%\n  drop_na()\n\nXc &lt;- model.matrix(~ age + white + pro, data = cesa)\nyc &lt;- cesa$vote\n\nstopifnot(nrow(Xc) == length(yc))\n\nfit_ces &lt;- fit_logit(Xc, yc)\nm_ces   &lt;- glm(vote ~ age + white + pro, family = binomial(link = \"logit\"), data = cesa)\n\n# glm's listwise deletion should land on the same N.\nstopifnot(nobs(m_ces) == nrow(cesa))\n\nc(rows_after_deletion = nrow(cesa),\n  iterations          = fit_ces$iterations,\n  converged           = fit_ces$converged)\n\n\nrows_after_deletion          iterations           converged \n              43225                   6                   1 \n\n\n43,225 rows survive listwise deletion, and glm agrees \u2014 which is the point of the stopifnot. Most of what is dropped is vote: everyone who did not vote for Biden or Trump is missing by construction, not by accident.\n\n\ncode\ntibble(term       = colnames(Xc),\n       hand       = fit_ces$coef,\n       glm        = coef(m_ces),\n       difference = fit_ces$coef - coef(m_ces))\n\n\n# A tibble: 4 x 4\n  term               hand         glm    difference\n  &lt;chr&gt;             &lt;dbl&gt;       &lt;dbl&gt;         &lt;dbl&gt;\n1 (Intercept)  0.4076236   0.4076236   2.697428e- 9\n2 age         -0.01425039 -0.01425039 -1.909900e-11\n3 white       -1.080594   -1.080594   -2.940523e- 9\n4 pro          2.850897    2.850897    2.017875e- 9\n\n\n\n\ncode\ntibble(term       = colnames(Xc),\n       hand_se    = fit_ces$se,\n       glm_se     = sqrt(diag(vcov(m_ces))),\n       difference = fit_ces$se - sqrt(diag(vcov(m_ces))))\n\n\n# A tibble: 4 x 4\n  term             hand_se       glm_se  difference\n  &lt;chr&gt;              &lt;dbl&gt;        &lt;dbl&gt;       &lt;dbl&gt;\n1 (Intercept) 0.05062157   0.05062096   6.126187e-7\n2 age         0.0007884331 0.0007884231 1.007417e-8\n3 white       0.03286079   0.03286012   6.652150e-7\n4 pro         0.02559190   0.02559151   3.894230e-7\n\n\nNine decimal places on the coefficients, six or seven on the standard errors, in six iterations, on 43,000 observations. Note that age runs from 18 to 95 while the other two covariates are 0/1 \u2014 a scale disparity that would cost a general-purpose optimizer some decimals. The exact Hessian absorbs it, which is the whole reason production routines compute one when they can.\n\n\ncode\nfit_ces$history %&gt;%\n  mutate(loglik   = round(loglik, 2),\n         max_grad = signif(max_grad, 4),\n         max_step = signif(max_step, 4))\n\n\n# A tibble: 6 x 4\n   iter    loglik    max_grad     max_step\n  &lt;dbl&gt;     &lt;dbl&gt;       &lt;dbl&gt;        &lt;dbl&gt;\n1     1 -29961.29 1.655   e+5 2.344   e+ 0\n2     2 -20376.09 2.904   e+4 4.369   e- 1\n3     3 -20008.71 3.93    e+3 1.035   e- 1\n4     4 -19999.94 1.033   e+2 3.159   e- 3\n5     5 -19999.93 8.508   e-2 2.803   e- 6\n6     6 -19999.93 6.532000e-8 2.269000e-12"
  },
  {
    "objectID": "ex22026answers.html#notes",
    "href": "ex22026answers.html#notes",
    "title": "Exercise #2 \u2014 Write the Maximizer (Answers)",
    "section": "Notes",
    "text": "Notes\n\nEvery number in this key is produced by the four functions in Part I. The only calls to glm are the two comparisons in questions 9 and 10.\nCommon failure modes seen in this assignment: building \\(W\\) as a full \\(n \\times n\\) diagonal matrix (works on the simulation, exhausts memory on the CES data); computing the standard errors from the loop\u2019s Hessian rather than recomputing at \\(\\hat\\beta\\); letting glm do the listwise deletion and then building X and y from different row sets; and returning estimates from a fit that hit maxit without converging."
  },
  {
    "objectID": "cescodebook26.html",
    "href": "cescodebook26.html",
    "title": "Codebook \u2014 ces.csv",
    "section": "",
    "text": "ces.csv holds 61,000 respondents from the 2020 Cooperative Election Study common content, one row per respondent, subset to twenty columns. Everything except age and the row index arrives as a character string, so nothing is ordered, coded, or labeled for you.\nTwo general cautions before the variable list. The extract carries no survey weights \u2014 the CES commonweight is not among these columns \u2014 so anything you compute here is an unweighted sample statistic, not an estimate of the 2020 electorate. And on the policy items, the labels tell you which side a respondent took, not what the side means: on three of them \u201cSupport\u201d is the conservative answer and on three others it is the liberal one. The direction column below is the part of this document to read twice."
  },
  {
    "objectID": "cescodebook26.html#source",
    "href": "cescodebook26.html#source",
    "title": "Codebook \u2014 ces.csv",
    "section": "Source",
    "text": "Source\nCooperative Election Study, Harvard University, 2020 common content. https://cces.gov.harvard.edu. Twenty columns, 61,000 rows, no survey weights."
  },
  {
    "objectID": "prediction26.html",
    "href": "prediction26.html",
    "title": "Prediction Methods for MLE Models",
    "section": "",
    "text": "Most MLE models are nonlinear, so their coefficients are not their marginal effects. To get a substantive quantity out of one, we have to transform the linear prediction \\(x_i\\widehat\\beta\\) through the link. The methods here apply to most MLE applications; the immediate examples are binary-response models, but these slides are the foundation for prediction in every model we encounter this semester.\nYou\u2019ll be familiar with these methods from the linear-models class \u2014 predictions at means, average effects, and simulation-based uncertainty. These slides are a review, but a rigorous one: in the nonlinear world the coefficient really is not the effect, so the transformations do more work. Treat these slides as a reference we\u2019ll refer back to when we need a quantity of interest from a count, a hazard, or a choice model later.\n\n\n\n\n\n\nThe process, up front\n\n\n\nEvery quantity of interest described here \u2014 and in every model we meet the rest of the semester \u2014 comes from the same sequence:\n\nestimate the model;\nchoose an approach \u2014 at-means (hold the other covariates at central tendencies, vary the \\(x\\) of interest) or average effects (set the \\(x\\) of interest for the whole estimation sample, predict every case, average);\nbuild the linear prediction \\(x\\widehat\\beta\\) for the interesting values of \\(x\\);\ntransform through the link, \\(F(x\\widehat\\beta)\\) \u2014 in the linear model this step did nothing; here it does the work;\nattach uncertainty by an analytic standard error, by simulating the parameters, or by a bootstrap;\nform the interval and plot.\n\nSteps 2 (the approach) and 5 (the technique) are independent choices: any approach pairs with any technique. These slides are organized around that distinction \u2014 the approaches first, then the techniques, then the two joined together to form the confidence interval."
  },
  {
    "objectID": "prediction26.html#at-means-predictions",
    "href": "prediction26.html#at-means-predictions",
    "title": "Prediction Methods for MLE Models",
    "section": "At-means predictions",
    "text": "At-means predictions\nAt-means predictions are what they sound like \u2014 effects with the other independent variables held at central tendencies. They are sometimes called adjusted predictions. The recipe:\n\nestimate the model;\ncreate an out-of-sample data frame;\nvary the \\(x\\) of interest; set the other \\(x\\) variables to appropriate central tendencies (mode for dummies, median for skewed or categorical variables) \u2014 hence \u201cat means\u201d;\ngenerate the quantities of interest in that out-of-sample data.\n\nIn symbols, the at-means prediction is \\(F(\\mathbf{x}_0\\widehat\\beta)\\), where \\(\\mathbf{x}_0\\) is a single row we construct \u2014 the \\(x\\) of interest at the value we want, everything else at its central tendency. One row, one prediction. Sweep the \\(x\\) of interest across its range and we get one prediction per value, and that is the curve we plot. The at-means curve describes one hypothetical case \u2014 here, one dyad \u2014 and how its probability moves as \\(x\\) moves."
  },
  {
    "objectID": "prediction26.html#average-effects",
    "href": "prediction26.html#average-effects",
    "title": "Prediction Methods for MLE Models",
    "section": "Average effects",
    "text": "Average effects\nAverage effects are in-sample, but they build a counterfactual for the variable of interest \u2014 they ask what the predictions would look like if the entire sample took a particular value on that variable, while everything else stayed as it is in the data.\nSuppose a model of wages with covariates for education and gender. We might ask: what would the predictions look like if the entire sample were male, but otherwise looked as it does? And then: what if the entire sample were female, all else the same? To answer, we set the gender variable to male, generate the prediction for every case, and average; then repeat with gender set to female. The recipe:\n\nestimate the model;\nin the estimation data, set the variable of interest to a particular value for the entire sample;\ngenerate the quantities of interest (expected values and standard errors);\ntake the average, and save it;\nrepeat for every value of the variable of interest, and plot.\n\nAverage effects are often the better choice because they represent the data more completely than a single central-tendency case can.\nIn symbols, the average effect at a value \\(x^{*}\\) is\n\\[\n\\bar P(x^{*}) = \\frac{1}{n}\\sum_{i=1}^{n} F(\\mathbf{x}_i\\widehat\\beta),\\qquad \\text{with every case's } x \\text{ of interest set to } x^{*},\n\\]\nwhere \\(\\mathbf{x}_i\\) is a real row of the estimation sample with only its \\(x\\) of interest overwritten. So we compute \\(n\\) predictions, one per case, and average them; then move \\(x^{*}\\) and do it again. The two approaches differ in what they hold constant. At-means holds the other covariates at one constructed value; average effects hold them at their observed values, all \\(n\\) of them. And they differ in what they produce: at-means yields one prediction per value of \\(x\\); average effects yield \\(n\\) predictions per value, collapsed to their mean.\nPut differently, the at-means curve is one case; the average-effects curve is the whole sample. In the linear model, with every covariate held at its mean, these are the same number \u2014 the average of the linear predictions is the linear prediction at the averages. Through a nonlinear link they are not: the average of \\(F(\\cdot)\\) across cases is not \\(F(\\cdot)\\) at the average case. We will see the gap between them on the running model below."
  },
  {
    "objectID": "prediction26.html#the-running-model",
    "href": "prediction26.html#the-running-model",
    "title": "Prediction Methods for MLE Models",
    "section": "The running model",
    "text": "The running model\nI\u2019ll use the democratic-peace data \u2014 dyad-years, a militarized dispute as \\(y\\), and covariates for shared border, the lower dyad democracy score, the capability ratio, and alliance.\n\n\ncode\ndp &lt;- read_dta(here::here(\"data\", \"dp.dta\"))\ndp$lncaprat &lt;- log(dp$caprat)   # a capability ratio is multiplicative, so model it on the log scale\n\nm1 &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n          family = binomial(link = \"logit\"),  data = dp)\nm2 &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n          family = binomial(link = \"probit\"), data = dp)\n\nmodelsummary(\n  list(\"Logit\" = m1, \"Probit\" = m2),\n  coef_rename = c(\"border\" = \"Shared border\", \"deml\" = \"Democracy\",\n                  \"lncaprat\" = \"Capabilities ratio (log)\", \"ally\" = \"Allied\"),\n  statistic = \"std.error\",\n  gof_omit  = \"AIC|BIC|RMSE|F|Log\",\n  notes     = \"Standard errors in parentheses.\"\n)\n\n\n\n\n    \n\n    \n    \n      \n        \n        \n              \n                 \n                Logit\n                Probit\n              \n        \n        Standard errors in parentheses.\n        \n                \n                  (Intercept)             \n                  -2.987 \n                  -1.653 \n                \n                \n                                          \n                  (0.107)\n                  (0.048)\n                \n                \n                  Shared border           \n                  1.036  \n                  0.491  \n                \n                \n                                          \n                  (0.088)\n                  (0.041)\n                \n                \n                  Democracy               \n                  -0.070 \n                  -0.031 \n                \n                \n                                          \n                  (0.007)\n                  (0.003)\n                \n                \n                  Capabilities ratio (log)\n                  -0.227 \n                  -0.101 \n                \n                \n                                          \n                  (0.024)\n                  (0.011)\n                \n                \n                  Allied                  \n                  -0.842 \n                  -0.371 \n                \n                \n                                          \n                  (0.081)\n                  (0.038)\n                \n                \n                  Num.Obs.                \n                  20990  \n                  20990"
  },
  {
    "objectID": "prediction26.html#at-means-predictions-direct-computation",
    "href": "prediction26.html#at-means-predictions-direct-computation",
    "title": "Prediction Methods for MLE Models",
    "section": "At-means predictions (direct computation)",
    "text": "At-means predictions (direct computation)\nHere I hold the other covariates at representative values \u2014 modal border and alliance (both zero), median capability ratio \u2014 and vary democracy across its range. I predict on the link scale, form the interval there, and push the point and both bounds through the logistic CDF. That last step is end-point transformation (EPT): building the interval on the linear scale and mapping it into probability space keeps the bounds inside \\([0,1]\\) and makes them asymmetric, as a probability interval should be.\n\n\ncode\n# Out-of-sample data: democracy varies; the others held at representative values.\nmem &lt;- tibble(deml = seq(-10, 10, 1), border = 0, ally = 0,\n              lncaprat = median(dp$lncaprat, na.rm = TRUE))\n\n# type = \"link\" gives the linear prediction x'b and its SE; transform by hand (EPT).\npr  &lt;- predict(m1, newdata = mem, type = \"link\", se.fit = TRUE)\nmem &lt;- mem %&gt;%\n  mutate(fit = pr$fit, se = pr$se.fit,\n         p  = plogis(fit),\n         lb = plogis(fit - 1.96 * se),\n         ub = plogis(fit + 1.96 * se))\n\nggplot(mem, aes(deml, p)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), fill = bu_green, alpha = 0.5) +\n  geom_line(color = bu_green, linewidth = 1) +\n  labs(title = \"At-means prediction: Pr(dispute) across democracy\",\n       x = \"minimum dyad democracy (Polity)\",\n       y = \"Pr(dispute) with 95% CI\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a02: At-means prediction: Pr(dispute) across democracy for a non-bordering, non-allied dyad at median capabilities, with a 95% interval by end-point transformation."
  },
  {
    "objectID": "prediction26.html#building-a-scenarios-linear-prediction",
    "href": "prediction26.html#building-a-scenarios-linear-prediction",
    "title": "Prediction Methods for MLE Models",
    "section": "Building a scenario\u2019s linear prediction",
    "text": "Building a scenario\u2019s linear prediction\nEvery quantity in these slides starts the same way \u2014 with a linear prediction \\(x\\widehat\\beta\\) for some scenario \u2014 so it is worth slowing down on how we build one. There are three ways, and they teach different things. Take one scenario: a non-bordering, non-allied dyad at median capabilities and a democracy score of zero.\nBy hand. Write the linear predictor out, one coefficient times one value at a time, pulling coefficients by name so nothing depends on their order:\n\nb &lt;- coef(m1)\nxb_hand &lt;- b[\"(Intercept)\"] + b[\"border\"] * 0 + b[\"deml\"] * 0 +\n           b[\"lncaprat\"] * median(dp$lncaprat, na.rm = TRUE) + b[\"ally\"] * 0\nas.numeric(xb_hand)\n\n[1] -3.682182\n\n\nThis is the most transparent version \u2014 you see exactly which value each coefficient multiplies. It is also the one that gets error-prone the moment the model has an interaction, a polynomial, or a factor: you have to remember every product term and every dummy contrast yourself.\nWith model.matrix. The same design row, built mechanically from the model\u2019s formula:\n\nscenario &lt;- tibble(border = 0, deml = 0, ally = 0,\n                   lncaprat = median(dp$lncaprat, na.rm = TRUE))\nX_row &lt;- model.matrix(~ border + deml + lncaprat + ally, data = scenario)\nas.numeric(X_row %*% coef(m1))\n\n[1] -3.682182\n\n\nSame number. model.matrix reads the formula and assembles the row \u2014 intercept, contrasts, interactions and all \u2014 so it stays consistent with how the model was fit. That consistency is the payoff: as specifications get complicated, the by-hand version is where mistakes hide, and model.matrix doesn\u2019t make them.\nWith predict. The fully automated route \u2014 which is model.matrix under the hood, plus the link:\n\npredict(m1, newdata = scenario, type = \"link\")   # x'b, the same value\n\n        1 \n-3.682182 \n\n\nI\u2019ll use the by-hand linear predictor where it makes the mechanics clearest \u2014 the simulation scenarios below \u2014 and lean on model.matrix for the matrix machinery, where writing \\(X\\) out by hand would be tedious and easy to botch."
  },
  {
    "objectID": "prediction26.html#average-effects-1",
    "href": "prediction26.html#average-effects-1",
    "title": "Prediction Methods for MLE Models",
    "section": "Average effects",
    "text": "Average effects\nNow the average-effects version, close to how you\u2019d first write it. First reconstruct the estimation sample \u2014 the rows the model was actually fit on, after listwise deletion \u2014 so the counterfactual runs on those cases and not on rows the model never saw (na.action(m1) returns the dropped indices). Then sweep democracy: at each value, set democracy to that value for the whole sample, predict every case on the link scale with its standard error, transform, and take the average predicted probability. For an interval I do the natural thing: end-point-transform each case\u2019s own interval, and average the bounds the same way I average the predictions.\nI\u2019ll write it first the familiar way \u2014 an explicit loop with a preallocated vector:\n\n\ncode\n# Reconstruct the estimation sample (rows kept after listwise deletion).\ndp$used &lt;- TRUE\ndp$used[na.action(m1)] &lt;- FALSE\ndpe &lt;- dp %&gt;% filter(used == TRUE)\n\npolity &lt;- seq(-10, 10, 1)\navg_nb &lt;- numeric(length(polity))          # average Pr(dispute), no border\nlb_nb  &lt;- numeric(length(polity))          # average of the case-level lower bounds\nub_nb  &lt;- numeric(length(polity))          # average of the case-level upper bounds\n\nfor (i in seq_along(polity)) {\n  cf &lt;- dpe                                 # counterfactual COPY -- never overwrite dpe itself\n  cf$deml   &lt;- polity[i]                    # set democracy for the whole sample\n  cf$border &lt;- 0\n  pr &lt;- predict(m1, newdata = cf, type = \"link\", se.fit = TRUE)   # every case's x'b and its SE\n  avg_nb[i] &lt;- mean(plogis(pr$fit), na.rm = TRUE)                  # average Pr(dispute)\n  lb_nb[i]  &lt;- mean(plogis(pr$fit - 1.96 * pr$se.fit), na.rm = TRUE)\n  ub_nb[i]  &lt;- mean(plogis(pr$fit + 1.96 * pr$se.fit), na.rm = TRUE)\n}\ntibble(polity, avg_nb, lb_nb, ub_nb) %&gt;% head()\n\n\n# A tibble: 6 x 4\n  polity avg_nb  lb_nb  ub_nb\n   &lt;dbl&gt;  &lt;dbl&gt;  &lt;dbl&gt;  &lt;dbl&gt;\n1    -10 0.0393 0.0337 0.0458\n2     -9 0.0368 0.0316 0.0427\n3     -8 0.0344 0.0297 0.0399\n4     -7 0.0321 0.0278 0.0372\n5     -6 0.0300 0.0260 0.0348\n6     -5 0.0281 0.0243 0.0325\n\n\nThe loop body is a single idea \u2014 \u201cset a scenario for the whole sample, average the predictions\u201d \u2014 repeated. That is what a function is for. Wrapping it gets us both curves without copying the loop:\n\n\ncode\navg_curve &lt;- function(border_value) {\n  map_dfr(seq(-10, 10, 1), function(d) {\n    cf &lt;- dpe                              # counterfactual copy, same rule as above\n    cf$deml   &lt;- d\n    cf$border &lt;- border_value\n    pr &lt;- predict(m1, newdata = cf, type = \"link\", se.fit = TRUE)\n    tibble(polity = d,\n           avg = mean(plogis(pr$fit), na.rm = TRUE),\n           lb  = mean(plogis(pr$fit - 1.96 * pr$se.fit), na.rm = TRUE),\n           ub  = mean(plogis(pr$fit + 1.96 * pr$se.fit), na.rm = TRUE))\n  })\n}\n\nnoborder &lt;- avg_curve(0)\nborder   &lt;- avg_curve(1)\n\nggplot(mapping = aes(polity)) +\n  geom_ribbon(data = noborder, aes(ymin = lb, ymax = ub), fill = bu_green,      alpha = 0.5) +\n  geom_ribbon(data = border,   aes(ymin = lb, ymax = ub), fill = bu_lightgreen, alpha = 0.5) +\n  geom_line(data = noborder, aes(y = avg), color = bu_green,      linewidth = 1) +\n  geom_line(data = border,   aes(y = avg), color = bu_lightgreen, linewidth = 1) +\n  annotate(\"text\", x = 0, y = border$ub[border$polity == 0] + 0.008,\n           label = \"shares a border\", color = bu_black) +\n  annotate(\"text\", x = 0, y = noborder$lb[noborder$polity == 0] - 0.006,\n           label = \"no border\", color = bu_green) +\n  labs(title = \"Average effects: average Pr(dispute) across democracy\",\n       x = \"Polity score\", y = \"average Pr(dispute) with 95% band\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a03: Average effects: average Pr(dispute) across democracy, with and without a shared border. The bands average the case-level intervals.\n\n\n\n\n\nThe curves are the average predicted probability at each democracy value. I want to be plain about what the bands are. Each case got its own end-point-transformed interval, and I averaged the bounds the same way I averaged the predictions. That is the uncertainty of a typical case in the counterfactual sample. It is not the uncertainty of the average: the average is a single quantity, a function of \\(\\widehat\\beta\\), and it deserves a standard error of its own. The two are not the same thing, and the band that averages case-level intervals is wider than the average\u2019s own interval. This is exactly where the prediction approach (average effects) meets the uncertainty technique. We fix it in the Uncertainty section, where we return to these curves and bound them properly two different ways \u2014 an analytic (delta-method) standard error for the average, and simulation."
  },
  {
    "objectID": "prediction26.html#at-means-and-average-effects-side-by-side",
    "href": "prediction26.html#at-means-and-average-effects-side-by-side",
    "title": "Prediction Methods for MLE Models",
    "section": "At-means and average effects, side by side",
    "text": "At-means and average effects, side by side\nWe now have both approaches on the same model, so put them on the same axes. Both curves below hold border at zero. The at-means curve also holds ally at its mode (zero) and the capability ratio at its median; the average-effects curve lets both vary as they do in the data.\n\n\ncode\nggplot() +\n  geom_ribbon(data = mem, aes(deml, ymin = lb, ymax = ub), fill = bu_lightgreen, alpha = 0.5) +\n  geom_line(data = mem,   aes(deml, p), color = bu_lightgreen, linewidth = 1) +\n  geom_ribbon(data = noborder, aes(polity, ymin = lb, ymax = ub), fill = bu_green, alpha = 0.5) +\n  geom_line(data = noborder,   aes(polity, avg), color = bu_green, linewidth = 1) +\n  annotate(\"text\", x = -5, y = mem$ub[mem$deml == -5] + 0.005, hjust = 0.3,\n           label = \"at-means: one non-allied dyad\", color = bu_black) +\n  annotate(\"text\", x = 4, y = noborder$lb[noborder$polity == 4] - 0.005, hjust = 0.4,\n           label = \"average effect: the whole sample\", color = bu_green) +\n  labs(title = \"Two approaches, one model (border = 0)\",\n       x = \"minimum dyad democracy (Polity)\", y = \"Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a04: At-means and average-effects curves on the same axes, border = 0.\n\n\n\n\n\nThe at-means curve sits above the average-effects curve everywhere. Nothing is wrong; the two are answering different questions. The at-means curve is a specific dyad \u2014 non-allied, at median capabilities \u2014 and non-allied dyads are the riskier kind (the alliance coefficient is \\(-0.84\\)). The average-effects curve is the whole estimation sample, and about a third of those dyad-years are allied, so their low probabilities pull the average down. There is a second, smaller reason the two would differ even if the sample were all non-allied: the logit is nonlinear, so the average of \\(\\Lambda(x_i\\widehat\\beta)\\) across cases is not \\(\\Lambda(\\cdot)\\) evaluated at an average case. Both reasons come to the same thing. The at-means prediction is one row we made up; the average effect is \\(n\\) rows we observed.\nThat \u201c\\(n\\) rows\u201d is worth seeing. At each democracy value the average effect is the mean of \\(n\\) individual predictions, one per dyad-year: every case\u2019s own probability of a dispute, given its own capabilities and alliance status, with democracy set to that value. Figure\u00a05 plots those \\(n\\) predictions as a density, one panel per democracy value. The solid line is their mean \u2014 the average effect at that value, the number the average-effects curve plots. The dashed line is the at-means prediction from Figure\u00a02.\n\n\ncode\n# Individual predicted probabilities, one per dyad-year, at three democracy values, border = 0.\ncase_preds &lt;- map_dfr(c(-10, 0, 10), function(d) {\n  cf &lt;- dpe\n  cf$deml   &lt;- d\n  cf$border &lt;- 0\n  tibble(polity = d, p = predict(m1, newdata = cf, type = \"response\"))\n})\n\n# The mean of each panel (the average effect) and the at-means prediction at that value.\nmarkers &lt;- tibble(polity = c(-10, 0, 10)) %&gt;%\n  mutate(avg     = map_dbl(polity, ~ mean(case_preds$p[case_preds$polity == .x])),\n         atmeans = map_dbl(polity, ~ mem$p[mem$deml == .x]))\n\nggplot(case_preds, aes(p)) +\n  geom_density(fill = bu_green, alpha = 0.5, color = bu_green) +\n  geom_vline(data = markers, aes(xintercept = avg),     color = bu_green, linewidth = 1) +\n  geom_vline(data = markers, aes(xintercept = atmeans), color = bu_black, linetype = \"dashed\") +\n  facet_wrap(~ polity, ncol = 1, labeller = label_both, scales = \"free_y\") +\n  labs(title = \"The individual predictions behind each average effect\",\n       subtitle = \"solid = their mean (the average effect); dashed = the at-means prediction\",\n       x = \"Pr(dispute)\", y = \"density\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a05: The distribution of the individual predicted probabilities at three democracy values, border = 0. Solid line: their mean, the average effect. Dashed line: the at-means prediction.\n\n\n\n\n\nEach panel is the distribution of \\(n = 20{,}990\\) predicted probabilities. The distribution is skewed: most dyad-years are low-risk, and a tail of non-allied dyads with roughly balanced capabilities stretches to the right. The mean of that distribution is the average effect. The at-means prediction sits to the right of it in every panel, for the reason above: it describes a non-allied dyad, and non-allied dyads are riskier than the sample. As democracy rises the whole distribution slides left \u2014 that is the effect of democracy, applied to every dyad-year at once \u2014 and the mean moves with it. The distribution also narrows as it moves: at low democracy the cases sit on the steep part of the logistic curve, where differences in capabilities and alliances translate into large differences in probability, and at high democracy they sit in the flat lower tail, where the same differences count for less."
  },
  {
    "objectID": "prediction26.html#simulation",
    "href": "prediction26.html#simulation",
    "title": "Prediction Methods for MLE Models",
    "section": "Simulation",
    "text": "Simulation\nSimulation \u2014 the King\u2013Tomz\u2013Wittenberg approach (King, Tomz, and Wittenberg 2000) \u2014 is an especially good approach for nonlinear models, because it propagates the estimation uncertainty through the transformation rather than approximating it. The recipe:\n\nestimate the model;\ndraw \\(m\\) times (say 1000) from the sampling distribution of \\(\\widehat\\beta\\) \u2014 a multivariate normal with mean \\(\\widehat\\beta\\) and covariance \\(\\widehat{\\mathrm{Var}}(\\widehat\\beta)\\);\nform the \\(m\\) linear predictions \\(x\\widehat\\beta\\);\ntransform each through the link;\ntake the 2.5th, 50th, and 97.5th percentiles;\nplot against \\(x\\).\n\nWhy does this work? Maximum likelihood theory says that in large samples \\(\\widehat\\beta\\) is distributed normally around the true \\(\\beta\\) with covariance \\(\\mathbf{V} = (-H)^{-1}\\), the inverse negative Hessian from the likelihood notes. We only get one sample, and so one \\(\\widehat\\beta\\), but we know the shape of the distribution it came from. Simulation takes that literally: treat \\(N(\\widehat\\beta, \\mathbf{V})\\) as the sampling distribution, and draw from it. Each draw \\(\\beta^{*}\\) is a coefficient vector another sample could plausibly have produced. Push each draw through the same linear prediction and the same link, and the set of quantities of interest that results carries the estimation uncertainty with it \u2014 no derivative, no approximation beyond the normality we already assumed when we reported standard errors. The percentiles of that set are the interval.\nTwo things to notice. First, the approach has not changed: the scenario below is the same at-means row as before. Simulation is a technique for uncertainty, not a new kind of prediction. Second, the quantity being simulated is the expected value \u2014 the probability of a dispute at a scenario, King, Tomz, and Wittenberg\u2019s \\(E(y \\mid x)\\). Their other quantity, the predicted value, adds the fundamental uncertainty by drawing an outcome \\(\\widetilde y\\) from the model given \\(\\beta^{*}\\). In the linear-model notes that meant adding a disturbance draw, and it produced a wider prediction interval. In a binary model \\(\\widetilde y\\) is a Bernoulli draw with probability \\(\\pi^{*}\\) \u2014 a zero or a one \u2014 so a \u201cprediction interval\u201d for a single case is just \\(\\{0, 1\\}\\). It is the expected value, the probability, that we report and plot.\n\n\ncode\n# Draw 1000 coefficient vectors from each model's sampling distribution.\n# as_tibble keeps the \"(Intercept)\" column name (data.frame would mangle it).\nsimL &lt;- as_tibble(MASS::mvrnorm(1000, coef(m1), vcov(m1)))\nsimP &lt;- as_tibble(MASS::mvrnorm(1000, coef(m2), vcov(m2)))\n\n# For each democracy value, build x'b by hand, take quantiles, transform.\nsim_curve &lt;- function(sim, link) {\n  map_dfr(seq(-10, 10, 1), function(d) {\n    xb &lt;- sim$`(Intercept)` + sim$border * 0 + sim$deml * d +\n          sim$lncaprat * median(dp$lncaprat, na.rm = TRUE) + sim$ally * 0\n    q  &lt;- quantile(xb, probs = c(.025, .5, .975))\n    tibble(polity = d, lb = link(q[1]), med = link(q[2]), ub = link(q[3]))\n  })\n}\n\nlogit_sim  &lt;- sim_curve(simL, plogis)\nprobit_sim &lt;- sim_curve(simP, pnorm)\n\npl &lt;- ggplot(logit_sim, aes(polity, med)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), fill = bu_green, alpha = 0.5) +\n  geom_line(color = bu_green, linewidth = 1) +\n  labs(title = \"Logit (simulated)\", x = \"Polity score\", y = \"Pr(dispute)\") +\n  theme_minimal()\n\npp &lt;- ggplot(probit_sim, aes(polity, med)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), fill = bu_lightgreen, alpha = 0.5) +\n  geom_line(color = bu_lightgreen, linewidth = 1) +\n  labs(title = \"Probit (simulated)\", x = \"Polity score\", y = NULL) +\n  theme_minimal()\n\npl + pp\n\n\n\n\n\n\n\n\nFigure\u00a06: Simulated at-means predictions (King\u2013Tomz\u2013Wittenberg), logit and probit, with 95% percentile bands.\n\n\n\n\n\nHere is what the draws are doing, one draw at a time. Each simulated \\(\\beta^{*}\\) produces a whole curve across democracy, not just one point, because the same draw is used at every value of \\(x\\). Fifty of the thousand draws:\n\n\ncode\n# Fifty of the 1000 simulated coefficient vectors, each traced across democracy.\ndraws &lt;- simL %&gt;% slice(1:50) %&gt;% mutate(draw = row_number())\nspaghetti &lt;- map_dfr(seq(-10, 10, 1), function(d) {\n  draws %&gt;%\n    mutate(polity = d,\n           p = plogis(`(Intercept)` + border * 0 + deml * d +\n                      lncaprat * median(dp$lncaprat, na.rm = TRUE) + ally * 0)) %&gt;%\n    select(draw, polity, p)\n})\n\nggplot() +\n  geom_line(data = spaghetti, aes(polity, p, group = draw), color = bu_lightgreen, alpha = 0.5) +\n  geom_line(data = logit_sim, aes(polity, med), color = bu_green, linewidth = 1.2) +\n  geom_line(data = logit_sim, aes(polity, lb),  color = bu_green, linetype = \"dashed\") +\n  geom_line(data = logit_sim, aes(polity, ub),  color = bu_green, linetype = \"dashed\") +\n  labs(title = \"Fifty simulated curves, and the percentiles of all 1000\",\n       x = \"Polity score\", y = \"Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a07: Fifty simulated at-means curves, one per coefficient draw, with the median and the 2.5th and 97.5th percentiles of all 1000.\n\n\n\n\n\nThe light lines are fifty plausible versions of the at-means curve \u2014 each one a coefficient vector the data could have produced. The dark line is the median of all one thousand at each democracy value, and the dashed lines the 2.5th and 97.5th percentiles. The band in Figure\u00a06 is the envelope of these curves. That is all the technique does.\n\nSimulating combinations of binary variables\nSimulation also lets us compare discrete scenarios and see the whole predicted distribution, not just a point and an interval. Here are the four combinations of border and alliance, at a low democracy score, each as a density of simulated predicted probabilities.\n\n\ncode\nxb &lt;- function(b, a) {\n  plogis(simL$`(Intercept)` + simL$border * b + simL$deml * (-7) +\n         simL$lncaprat * median(dp$lncaprat, na.rm = TRUE) + simL$ally * a)\n}\ncombos &lt;- tibble(b0a0 = xb(0, 0), b1a0 = xb(1, 0),\n                 b0a1 = xb(0, 1), b1a1 = xb(1, 1))\n\nggplot(combos) +\n  geom_density(aes(b0a0), fill = bu_green, alpha = 0.3) +\n  geom_density(aes(b1a0), fill = bu_green, alpha = 0.3) +\n  geom_density(aes(b0a1), fill = bu_green, alpha = 0.3) +\n  geom_density(aes(b1a1), fill = bu_green, alpha = 0.3) +\n  annotate(\"text\", x = 0.04, y = 200, label = \"no border, allied\",     color = bu_green) +\n  annotate(\"text\", x = 0.07, y = 150, label = \"no border, not allied\", color = bu_green) +\n  annotate(\"text\", x = 0.09, y = 50,  label = \"border, allied\",        color = bu_green) +\n  annotate(\"text\", x = 0.13, y = 70,  label = \"border, not allied\",    color = bu_green) +\n  labs(title = \"Simulated Pr(dispute) for four scenarios\",\n       x = \"Pr(dispute)\", y = \"density\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a08: Simulated Pr(dispute) for the four border and alliance scenarios at a democracy score of -7."
  },
  {
    "objectID": "prediction26.html#analytic-standard-errors",
    "href": "prediction26.html#analytic-standard-errors",
    "title": "Prediction Methods for MLE Models",
    "section": "Analytic standard errors",
    "text": "Analytic standard errors\n\nML standard error of the linear prediction\nThe most direct measure of uncertainty is the standard error of the linear prediction \\(X\\widehat\\beta\\). Under maximum likelihood,\n\\[\n\\mathrm{Var}(X\\widehat\\beta) = \\mathbf{X}\\,\\mathbf{V}\\,\\mathbf{X}',\n\\]\nan \\(N \\times N\\) matrix, where \\(\\mathbf{V}\\) is the variance\u2013covariance matrix of \\(\\widehat\\beta\\) (the \\((-H)^{-1}\\) from the likelihood notes). Its diagonal holds the variances of the \\(N\\) predictions, so\n\\[\n\\mathrm{se}(X\\widehat\\beta) = \\sqrt{\\mathrm{diag}(\\mathbf{X}\\,\\mathbf{V}\\,\\mathbf{X}')},\n\\]\nan \\(N \\times 1\\) vector. These are standard errors of the linear prediction, so to get an interval on the probability we still map the bounds through the link (end-point transformation, below).\nHere it is by hand, on the at-means grid of Figure\u00a02. I build the design matrix with model.matrix, form \\(\\sqrt{\\mathrm{diag}(XVX')}\\), and confirm it matches what predict returns \u2014 the point being that predict\u2019s standard errors are this matrix computation:\n\n\ncode\n# Design matrix for the at-means grid (democracy varies, others held).\nXg &lt;- model.matrix(~ border + deml + lncaprat + ally, data = mem)\nV  &lt;- vcov(m1)\n\nxb     &lt;- as.vector(Xg %*% coef(m1))            # linear prediction x'b\nse_lin &lt;- sqrt(diag(Xg %*% V %*% t(Xg)))        # ML SE of x'b, by hand\n\n# Check against predict()'s SE of the linear prediction.\ntibble(deml       = mem$deml,\n       se_byhand  = se_lin,\n       se_predict = predict(m1, newdata = mem, type = \"link\", se.fit = TRUE)$se.fit) %&gt;%\n  head(4)\n\n\n# A tibble: 4 x 3\n   deml se_byhand se_predict\n  &lt;dbl&gt;     &lt;dbl&gt;      &lt;dbl&gt;\n1   -10    0.0605     0.0605\n2    -9    0.0578     0.0578\n3    -8    0.0558     0.0558\n4    -7    0.0547     0.0547\n\n\nThey agree to machine precision: predict is doing exactly this \\(\\sqrt{\\mathrm{diag}(XVX')}\\).\n\n\nDelta-method standard errors\nThe ML route works for a monotone function of \\(X\\widehat\\beta\\), like the logit and probit. When the function is not monotone in \\(X\\widehat\\beta\\) \u2014 as in multinomial logit \u2014 we use the delta method, a first-order Taylor approximation of the function\u2019s variance. Greene (2012) (693ff) gives the general form\n\\[\n\\mathrm{Var}\\!\\left[F(X\\widehat\\beta)\\right] = f(x'\\widehat\\beta)^2 \\; \\mathbf{x}'\\,\\mathbf{V}\\,\\mathbf{x},\n\\]\nthe variance of whatever \\(F(X\\widehat\\beta)\\) is \u2014 here a predicted probability. For the logit, \\(f = F(1-F)\\), so the delta standard error of a predicted probability is\n\\[\np\\,(1-p)\\,\\sqrt{\\mathbf{x}'\\,\\mathbf{V}\\,\\mathbf{x}}\n\\]\n\u2014 the predicted probability times its complement times the standard error of the linear prediction. On the same grid, that is one line on top of the ML standard error we just built:\n\n\ncode\np        &lt;- plogis(xb)             # predicted probability\nse_delta &lt;- p * (1 - p) * se_lin   # delta SE of the predicted probability\ntibble(deml = mem$deml, p = p, se_delta = se_delta) %&gt;% head(4)\n\n\n# A tibble: 4 x 3\n   deml      p se_delta\n  &lt;dbl&gt;  &lt;dbl&gt;    &lt;dbl&gt;\n1   -10 0.0483  0.00278\n2    -9 0.0452  0.00249\n3    -8 0.0422  0.00226\n4    -7 0.0395  0.00207\n\n\nSo we now have two standard errors for the same at-means grid \u2014 the ML standard error of the linear prediction, and the delta standard error of the probability \u2014 and each builds an interval. The ML route builds it on the link scale and transforms the ends. The delta route builds it directly on the probability scale, \\(p \\pm 1.96\\cdot\\mathrm{se}_\\delta\\). Here they are on top of each other:\n\n\ncode\ngrid_ci &lt;- mem %&gt;%\n  mutate(ept_lb   = plogis(fit - 1.96 * se),  ept_ub   = plogis(fit + 1.96 * se),\n         delta_lb = p - 1.96 * se_delta,      delta_ub = p + 1.96 * se_delta)\n\nggplot(grid_ci, aes(deml)) +\n  geom_ribbon(aes(ymin = ept_lb, ymax = ept_ub), fill = bu_green, alpha = 0.5) +\n  geom_line(aes(y = p), color = bu_green, linewidth = 1) +\n  geom_line(aes(y = delta_lb), color = bu_black, linetype = \"dashed\") +\n  geom_line(aes(y = delta_ub), color = bu_black, linetype = \"dashed\") +\n  labs(title = \"At-means interval, two standard errors\",\n       subtitle = \"shaded = ML standard error with end-point transformation; dashed = delta method\",\n       x = \"minimum dyad democracy (Polity)\", y = \"Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a09: The at-means interval two ways, full sample: ML standard error with end-point transformation, and the delta method.\n\n\n\n\n\nThey are indistinguishable. With 20,990 dyad-years the standard error of the linear prediction is small, and when the interval is narrow relative to the distance to the boundary the two routes agree to the third decimal. The difference shows up when the interval is wide \u2014 small samples, extreme scenarios. To see it, refit the same model on a random 500 of the dyad-years and rebuild the same grid:\n\n\ncode\nset.seed(20260610)\ndp_small &lt;- dpe %&gt;% slice_sample(n = 500)\nm_small  &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n                family = binomial(link = \"logit\"), data = dp_small)\n\npr_s  &lt;- predict(m_small, newdata = mem, type = \"link\", se.fit = TRUE)\nsmall &lt;- tibble(deml = mem$deml, fit = pr_s$fit, se = pr_s$se.fit) %&gt;%\n  mutate(p        = plogis(fit),\n         ept_lb   = plogis(fit - 1.96 * se),          ept_ub   = plogis(fit + 1.96 * se),\n         delta_lb = p - 1.96 * p * (1 - p) * se,      delta_ub = p + 1.96 * p * (1 - p) * se)\n\nggplot(small, aes(deml)) +\n  geom_ribbon(aes(ymin = ept_lb, ymax = ept_ub), fill = bu_green, alpha = 0.5) +\n  geom_line(aes(y = p), color = bu_green, linewidth = 1) +\n  geom_line(aes(y = delta_lb), color = bu_black, linetype = \"dashed\") +\n  geom_line(aes(y = delta_ub), color = bu_black, linetype = \"dashed\") +\n  geom_hline(yintercept = 0, color = bu_black, linewidth = 0.3) +\n  labs(title = \"The same two intervals, n = 500\",\n       subtitle = \"shaded = ML standard error with end-point transformation; dashed = delta method\",\n       x = \"minimum dyad democracy (Polity)\", y = \"Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a010: The same two intervals on a random 500 dyad-years.\n\n\n\n\n\nThat draw holds 26 disputes among its 500 dyad-years, so the standard errors are many times larger. Now the two intervals part company. The delta interval is symmetric around \\(p\\) on the probability scale, and from the middle of the grid on its lower bound crosses below zero \u2014 a negative probability. The end-point-transformed interval cannot do that: it is symmetric on the link scale, where nothing is bounded, and the logistic CDF maps both ends back inside \\((0,1)\\), squeezing the lower bound toward zero and stretching the upper bound. That asymmetry is not a defect; it is what an interval on a probability should look like near the boundary. This is why we build intervals on the link scale whenever the quantity is a monotone function of \\(x\\widehat\\beta\\), and reserve the delta method for the cases \u2014 averages, marginal effects, multinomial probabilities \u2014 where there is no single link scale to build on.\n\n\nStandard errors for a linear combination\nSometimes the quantity is a linear combination of coefficients, and its standard error needs the covariance between them. Polynomials are the common case (a polynomial is a variable interacted with itself):\n\\[\ny = \\widehat\\beta_0 + \\widehat\\beta_1 x_1 + \\widehat\\beta_2 x_1^2 + \\varepsilon.\n\\]\nThe marginal effect of \\(x_1\\) is not a single coefficient but \\(\\widehat\\beta_1 + 2\\widehat\\beta_2 x_1\\), whose standard error carries the covariance term:\n\\[\n\\sqrt{\\mathrm{Var}(\\widehat\\beta_1) + 4x_1^2\\,\\mathrm{Var}(\\widehat\\beta_2) + 4x_1\\,\\mathrm{Cov}(\\widehat\\beta_1, \\widehat\\beta_2)}.\n\\]\nThe covariances come straight from \\(\\mathbf{V} =\\) vcov(model).\nThe running model has no polynomial, so add one \u2014 democracy and its square. The sign of the marginal effect then depends on where we are in democracy, and so does its standard error.\n\n\ncode\nm3 &lt;- glm(dispute ~ border + deml + I(deml^2) + lncaprat + ally,\n          family = binomial(link = \"logit\"), data = dp)\n\nmodelsummary(\n  list(\"Logit, quadratic in democracy\" = m3),\n  coef_rename = c(\"border\" = \"Shared border\", \"deml\" = \"Democracy\",\n                  \"I(deml^2)\" = \"Democracy squared\",\n                  \"lncaprat\" = \"Capabilities ratio (log)\", \"ally\" = \"Allied\"),\n  statistic = \"std.error\",\n  gof_omit  = \"AIC|BIC|RMSE|F|Log\",\n  notes     = \"Standard errors in parentheses.\"\n)\n\n\n\n\n    \n\n    \n    \n      \n        \n        \n              \n                 \n                Logit, quadratic in democracy\n              \n        \n        Standard errors in parentheses.\n        \n                \n                  (Intercept)             \n                  -2.543 \n                \n                \n                                          \n                  (0.130)\n                \n                \n                  Shared border           \n                  1.065  \n                \n                \n                                          \n                  (0.088)\n                \n                \n                  Democracy               \n                  -0.079 \n                \n                \n                                          \n                  (0.008)\n                \n                \n                  Democracy squared       \n                  -0.009 \n                \n                \n                                          \n                  (0.002)\n                \n                \n                  Capabilities ratio (log)\n                  -0.225 \n                \n                \n                                          \n                  (0.024)\n                \n                \n                  Allied                  \n                  -0.840 \n                \n                \n                                          \n                  (0.081)\n                \n                \n                  Num.Obs.                \n                  20990  \n                \n        \n      \n    \n\n\n\nThe marginal effect of democracy on the linear predictor is \\(\\widehat\\beta_{d} + 2\\widehat\\beta_{d^2}\\,d\\). Here it is across the range of democracy, with its standard error built two ways \u2014 the written-out formula above, and the general delta form \\(\\sqrt{\\mathbf{g}'\\mathbf{V}\\mathbf{g}}\\) with gradient \\(\\mathbf{g} = (0,\\, 0,\\, 1,\\, 2d,\\, 0,\\, 0)\\), which is the same formula as a matrix product:\n\n\ncode\nb3 &lt;- coef(m3); V3 &lt;- vcov(m3)\nd  &lt;- seq(-10, 10, 1)\n\n# Marginal effect of democracy on x'b, and its SE from the written-out formula.\nme    &lt;- b3[\"deml\"] + 2 * b3[\"I(deml^2)\"] * d\nse_me &lt;- sqrt(V3[\"deml\", \"deml\"] + 4 * d^2 * V3[\"I(deml^2)\", \"I(deml^2)\"] +\n              4 * d * V3[\"deml\", \"I(deml^2)\"])\n\n# The same SE as sqrt(g'Vg): one gradient row per democracy value (21 x k).\nG &lt;- matrix(0, nrow = length(d), ncol = length(b3), dimnames = list(NULL, names(b3)))\nG[, \"deml\"]      &lt;- 1\nG[, \"I(deml^2)\"] &lt;- 2 * d\nse_gVg &lt;- sqrt(diag(G %*% V3 %*% t(G)))\n\ntibble(deml = d, me = me, se_formula = se_me, se_gVg = se_gVg) %&gt;%\n  filter(deml %in% c(-10, -5, 0, 5, 10))\n\n\n# A tibble: 5 x 4\n   deml       me se_formula  se_gVg\n  &lt;dbl&gt;    &lt;dbl&gt;      &lt;dbl&gt;   &lt;dbl&gt;\n1   -10  0.0965     0.0291  0.0291 \n2    -5  0.00889    0.0150  0.0150 \n3     0 -0.0787     0.00785 0.00785\n4     5 -0.166      0.0189  0.0189 \n5    10 -0.254      0.0333  0.0333 \n\n\n\n\ncode\ntibble(deml = d, me = me, lb = me - 1.96 * se_me, ub = me + 1.96 * se_me) %&gt;%\n  ggplot(aes(deml, me)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub), fill = bu_green, alpha = 0.5) +\n  geom_line(color = bu_green, linewidth = 1) +\n  geom_hline(yintercept = 0, color = bu_black, linewidth = 0.3) +\n  labs(title = \"Marginal effect of democracy on the linear predictor, quadratic model\",\n       x = \"minimum dyad democracy (Polity)\",\n       y = expression(hat(beta)[d] + 2 * hat(beta)[d^2] * d)) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a011: Marginal effect of democracy on the linear predictor in the quadratic model, with a delta-method 95% band.\n\n\n\n\n\nThe effect is positive among the least democratic dyads, crosses zero at a Polity score of about -4.5, and turns steadily more negative from there. The band is narrowest near the center of the data and widens toward the ends, because the \\(4d^2\\,\\mathrm{Var}(\\widehat\\beta_{d^2})\\) term grows with \\(d^2\\). And the covariance term is doing real work even though \\(\\mathrm{Cov}(\\widehat\\beta_d, \\widehat\\beta_{d^2})\\) is tiny on its own \u2014 it is multiplied by \\(4d\\). Two cautions. This is the effect on the linear predictor, on the logit scale; the effect on the probability multiplies it by \\(\\Lambda(1-\\Lambda)\\) at the scenario, and its standard error is the delta method again, one more layer of chain rule (marginaleffects::slopes() returns that version). And the interval crossing zero is the joint-hypothesis question the polynomial raises: the effect of democracy is not one number to test, it is a function of democracy.\n\n\nConfidence intervals \u2014 end-point transformation\nTo get an interval on a transformed quantity, build the bounds on the linear scale with either standard error, then transform:\n\\[\nF(X\\widehat\\beta - c\\cdot\\mathrm{se}) \\;\\le\\; F(X\\widehat\\beta) \\;\\le\\; F(X\\widehat\\beta + c\\cdot\\mathrm{se}),\n\\]\nwith \\(c\\) a critical value on the normal (\\(z = 1.96\\)). With ML standard errors the transformed bounds respect the distribution\u2019s limits \u2014 a probability interval stays in \\([0,1]\\), and the bounds are asymmetric, as a nonlinear model\u2019s should be. With delta standard errors the bounds are not guaranteed to obey those limits. (Figure\u00a02 is exactly this, built from se_lin.)"
  },
  {
    "objectID": "prediction26.html#simulation-methods",
    "href": "prediction26.html#simulation-methods",
    "title": "Prediction Methods for MLE Models",
    "section": "Simulation methods",
    "text": "Simulation methods\nTwo simulation routes to an interval. Both end the same way \u2014 a distribution of quantities of interest, summarized by its percentiles \u2014 and they differ in what gets resampled to build that distribution.\nSimulate the parameters \u2014 the King\u2013Tomz\u2013Wittenberg approach (King, Tomz, and Wittenberg 2000), used above. Estimate once. Draw a large sample of coefficient vectors from \\(N(\\widehat\\beta, \\mathbf{V})\\), generate the quantity of interest for each draw, and read percentiles off the result. The model is never re-estimated; the only inputs are \\(\\widehat\\beta\\) and \\(\\mathbf{V}\\), so the whole thing takes a second. What it assumes is that the sampling distribution of \\(\\widehat\\beta\\) really is that normal \u2014 the same assumption behind every \\(z\\)-statistic in the regression table. That is exactly Figure\u00a06 and Figure\u00a07; nothing new to build.\nBootstrap the data (Efron and Tibshirani 1993). Instead of resampling parameters, resample the data: draw a sample with replacement from the estimation sample, refit the model, recompute the quantity of interest, and repeat. The spread of the recomputed quantities is the uncertainty. Nothing is assumed about the shape of the sampling distribution; the resampling builds it empirically. The cost is a full re-estimation per draw \u2014 a thousand logits here, which is fine, and a thousand fits of something slow, which is not.\nIn political science the parameter-simulation approach is far more common \u2014 that is my experience of the published work, and of the software. The bootstrap is what we reach for when we doubt the normal approximation, or when the quantity of interest depends on the data in a way the parameters alone don\u2019t capture. Here is the bootstrap in full, for the predicted probability of a dispute at a representative profile \u2014 an explicit loop, refitting the logit each pass:\n\n\ncode\nset.seed(20260610)\nB &lt;- 1000\nprofile &lt;- tibble(border = 0, deml = 0, ally = 0,\n                  lncaprat = median(dp$lncaprat, na.rm = TRUE))\n\nboot_p &lt;- numeric(B)\nfor (b in 1:B) {\n  idx &lt;- sample(nrow(dpe), replace = TRUE)          # resample the estimation sample\n  mb  &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n             family = binomial(\"logit\"), data = dpe[idx, ])\n  boot_p[b] &lt;- predict(mb, newdata = profile, type = \"response\")\n}\n\nquantile(boot_p, c(.025, .5, .975))   # percentile 95% CI\n\n\n      2.5%        50%      97.5% \n0.02154761 0.02442032 0.02783351 \n\n\ncode\n# ---- MORE EFFICIENT -----------------------------------\n# replicate() runs the braced expression B times and collects the B results\n# into a vector, so the preallocated boot_p and the counter b go away:\n#   boot_p &lt;- replicate(B, {\n#     idx &lt;- sample(nrow(dpe), replace = TRUE)\n#     mb  &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n#                family = binomial(\"logit\"), data = dpe[idx, ])\n#     predict(mb, newdata = profile, type = \"response\")\n#   })\n#   quantile(boot_p, c(.025, .5, .975))\n\n\n(The same loop written compactly is replicate(B, { ... }); the explicit loop just makes each step visible.) Now put the two distributions side by side. The parameter-simulation version for the same profile takes one line, because the draws already exist:\n\n\ncode\n# Parameter-simulation draws of the same quantity, from the 1000 simL draws above.\nsim_p &lt;- plogis(simL$`(Intercept)` + simL$border * 0 + simL$deml * 0 +\n                simL$lncaprat * median(dp$lncaprat, na.rm = TRUE) + simL$ally * 0)\n\nbind_rows(tibble(p = boot_p, technique = \"bootstrap: resample the data, refit\"),\n          tibble(p = sim_p,  technique = \"parameter simulation: draw the coefficients\")) %&gt;%\n  ggplot(aes(p, fill = technique, color = technique)) +\n  geom_density(alpha = 0.3) +\n  scale_fill_manual(values = bu_two) +\n  scale_color_manual(values = bu_two) +\n  labs(title = \"Two distributions of the same quantity of interest\",\n       subtitle = \"Pr(dispute) at the representative profile, 1000 draws each\",\n       x = \"Pr(dispute)\", y = \"density\") +\n  theme_minimal() +\n  theme(legend.position = \"bottom\", legend.title = element_blank())\n\n\n\n\n\n\n\n\nFigure\u00a012: Bootstrap and parameter-simulation distributions of Pr(dispute) at one profile, 1000 draws each.\n\n\n\n\n\nThe two distributions sit on top of each other \u2014 they estimate the same uncertainty two ways. And since every technique is now in hand at this one profile, here are all four intervals for the same number:\n\n\ncode\npr_p  &lt;- predict(m1, newdata = profile, type = \"link\", se.fit = TRUE)\np_hat &lt;- plogis(pr_p$fit)\n\ntibble(technique = c(\"ML SE + end-point transformation\", \"delta method\",\n                     \"parameter simulation (KTW)\", \"bootstrap\"),\n       lower = c(plogis(pr_p$fit - 1.96 * pr_p$se.fit),\n                 p_hat - 1.96 * p_hat * (1 - p_hat) * pr_p$se.fit,\n                 quantile(sim_p,  .025),\n                 quantile(boot_p, .025)),\n       point = c(p_hat, p_hat, median(sim_p), median(boot_p)),\n       upper = c(plogis(pr_p$fit + 1.96 * pr_p$se.fit),\n                 p_hat + 1.96 * p_hat * (1 - p_hat) * pr_p$se.fit,\n                 quantile(sim_p,  .975),\n                 quantile(boot_p, .975))) %&gt;%\n  mutate(across(where(is.numeric), ~ round(.x, 4)))\n\n\n# A tibble: 4 x 4\n  technique                         lower  point  upper\n  &lt;chr&gt;                             &lt;dbl&gt;  &lt;dbl&gt;  &lt;dbl&gt;\n1 ML SE + end-point transformation 0.0215 0.0246 0.028 \n2 delta method                     0.0213 0.0246 0.0278\n3 parameter simulation (KTW)       0.0217 0.0247 0.0281\n4 bootstrap                        0.0215 0.0244 0.0278\n\n\nFour techniques, one quantity, one interval to the third decimal. With a large sample and a probability well away from the boundary, it does not matter how we bound it. The approach \u2014 which scenario we asked about \u2014 fixed the number; the technique only put error bars on it. Keep that picture for the cases where the techniques do disagree, because then the disagreement is telling you something: small \\(n\\), a boundary nearby, or a quantity the delta method approximates badly."
  },
  {
    "objectID": "prediction26.html#average-effects-two-ways",
    "href": "prediction26.html#average-effects-two-ways",
    "title": "Prediction Methods for MLE Models",
    "section": "Average effects, two ways",
    "text": "Average effects, two ways\nNow put the pieces together. Take the average-effects approach from above and compute its interval two ways: with a delta-method standard error, and by simulation. Both are standard errors of the average, which the averaged case-level intervals were not.\nI rebuild the average effect here first, so both techniques attach to the same object. The recipe is the one from the average-effects section: take the estimation sample, set democracy to a value for every case, set border to zero, predict every case, average. The one change is that I work with the design matrix \\(X\\) directly instead of a data frame and predict(), because the delta gradient and the simulation draws both want \\(X\\). As in the average-effects section, the live code is an explicit loop so every step is visible; a tidier version follows each chunk in comments.\n\n\ncode\n# Estimation-sample design matrix (n x k), coefficients, covariance matrix.\nX &lt;- model.matrix(m1)\nb &lt;- coef(m1); V &lt;- vcov(m1)\n\npolity  &lt;- seq(-10, 10, 1)\navg_mat &lt;- numeric(length(polity))         # average Pr(dispute) at each democracy value\n\nfor (i in seq_along(polity)) {\n  Xi &lt;- X                                  # counterfactual COPY of the design matrix\n  Xi[, \"deml\"]   &lt;- polity[i]              # democracy = this value, for every case\n  Xi[, \"border\"] &lt;- 0                      # no border, for every case\n  xb &lt;- as.vector(Xi %*% b)                # n linear predictions\n  pr &lt;- plogis(xb)                         # n predicted probabilities\n  avg_mat[i] &lt;- mean(pr)                   # their average\n}\nae_point &lt;- tibble(polity, avg = avg_mat)\n\n# ---- MORE EFFICIENT -----------------------------------\n# A function builds the counterfactual design matrix, and map_dfr replaces\n# the loop and the preallocated vector:\n#   cf_X &lt;- function(d) { Xi &lt;- X; Xi[, \"deml\"] &lt;- d; Xi[, \"border\"] &lt;- 0; Xi }\n#   ae_point &lt;- map_dfr(polity, function(d) {\n#     tibble(polity = d, avg = mean(plogis(as.vector(cf_X(d) %*% b))))\n#   })\n\n\nThat is the same average effect as in Figure\u00a03, recomputed here so the code sits next to the two techniques that build on it. Now attach uncertainty to it.\nDelta. Start with what the average effect is, at one democracy value:\n\\[\n\\bar P = \\frac{1}{n}\\sum_{i=1}^{n} \\Lambda(x_i\\widehat\\beta),\n\\]\nwhere \\(\\Lambda\\) is the logistic CDF and \\(x_i\\) is case \\(i\\)\u2019s counterfactual row of covariates. The covariate rows are fixed numbers. The only estimated thing in \\(\\bar P\\) is \\(\\widehat\\beta\\), so \\(\\bar P\\) is a function of \\(\\widehat\\beta\\) and nothing else \u2014 and a function of an estimate has a sampling variance, just as the estimate does. The delta method gives it. For any function \\(h(\\widehat\\beta)\\),\n\\[\n\\mathrm{Var}\\big[h(\\widehat\\beta)\\big] \\approx g'\\,\\mathbf{V}\\,g, \\qquad g = \\frac{\\partial h}{\\partial \\widehat\\beta},\n\\]\nwhere \\(g\\) is the gradient, a \\(k \\times 1\\) vector with one entry per coefficient, and \\(\\mathbf{V}\\) is the covariance matrix of \\(\\widehat\\beta\\). So the job is to differentiate the average with respect to the coefficients. Take one case\u2019s term first. By the chain rule, the derivative of \\(\\Lambda(x_i\\widehat\\beta)\\) with respect to \\(\\widehat\\beta\\) is the logistic density at that case, times the derivative of \\(x_i\\widehat\\beta\\) with respect to \\(\\widehat\\beta\\), which is just \\(x_i\\):\n\\[\n\\frac{\\partial \\Lambda(x_i\\widehat\\beta)}{\\partial \\widehat\\beta} = \\Lambda_i(1-\\Lambda_i)\\, x_i .\n\\]\nThat is the same weight that turns a coefficient into a marginal effect at the top of these slides \u2014 the logit density \\(\\Lambda(1-\\Lambda)\\) \u2014 now multiplying the whole covariate row instead of one coefficient. The derivative of an average is the average of the derivatives, so\n\\[\ng = \\frac{1}{n}\\sum_{i=1}^{n} \\Lambda_i(1-\\Lambda_i)\\, x_i ,\n\\]\neach case\u2019s weight times its row, averaged over the sample. Then\n\\[\n\\mathrm{se}(\\bar P) = \\sqrt{g'\\,\\mathbf{V}\\,g},\n\\]\nand the interval is \\(\\bar P \\pm 1.96\\,\\mathrm{se}(\\bar P)\\). In code, that is the rebuild loop with three lines added:\n\n\ncode\navg_d &lt;- numeric(length(polity))\nlb_d  &lt;- numeric(length(polity))\nub_d  &lt;- numeric(length(polity))\n\nfor (i in seq_along(polity)) {\n  Xi &lt;- X                                        # counterfactual design matrix, as above\n  Xi[, \"deml\"]   &lt;- polity[i]\n  Xi[, \"border\"] &lt;- 0\n  pr   &lt;- plogis(as.vector(Xi %*% b))            # n predicted probabilities\n  Pbar &lt;- mean(pr)                               # the average effect\n  w    &lt;- pr * (1 - pr)                          # logit weight for each case, n x 1\n  g    &lt;- colMeans(w * Xi)                       # each row of Xi scaled by its weight, then averaged: k x 1\n  se   &lt;- sqrt(as.numeric(t(g) %*% V %*% g))     # sqrt(g' V g)\n  avg_d[i] &lt;- Pbar\n  lb_d[i]  &lt;- Pbar - 1.96 * se\n  ub_d[i]  &lt;- Pbar + 1.96 * se\n}\nae_delta &lt;- tibble(polity, avg = avg_d, lb = lb_d, ub = ub_d)\n\n# ---- MORE EFFICIENT -----------------------------------\n# Same computation with cf_X() and map_dfr; each call returns one row.\n#   ae_delta &lt;- map_dfr(polity, function(d) {\n#     Xi   &lt;- cf_X(d)\n#     pr   &lt;- plogis(as.vector(Xi %*% b))\n#     Pbar &lt;- mean(pr)\n#     g    &lt;- colMeans(pr * (1 - pr) * Xi)\n#     se   &lt;- sqrt(as.numeric(t(g) %*% V %*% g))\n#     tibble(polity = d, avg = Pbar, lb = Pbar - 1.96 * se, ub = Pbar + 1.96 * se)\n#   })\n\n\nWalk through the three new lines. w is the logistic density at each case\u2019s linear prediction, \\(\\Lambda_i(1-\\Lambda_i)\\), so it is a vector of \\(n\\) weights. w * Xi multiplies each case\u2019s row of covariates by that case\u2019s weight \u2014 R recycles the weight down each column, so row \\(i\\) of the result is \\(\\Lambda_i(1-\\Lambda_i)\\,x_i\\) \u2014 and colMeans averages those \\(n\\) rows into one row of length \\(k\\). That row is the gradient \\(g\\). The last line is the quadratic form \\(g'\\mathbf{V}g\\) and its square root.\nSimulation. Draw coefficient vectors from \\(N(\\widehat\\beta, \\mathbf{V})\\); for each draw, compute the average effect exactly as in the rebuild; take percentiles of the 1000 averages. No derivative, and the bounds stay inside \\([0,1]\\) because every one of the 1000 averages does:\n\n\ncode\nsims &lt;- MASS::mvrnorm(1000, b, V)                # 1000 x k coefficient draws\n\navg_s &lt;- numeric(length(polity))\nlb_s  &lt;- numeric(length(polity))\nub_s  &lt;- numeric(length(polity))\n\nfor (i in seq_along(polity)) {\n  Xi &lt;- X                                        # counterfactual design matrix, as above\n  Xi[, \"deml\"]   &lt;- polity[i]\n  Xi[, \"border\"] &lt;- 0\n  XB    &lt;- Xi %*% t(sims)                        # n x 1000: column j = every case's x'b under draw j\n  Pdraw &lt;- colMeans(plogis(XB))                  # 1000 average effects, one per draw\n  q     &lt;- quantile(Pdraw, c(.025, .5, .975))\n  avg_s[i] &lt;- q[2]\n  lb_s[i]  &lt;- q[1]\n  ub_s[i]  &lt;- q[3]\n}\nae_sim &lt;- tibble(polity, avg = avg_s, lb = lb_s, ub = ub_s)\n\n# ---- MORE EFFICIENT -----------------------------------\n#   ae_sim &lt;- map_dfr(polity, function(d) {\n#     q &lt;- quantile(colMeans(plogis(cf_X(d) %*% t(sims))), c(.025, .5, .975))\n#     tibble(polity = d, avg = q[2], lb = q[1], ub = q[3])\n#   })\n\n\nXB is \\(n \\times 1000\\): column \\(j\\) holds every case\u2019s linear prediction under draw \\(j\\). Transform, take column means, and each column collapses to one average effect. That is the rebuild loop run a thousand times at once, once per draw.\n\n\ncode\nbind_rows(mutate(ae_delta, method = \"delta\"),\n          mutate(ae_sim,   method = \"simulation\")) %&gt;%\n  ggplot(aes(polity, avg)) +\n  geom_ribbon(data = noborder, aes(x = polity, ymin = lb, ymax = ub), inherit.aes = FALSE,\n              fill = NA, color = bu_black, linetype = \"dotted\") +\n  geom_ribbon(aes(ymin = lb, ymax = ub, fill = method), alpha = 0.5) +\n  geom_line(aes(color = method), linewidth = 1) +\n  scale_fill_manual(values = bu_two) +\n  scale_color_manual(values = bu_two) +\n  labs(title = \"Average effects, two uncertainty techniques\",\n       subtitle = \"dotted = the averaged case-level intervals from the average-effects section\",\n       x = \"Polity score\", y = \"average Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a013: Average effects with delta-method and simulated intervals; the dotted lines are the averaged case-level intervals.\n\n\n\n\n\nThe two bands sit almost on top of each other. The approach (average effects) fixed what we computed; the technique (delta or simulation) changed only how we bounded it, and here the two agree. The dotted lines are the averaged case-level intervals from the average-effects section, and they sit outside both. That band measured how uncertain a typical case is; these two measure how uncertain the average is. Same curve, three bands, and only two of them are standard errors of the average.\n\nWhen the two techniques disagree\nThe delta interval is a first-order approximation. It treats the average as linear in \\(\\widehat\\beta\\) near the estimate, so it is symmetric by construction and it does not know that a probability stops at zero. The simulated interval pushes every draw through the link, so it carries the curvature and the boundary, and it can be lopsided. With 20,990 cases the standard errors are small, the linear approximation is excellent, and the two coincide. They part company when the standard errors are large relative to the distance to the boundary: small samples, rare events, extreme scenarios. The 500-case refit from Figure\u00a010 shows it \u2014 same approach, same two techniques, less data:\n\n\ncode\nX_s &lt;- model.matrix(m_small); b_s &lt;- coef(m_small); V_s &lt;- vcov(m_small)\nsims_s &lt;- MASS::mvrnorm(1000, b_s, V_s)\n\nae_small &lt;- tibble()\nfor (i in seq_along(polity)) {\n  Xi &lt;- X_s                                      # counterfactual design matrix, n = 500\n  Xi[, \"deml\"]   &lt;- polity[i]\n  Xi[, \"border\"] &lt;- 0\n  pr   &lt;- plogis(as.vector(Xi %*% b_s))\n  Pbar &lt;- mean(pr)\n  g    &lt;- colMeans(pr * (1 - pr) * Xi)\n  se   &lt;- sqrt(as.numeric(t(g) %*% V_s %*% g))                       # delta\n  q    &lt;- quantile(colMeans(plogis(Xi %*% t(sims_s))), c(.025, .5, .975))   # simulation\n  ae_small &lt;- bind_rows(ae_small,\n    tibble(polity = polity[i], method = \"delta\",\n           avg = Pbar, lb = Pbar - 1.96 * se, ub = Pbar + 1.96 * se),\n    tibble(polity = polity[i], method = \"simulation\",\n           avg = q[2], lb = q[1], ub = q[3]))\n}\n\nggplot(ae_small, aes(polity, avg)) +\n  geom_ribbon(aes(ymin = lb, ymax = ub, fill = method), alpha = 0.5) +\n  geom_line(aes(color = method), linewidth = 1) +\n  geom_hline(yintercept = 0, color = bu_black, linewidth = 0.3) +\n  scale_fill_manual(values = bu_two) +\n  scale_color_manual(values = bu_two) +\n  labs(title = \"Average effects, two techniques, n = 500\",\n       x = \"Polity score\", y = \"average Pr(dispute)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a014: Average effects with delta-method and simulated intervals on the 500-case refit.\n\n\n\n\n\nNow the delta band crosses zero from the middle of the grid on, and the simulated band is lopsided: a short tail toward zero and a long one upward, which is what the sampling distribution of a small probability looks like. The two techniques agree on the estimate; they disagree about its shape, and the delta method is the one approximating. When they disagree, the simulation is the better guide. And if the sample is small enough that you doubt the normal in \\(N(\\widehat\\beta, \\mathbf{V})\\) as well, that is the case for the bootstrap."
  },
  {
    "objectID": "prediction26.html#at-means-or-average-effects",
    "href": "prediction26.html#at-means-or-average-effects",
    "title": "Prediction Methods for MLE Models",
    "section": "At-means or average effects?",
    "text": "At-means or average effects?\nAt-means predictions are easier to write and easier to describe. One row, one sentence of assumptions: \u201ca non-allied, non-contiguous dyad at median capabilities.\u201d That is also their problem. The row is a case we made up, and it may not exist in the data. A dyad sitting at the median of every covariate at once is nobody in particular, and if we hold a dummy at its mean instead of its mode we get something worse: a dyad that is 35% allied, which describes no dyad at all. The choice of \u201ccentral\u201d values is ours, and the number moves when we change it. The at-means curve in Figure\u00a04 sat above the average-effects curve for exactly this reason \u2014 we picked a non-allied dyad, and non-allied dyads are riskier than the sample.\nFixed effects make this acute. With unit dummies in the model, an at-means prediction has to hold the fixed effects at something: the reference unit, which is an arbitrary choice, or their mean, which is a unit that does not exist. Either way the prediction describes no observed case. Average effects sidestep this entirely. Every case keeps its own fixed effect, its own covariates, and its own place in the data; only the variable of interest is moved. The counterfactual is \u201cwhat if every dyad-year in the data had democracy equal to \\(d\\),\u201d which is a question about the real cases, and the answer is an average over them.\nSo: average effects by default. They represent the data rather than a constructed case, they let the data speak for themselves in the real cases, and they are the right object in fixed-effects models and any other model carrying many nuisance parameters. They are also what marginaleffects::avg_predictions() and avg_slopes() compute. Reach for at-means when the single case is the point \u2014 a specific, substantively meaningful profile you want to describe; an illustrative scenario for a figure; a comparison of a few named cases, as in Figure\u00a08. When you do, say what the case is, in words, in the figure note. The two approaches answer different questions, and the reader needs to know which one was asked."
  },
  {
    "objectID": "prediction26.html#analytic-or-simulated-uncertainty",
    "href": "prediction26.html#analytic-or-simulated-uncertainty",
    "title": "Prediction Methods for MLE Models",
    "section": "Analytic or simulated uncertainty?",
    "text": "Analytic or simulated uncertainty?\nBoth rest on the same foundation: \\(\\widehat\\beta \\sim N(\\beta, \\mathbf{V})\\) in large samples. Neither is \u201cmore correct\u201d about that. They differ in what they do with it.\nThe analytic route \u2014 an ML standard error with end-point transformation, or the delta method \u2014 is closed-form, instant, and deterministic: the same number every run, and the number predict() and marginaleffects return by default. Its weakness is the delta method\u2019s, which is that it approximates a nonlinear quantity of interest by a line through the estimate and returns a symmetric interval. Simulation takes no derivative. It pushes every draw through the actual link and the actual computation, so it carries the curvature and the boundary, and it works the same way for any quantity of interest you can compute \u2014 a difference of two averages, a ratio of two probabilities, an expected count at one scenario minus another. Its costs are randomness (set the seed and report the number of draws) and a little computing time, which for anything we do in this course is negligible.\nWhen the two agree \u2014 a large sample, a quantity well away from its boundary \u2014 choose on convenience, which means analytic. When the quantity of interest is a nonlinear function of several parameters, or the sample is small, or the quantity sits near a boundary, simulate. A practical rule: if you can build the interval on the link scale and transform the end points, do that; if you cannot and \\(n\\) is large, delta; if in doubt, simulate. Simulation never does worse than the delta method on the shape of the interval, and it is what readers in our field expect to see. Bootstrap when you doubt the normal for \\(\\widehat\\beta\\) itself \u2014 separation, a handful of events, or clustered data where the cluster is the right unit to resample."
  },
  {
    "objectID": "prediction26.html#if-analytic-ml-or-delta",
    "href": "prediction26.html#if-analytic-ml-or-delta",
    "title": "Prediction Methods for MLE Models",
    "section": "If analytic, ML or delta?",
    "text": "If analytic, ML or delta?\nThe ML standard error is the standard error of \\(x\\widehat\\beta\\) on the link scale. Use it, with end-point transformation, whenever the quantity of interest is a monotone transformation of a single linear prediction: a predicted probability, an expected count, a hazard at one scenario. It is exact for the linear predictor, and the transformed interval respects the bounds and is asymmetric the way an interval on a probability should be.\nUse the delta method when the quantity is not one monotone function of one linear prediction, because then there is no link scale to build on. That covers an average of predictions across cases (the average effect: \\(n\\) linear predictions, transformed, then averaged); a marginal effect (a density times a coefficient); a first difference between two scenarios (one transformed value minus another); and multinomial probabilities (a ratio involving several linear predictors). In each of these the delta method gives an approximate standard error on the quantity\u2019s own scale, symmetric, and reliable as long as the interval is narrow relative to the distance to any boundary. Figure\u00a014 is what it looks like when that condition fails."
  },
  {
    "objectID": "prediction26.html#the-short-version",
    "href": "prediction26.html#the-short-version",
    "title": "Prediction Methods for MLE Models",
    "section": "The short version",
    "text": "The short version\n\nAverage effects unless a specific case is the point \u2014 and always with fixed effects.\nOne scenario, monotone link: ML standard error on the link scale, end points transformed.\nAverages, marginal effects, first differences: delta if \\(n\\) is large and the quantity is away from its boundary; otherwise simulate.\nSimulation is right wherever delta is right and better where it is not, and it is what the discipline expects; report the seed and the number of draws.\nBootstrap when you doubt \\(N(\\widehat\\beta, \\mathbf{V})\\) itself.\nWhatever you choose, say which approach and which technique in the figure note."
  },
  {
    "objectID": "binarymodels26.html",
    "href": "binarymodels26.html",
    "title": "Binary Response Models",
    "section": "",
    "text": "How can we model a binary \\(y\\) variable?\nDoes OLS (the linear probability model) work sufficiently well?\nHow can we build a maximum likelihood model that does work?\n\n\n\n\nBefore we get into the details, here is where we are going. Every limited-\\(y\\) model in this course \u2014 binary, count, ordered, duration \u2014 is built the same way, following the same short recipe:\n\n\n\n\n\n\nBuilding a maximum likelihood model\n\n\n\n\nObserve \\(y\\); consider its distribution; write down its PDF (or PMF).1\nWrite the joint probability of the data under that distribution.\nRewrite the joint probability as a likelihood.\nSimplify \u2014 take logs.\nParameterize \u2014 make the distribution\u2019s parameter a function of covariates, \\(x\\beta\\) (the systematic component).\nChoose a link function \\(F\\) that connects \\(x\\beta\\) to that parameter and respects its bounds.\n\n\n\nThat is the whole process - we\u2019ll follow this for much of the semester. The rest of these notes walk through these steps for the binary case: I motivate why we leave OLS behind, build the likelihood step by step, estimate it, and then evaluate the estimates."
  },
  {
    "objectID": "binarymodels26.html#questions",
    "href": "binarymodels26.html#questions",
    "title": "Binary Response Models",
    "section": "",
    "text": "How can we model a binary \\(y\\) variable?\nDoes OLS (the linear probability model) work sufficiently well?\nHow can we build a maximum likelihood model that does work?"
  },
  {
    "objectID": "binarymodels26.html#the-process",
    "href": "binarymodels26.html#the-process",
    "title": "Binary Response Models",
    "section": "",
    "text": "Before we get into the details, here is where we are going. Every limited-\\(y\\) model in this course \u2014 binary, count, ordered, duration \u2014 is built the same way, following the same short recipe:\n\n\n\n\n\n\nBuilding a maximum likelihood model\n\n\n\n\nObserve \\(y\\); consider its distribution; write down its PDF (or PMF).1\nWrite the joint probability of the data under that distribution.\nRewrite the joint probability as a likelihood.\nSimplify \u2014 take logs.\nParameterize \u2014 make the distribution\u2019s parameter a function of covariates, \\(x\\beta\\) (the systematic component).\nChoose a link function \\(F\\) that connects \\(x\\beta\\) to that parameter and respects its bounds.\n\n\n\nThat is the whole process - we\u2019ll follow this for much of the semester. The rest of these notes walk through these steps for the binary case: I motivate why we leave OLS behind, build the likelihood step by step, estimate it, and then evaluate the estimates."
  },
  {
    "objectID": "binarymodels26.html#example-democratic-peace-data",
    "href": "binarymodels26.html#example-democratic-peace-data",
    "title": "Binary Response Models",
    "section": "Example \u2014 Democratic Peace data",
    "text": "Example \u2014 Democratic Peace data\nAs a running example we\u2019ll consider the Democratic Peace data from Oneal and Russett (1997) (ISQ). The unit is the dyad-year, \\(y\\) is the presence of a militarized dispute, and key covariates include shared border, the lower of the two dyad Polity scores, the capabilities ratio, and an alliance indicator. The substantive expectation: as the minimum democracy score in the dyad rises, the probability of a MID should fall.\n\nPredictions out of bounds\n\n\ncode\n# I fit the two binary models and OLS on the same specification, to compare\n# what each one predicts.\nm_logit &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n               family = binomial(link = \"logit\"), data = dp)\nm_probit &lt;- glm(dispute ~ border + deml + lncaprat + ally,\n                family = binomial(link = \"probit\"), data = dp)\nm_ols   &lt;- lm(dispute ~ border + deml + lncaprat + ally, data = dp)\n\npreds &lt;- tibble(\n  logit   = predict(m_logit, type = \"response\"),\n  ols     = predict(m_ols),\n  dispute = factor(dp$dispute)\n)\n\nn_oob &lt;- sum(preds$ols &lt; 0)   # OLS predictions below the probability floor\n\nggplot(preds, aes(x = logit, y = ols, color = dispute)) +\n  geom_point(alpha = 0.5) +\n  geom_hline(yintercept = 0) +\n  annotate(\"text\", x = 0.11, y = -0.11,\n           label = paste(format(n_oob, big.mark = \",\"), \"OLS predictions below 0\"),\n           color = \"red\", size = 3.5) +\n  scale_color_manual(values = bu_two) +\n  labs(title = \"Predictions from logit and OLS\",\n       x = \"logit prediction (probability)\",\n       y = \"OLS prediction\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a01\n\n\n\n\n\nAbout 12% of the OLS predictions fall below zero. None exceed one here, but only because disputes are rare and the fitted values stay low; with a more common outcome we\u2019d see predictions above one as well. Either way, a number outside \\([0,1]\\) is not a probability, whatever else it is.\n\n\nHeteroskedastic, non-normal residuals\n\n\ncode\ntibble(resid = residuals(m_ols), dispute = factor(dp$dispute)) %&gt;%\n  ggplot(aes(x = resid, color = dispute)) +\n  geom_density() +\n  geom_vline(xintercept = 0, linetype = \"dashed\") +\n  scale_color_manual(values = bu_two) +\n  labs(title = \"Density of OLS residuals\", x = \"residual\", y = \"density\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\nThe residuals bunch around the values that produce \\(\\{0,1\\}\\) outcomes from a linear fit. In fact the distribution looks more binomial than normal \u2014 clustered near zero and one \u2014 which shouldn\u2019t surprise us, since \\(y\\) only ever takes the values zero and one and the residuals are just \\(u = y - \\widehat{y}\\). So they aren\u2019t going to be normal, and they aren\u2019t going to be homoskedastic."
  },
  {
    "objectID": "binarymodels26.html#so-when-is-the-lpm-appropriate",
    "href": "binarymodels26.html#so-when-is-the-lpm-appropriate",
    "title": "Binary Response Models",
    "section": "So when is the LPM appropriate?",
    "text": "So when is the LPM appropriate?\nThe best answer is never.\n\nThere seems to be a mild trend in the discipline to rehabilitate the LPM, though it\u2019s not clear why \u2014 it\u2019s hard to find statements about the advantages of doing so in any particular setting, or about the disadvantages of estimating a logit or probit that would lead us to prefer the LPM.\n\nOLS is a rockin\u2019 estimator, but it\u2019s just not well suited to limited \\(y\\) variables. Efforts to rehabilitate the LPM are like putting lipstick on a pig."
  },
  {
    "objectID": "binarymodels26.html#examples-of-limited-dependent-variables",
    "href": "binarymodels26.html#examples-of-limited-dependent-variables",
    "title": "Binary Response Models",
    "section": "Examples of limited dependent variables",
    "text": "Examples of limited dependent variables\nBinary \\(y\\) is just one kind of limited variable. The same logic \u2014 build the model around the distribution of what we actually observe \u2014 carries through the whole course. A few of the types we\u2019ll meet:\n\nbinary variables: 0 = peace, 1 = war; 0 = didn\u2019t vote, 1 = voted.\nunordered (nominal) categories: which car you buy \u2014 Honda, Toyota, Ford, Buick; vote choice across several candidates; policy options.\nordered variables with few values: many survey responses (disagree / neutral / agree).\ncounts: the number of militarized disputes a dyad has in a year; episodes of torture in a country-year; arrests; doctor visits \u2014 \\(0, 1, 2, 3, \\ldots\\)\ntime to an event: how long a civil war lasts; how long a leader survives in office; time until a patient relapses.\n\nEach gets its own distribution and its own link, but the recipe is always the same \u2014 which is the whole point of building these models from the distribution up."
  },
  {
    "objectID": "binarymodels26.html#the-latent-variable-motivation",
    "href": "binarymodels26.html#the-latent-variable-motivation",
    "title": "Binary Response Models",
    "section": "The latent variable motivation",
    "text": "The latent variable motivation\nImagine a latent continuous variable \\(y^*\\) that lives on the real line:\n\\[\ny_i^* = x_i\\beta + \\varepsilon_i\n\\]\nWe don\u2019t observe \\(y^*\\) directly (if we did, we\u2019d probably be using OLS). We observe a thresholded version of it:\n\\[\ny_i = \\begin{cases} 1 & \\text{if } y_i^* &gt; 0 \\\\ 0 & \\text{if } y_i^* \\leq 0 \\end{cases}\n\\]\nThe probability of observing \\(y_i = 1\\) is the probability that \\(y_i^*\\) exceeds the threshold:\n\\[\n\\begin{aligned}\nPr(y_i = 1 \\mid x_i)\n   &= Pr(y_i^* &gt; 0)                  && \\text{(threshold)} \\\\\n   &= Pr(x_i\\beta+\\varepsilon_i &gt; 0)                  && \\text{(threshold restated)} \\\\\n   &= Pr(\\varepsilon_i &gt; -x_i\\beta)  && \\text{(isolate the error)} \\\\\n   &= 1 - F_\\varepsilon(-x_i\\beta)   && \\text{(right tail)} \\\\\n   &= F_\\varepsilon(x_i\\beta)        && \\text{(symmetry)}\n\\end{aligned}\n\\]\nTake the equalities in turn:\n\n(threshold) \u2014 the step to \\(Pr(y_i^* &gt; 0)\\) is the measurement step: we record a one exactly when the latent \\(y_i^*\\) clears the threshold, so \\(Pr(y_i=1)\\) just is \\(Pr(y_i^* &gt; 0)\\).\n(isolate the error) \u2014 the step to \\(Pr(\\varepsilon_i &gt; -x_i\\beta)\\) substitutes \\(y_i^* = x_i\\beta + \\varepsilon_i\\) and isolates the only random piece, \\(\\varepsilon_i\\): given \\(x_i\\), the systematic part \\(x_i\\beta\\) is fixed, so a \u201cone\u201d requires the unobserved \\(\\varepsilon_i\\) to exceed \\(-x_i\\beta\\); the observed factors set a bar, and the error has to clear it. This is the substantive line, and the same object a random-utility model produces (see the note below).\n(right tail) \u2014 the step to \\(1 - F_\\varepsilon(-x_i\\beta)\\) is where the CDF enters, and with it something we can actually compute. A greater-than probability on \\(\\varepsilon_i\\) is an upper tail, and an upper tail is one minus the CDF: \\(Pr(\\varepsilon_i &gt; c) = 1 - F_\\varepsilon(c)\\), here with \\(c = -x_i\\beta\\). Up to now we had an event \u2014 the error clearing the bar; this rewrites it as a function of \\(x_i\\beta\\), namely \\(F_\\varepsilon\\) itself. That function is the link, and it is what we parameterize and drop into the likelihood to estimate. No \\(F\\), no estimator.\n(symmetry) \u2014 the step to \\(F_\\varepsilon(x_i\\beta)\\) uses the symmetry of \\(F_\\varepsilon\\) about zero such that \\(1 - F_\\varepsilon(-a) = F_\\varepsilon(a)\\). What it buys is the tidy, estimable form \\(Pr(y_i=1) = F_\\varepsilon(x_i\\beta)\\): the CDF read at the linear predictor directly, increasing in \\(x_i\\beta\\) (more of the linear prediction \\(x_i\\beta\\), more probability) and evaluated at the same \\(x_i\\beta\\) OLS would use. That is the form we parameterize and fit. Both the normal and the logistic are symmetric, so both collapse to it; an asymmetric error (the Gumbel, hence complementary log-log) does not, and keeps the \\(1 - F_\\varepsilon(-x_i\\beta)\\) shape.\n\n\n\n\n\n\n\nThe random-utility motivation\n\n\n\nThe latent variable has a behavioral interpretation you have already encountered in formal theory. Let \\(y_i^*\\) be the net utility of one alternative over the other \u2014 the utility of option 1 minus the utility of option 0. The labels 0 and 1 are arbitrary: the two options could be two candidates, two commuting modes, two ice cream flavors. Split it the same way: \\(x_i\\beta\\) is the systematic (representative) utility difference \u2014 the part explained by observed characteristics \u2014 and \\(\\varepsilon_i\\) is the idiosyncratic part, the unobserved, agent-specific taste shock. An agent picks option 1 when its net utility is positive, \\(y_i^* &gt; 0\\), which is exactly \\(\\varepsilon_i &gt; -x_i\\beta\\): the individual taste shock has to be large enough to overcome whatever the observed factors imply. So the middle term of the derivation is a choice rule, and the binary model falls straight out of the utility theory \u2014 a direct line from the behavioral model to the estimator, rather than a latent variable posited from nowhere.\nTwo things follow. First, the distribution we assume for \\(\\varepsilon_i\\) is a model of the unobserved utility: assume those shocks are logistic and we get the logit; assume normal, we get the probit. Choosing the link is choosing how unmodeled tastes are distributed. Second, nothing here needs there to be only two options. Give each alternative \\(j\\) its own utility \\(U_{ij} = x_i\\beta_j + \\varepsilon_{ij}\\) and let the agent, \\(i\\), choose the one with the highest utility, and the same machinery covers a whole menu of options \u2014 candidates in an election, brands on a shelf, foreign policy options. The binary model is just the two-option case of that; we\u2019ll build the general version \u2014 several alternatives, McFadden\u2019s derivation, and what the error assumption buys and costs \u2014 in the choice model weeks later in the semester.\n\n\nThis is what makes the link function not arbitrary: \\(F\\) is the CDF of the latent error. Different assumptions about \\(\\varepsilon\\) give different link functions.\n\n\\(\\varepsilon \\sim N(0,1)\\) \\(\\longrightarrow\\) probit, \\(F = \\Phi\\)\n\\(\\varepsilon \\sim \\text{Logistic}(0,1)\\) \\(\\longrightarrow\\) logit, \\(F = \\Lambda\\)\n\\(\\varepsilon \\sim \\text{Gumbel}\\) \\(\\longrightarrow\\) complementary log-log\n\nFigure\u00a02 illustrates this for a normal (probit) error. Think of the latent variable \\(y^* = x\\beta + \\varepsilon\\) as a distribution sitting over the real line, centered at \\(x\\beta\\), with the threshold fixed at zero; \\(Pr(y=1)\\) is the share of it lying to the right of zero. Raising \\(x\\beta\\) slides the whole distribution rightward, so more of it clears the threshold and \\(Pr(y=1)\\) rises; lowering \\(x\\beta\\) slides it left. Imagine \\(x\\beta\\) at values ranging across the x-axis, and that share accumulates from zero up to one \u2014 which is exactly what a CDF does, and why \\(F\\) is one. So the link is no arbitrary sigmoid: choosing \\(F\\) is choosing a distribution for the error.2\n\n\ncode\n# TOP: the latent bell y* = xb + e ~ N(xb, 1); the shaded area past the fixed\n# threshold at 0 is Pr(y=1). That area = 1 - Phi(-xb) = Phi(xb) (standardize --\n# the threshold sits -xb SDs from the mean). BOTTOM: plot each shaded area\n# against xb and it traces the CDF Phi, so the shaded area up top IS the CDF\n# height below. Drawn for a normal error (probit); logit tells the same story.\nxb_vals &lt;- c(-1.5, 0, 1.5)\nstrip   &lt;- sprintf(\"xb = %g   (shaded area = %.2f)\", xb_vals, pnorm(xb_vals))\n\ndens_df &lt;- tibble(xb    = rep(xb_vals, each = 400),\n                  ystar = rep(seq(-5, 5, length.out = 400), times = 3)) %&gt;%\n  mutate(dens  = dnorm(ystar, mean = xb, sd = 1),\n         facet = factor(sprintf(\"xb = %g   (shaded area = %.2f)\", xb, pnorm(xb)),\n                        levels = strip))\nshaded &lt;- filter(dens_df, ystar &gt; 0)\n\n# Top row: the three bells, shaded area past the threshold = Pr(y=1) = Phi(xb).\np_bells &lt;- ggplot(dens_df, aes(ystar, dens)) +\n  geom_area(data = shaded, fill = bu_lightgreen, alpha = 0.7) +\n  geom_line(color = bu_green, linewidth = 0.7) +\n  geom_vline(xintercept = 0, linetype = \"dashed\") +           # the fixed threshold\n  facet_wrap(~ facet, nrow = 1) +\n  labs(title = \"Top: the shaded area past the threshold is Pr(y=1) = Phi(xb)\",\n       x = expression(y^\"*\"~\"(latent variable)\"), y = NULL) +\n  theme_minimal()\n\n# Bottom row: those areas plotted against xb -- this traces the CDF, the link.\ncdf_df &lt;- tibble(xb = seq(-5, 5, length.out = 400), p = pnorm(xb))\nmarks  &lt;- tibble(xb = xb_vals, p = pnorm(xb_vals))\n\np_cdf &lt;- ggplot(cdf_df, aes(xb, p)) +\n  geom_line(color = bu_green, linewidth = 1) +\n  geom_segment(data = marks, aes(x = xb, xend = xb, y = 0, yend = p),\n               color = bu_grey, linetype = \"dotted\") +        # read up from xb\n  geom_segment(data = marks, aes(x = -5, xend = xb, y = p, yend = p),\n               color = bu_grey, linetype = \"dotted\") +        # ... across to the height\n  geom_point(data = marks, aes(xb, p), color = bu_black, size = 2) +\n  geom_text(data = marks, aes(xb, p, label = sprintf(\"(%g, %.2f)\", xb, p)),\n            vjust = -0.9, hjust = -0.05, size = 3.1, color = bu_black) +\n  labs(title = \"Bottom: plot each shaded area against xb and you trace the CDF -- the link F\",\n       x = expression(x*beta), y = expression(Phi(x*beta))) +\n  theme_minimal()\n\np_bells / p_cdf\n\n\n\n\n\n\n\n\nFigure\u00a02\n\n\n\n\n\nLook at the two panels together. Each curve\u2019s shaded area is a single number \u2014 the probability past the threshold \u2014 and computing it is one standardization: the bell is \\(N(x\\beta, 1)\\), so the threshold at \\(0\\) sits \\(-x\\beta\\) standard deviations from the mean, and the area to its right is \\(1 - \\Phi(-x\\beta) = \\Phi(x\\beta)\\). That is the whole translation onto the CDF. Take the lefthand bell-curve, \\(x\\beta = -1.5\\): the threshold is \\(1.5\\) standard deviations above the mean, so only the far tail clears it \u2014 about 0.07 of the mass \u2014 which is \\(\\Phi(-1.5)\\), and that is exactly the height of the CDF at \\(x\\beta = -1.5\\) in the bottom panel. The middle bell splits at the mean (area \\(0.5\\)), the right bell clears most of the threshold (about 0.93), and plotting those three areas against \\(x\\beta\\) traces the whole sigmoid: the CDF is the shaded area, accumulated as \\(x\\beta\\) moves. Keep these PDFs in mind because the topic returns in the marginal-effects section below, where we use the PDF\u2019s height rather than its area.\nExamine the figure a second time through the lens of \\(Pr(\\varepsilon_i &gt; -x_i\\beta)\\) from the latent variable motivation above: the shaded area is that probability \u2014 the chance the idiosyncratic \\(\\varepsilon_i\\) clears the bar the systematic part sets at \\(-x_i\\beta\\). For the lefthand bell, \\(x_i\\beta = -1.5\\), so the bar sits at \\(-x_i\\beta = +1.5\\): reaching \\(y=1\\) takes a large favorable shock, \\(\\varepsilon_i &gt; 1.5\\), which happens only rarely. For the righthand bell, \\(x_i\\beta = +1.5\\), so the bar drops to \\(-x_i\\beta = -1.5\\): almost any \\(\\varepsilon_i\\) clears it, and it is now \\(y=0\\) that would need a large unfavorable shock, \\(\\varepsilon_i &lt; -1.5\\). Raising \\(x_i\\beta\\) slides the bar left \u2014 from \\(+1.5\\) toward \\(-1.5\\), asking less and less of the error, the same movement as the shaded area growing from 0.07 to 0.93."
  },
  {
    "objectID": "binarymodels26.html#a-nonlinear-model-for-binary-data",
    "href": "binarymodels26.html#a-nonlinear-model-for-binary-data",
    "title": "Binary Response Models",
    "section": "A nonlinear model for binary data",
    "text": "A nonlinear model for binary data\n\\(y\\) is binary, and we\u2019ve established that the linear model is not appropriate. The observed variable \\(y\\) is binomial (iid) \u2014 it takes the value 1 with probability \\(\\pi_i\\) and 0 with probability \\(1-\\pi_i\\):\n\\[\ny \\sim f_{\\text{binomial}}(\\pi_i)\n\\]\n\\[\ny_i = \\begin{cases} 1, & \\text{with probability } \\pi_i \\\\ 0, & \\text{with probability } 1-\\pi_i \\end{cases}\n\\]\nTwo features run through everything below. First, the model has two parts \u2014 one for the ones and one for the zeros \u2014 and they are mirror images of each other: whatever we write for \\(\\pi_i\\) when \\(y=1\\), we write for \\(1-\\pi_i\\) when \\(y=0\\). Second, both parts go through the same link function \\(F\\), which turns the linear predictor \\(x_i\\beta\\) into a probability:\n\\[\n\\pi_i = F(x_i\\beta), \\qquad 1-\\pi_i = 1 - F(x_i\\beta)\n\\]\nSo using the link, \\(F(x_i\\beta)\\) is the probability of a one, and \\(1 - F(x_i\\beta)\\) is the probability of a zero. Keep that symmetry in mind because this is the shape of every likelihood equation that follows."
  },
  {
    "objectID": "binarymodels26.html#the-binomial-likelihood",
    "href": "binarymodels26.html#the-binomial-likelihood",
    "title": "Binary Response Models",
    "section": "The binomial likelihood",
    "text": "The binomial likelihood\nWrite the binomial density (the probability of a single observation):\n\\[\nPr(y_i = y \\mid \\pi_i) = \\pi_i^{y_i}(1-\\pi_i)^{1-y_i}\n\\]\nRead this as the two-part model in one line: when \\(y_i = 1\\) it returns \\(\\pi_i\\); when \\(y_i = 0\\) it returns \\(1-\\pi_i\\). The exponents are just switches that select the right piece.\nWrite the joint probability of the data as a likelihood \u2014 the product over \\(n\\) iid observations, read as a function of the unknown \\(\\pi\\):\n\\[\n\\mathcal{L}(\\pi \\mid y) = \\prod_{i=1}^{n}\\pi_i^{y_i}(1-\\pi_i)^{1-y_i}\n\\]\nTake the log of that likelihood, which turns the product into a sum:\n\\[\n\\ln\\mathcal{L}(\\pi \\mid y) = \\sum_{i=1}^{n}\\left[y_i \\ln \\pi_i + (1-y_i)\\ln(1-\\pi_i)\\right]\n\\]\nWe now have a generic and flexible log-likelihood function appropriate for binary response variables."
  },
  {
    "objectID": "binarymodels26.html#parameterize-the-model",
    "href": "binarymodels26.html#parameterize-the-model",
    "title": "Binary Response Models",
    "section": "Parameterize the model",
    "text": "Parameterize the model\nSo far \\(\\pi_i\\) is just \u201cthe probability of a one.\u201d To make it a model \u2014 to let covariates shape that probability \u2014 we parameterize it: we make \\(\\pi_i\\) a function of some variables and their slope effects, \\(x\\beta\\). This step is worth pausing on, because parameterizing a distribution\u2019s parameter will be a recurring act all semester and may be a new idea. Every distribution we meet has a parameter (here \\(\\pi\\) for the binomial; for the Poisson it will be a rate \\(\\lambda\\)). \u201cParameterizing\u201d means writing that parameter as a function of covariates \u2014 the systematic component of the model \u2014 so that the data can move the probability around:\n\\[\n\\pi_i = F(x\\beta)\n\\]\nSubstituting into the log-likelihood gives the binomial log-likelihood function in terms of \\(\\beta\\), which is what we actually estimate:\n\\[\n\\ln\\mathcal{L}(\\beta \\mid y, X) = \\sum_{i=1}^{n}\\left[y_i \\ln F(x_i\\beta) + (1-y_i)\\ln(1-F(x_i\\beta))\\right]\n\\]\nBut we still need to fill in \\(F\\), the link function."
  },
  {
    "objectID": "binarymodels26.html#the-link-function",
    "href": "binarymodels26.html#the-link-function",
    "title": "Binary Response Models",
    "section": "The link function",
    "text": "The link function\nWe parameterized \\(\\pi_i = F(x\\beta)\\), and now we need to choose an \\(F\\) that maps the linear prediction onto \\(\\pi_i\\) \u2014 the probability of a one. The systematic component \\(x_i\\beta\\) can range over \\([-\\infty, +\\infty]\\), but a probability cannot \u2014 so \\(F\\) has to do two things:\n\nmap the whole real line onto \\([0,1]\\), so our prediction \\(\\widehat{\\pi}_i\\) is a genuine probability;\ndo it smoothly and monotonically \u2014 a sigmoid \u2014 so more \\(x\\beta\\) always means more probability.\n\nAny sigmoid CDF satisfies the bounds; the two we use most commonly are the standard normal (probit) and the logistic (logit). Recall that the CDF we choose for the link also characterizes the distribution of the latent errors per the discussion above.\n\\[\nPr(y_i = 1 \\mid X) = \\Phi(x_i\\beta) \\qquad \\text{(probit)}\n\\]\n\\[\nPr(y_i = 1 \\mid X) = \\frac{1}{1 + e^{-x_i\\beta}} \\qquad \\text{(logit)}\n\\]\n\n\n\n\n\n\nImportant Concept\n\n\n\nThe link function maps the linear prediction onto the sigmoid probability space and obeys the bounds \\([0,1]\\). Choosing the link is choosing a distribution for the latent error.\n\n\nThis is an important general idea to remember going forward. The link is a translator between two number lines, one for \\(x\\beta\\) (the linear prediction) and one for \\(\\pi\\) (the probability we actually want). Pick a value on the \\(x\\beta\\) axis, read up to the curve, read across \u2014 that\u2019s \\(F(x\\beta)\\). And note what \u201csliding\u201d means here: the curve itself is fixed and never moves; a change in \\(x\\beta\\) just slides the predicted point along it \u2014 left for less, right for more. Every predicted probability in the model is from this one curve.\n\n\ncode\n# The two CDFs as translators from the linear predictor to a probability,\n# with a few (xb, pi) pairs marked on the logit curve.\nxb &lt;- seq(-5, 5, length.out = 400)\ncurve_df &lt;- tibble(xb = xb, logit = plogis(xb), probit = pnorm(xb))\n\nmarks &lt;- tibble(xb = c(-2, 0, 2)) %&gt;% mutate(pi = plogis(xb))\n\nggplot(curve_df, aes(xb, logit)) +\n  geom_line(color = bu_green, linewidth = 1) +\n  geom_line(aes(y = probit), color = bu_lightgreen, linewidth = 1, linetype = \"dashed\") +\n  geom_segment(data = marks,\n               aes(x = xb, xend = xb, y = 0, yend = pi),\n               color = bu_grey, linetype = \"dotted\") +\n  geom_segment(data = marks,\n               aes(x = -5, xend = xb, y = pi, yend = pi),\n               color = bu_grey, linetype = \"dotted\") +\n  geom_point(data = marks, aes(xb, pi), color = bu_black, size = 2) +\n  geom_text(data = marks,\n            aes(xb, pi, label = sprintf(\"(%g, %.2f)\", xb, pi)),\n            vjust = -0.8, hjust = -0.05, size = 3.4) +\n  annotate(\"text\", x = 3.5, y = 0.6, label = \"logit~(Lambda)\", parse = TRUE, color = bu_green) +\n  annotate(\"text\", x = 1.5, y = 0.95, label = \"probit~(Phi)\", parse = TRUE, color = bu_black) +\n  scale_y_continuous(limits = c(0, 1.05)) +\n  labs(title = \"Link function as translator: from linear predictor to probability\",\n       x = expression(x*beta~\"(linear prediction)\"),\n       y = expression(pi == F(x*beta))) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a03\n\n\n\n\n\n\n\n\n\n\n\nWhich link to choose?\n\n\n\nIt\u2019s also worth pointing out that in most (nearly all) social science applications, the choice of link function is inconsequential, particularly between logit and probit. As you can see, the differences lie mainly in the tails; logit has fatter tails, so more probability at the extremes. But the differences are small and in empirical settings, mostly unimportant. As we\u2019ll see in a couple of weeks, we can select other link functions that have different properties that may more closely characterize the distribution of \\(y\\) and our expectations about \\(\\varepsilon\\)."
  },
  {
    "objectID": "binarymodels26.html#probit-and-logit-log-likelihoods",
    "href": "binarymodels26.html#probit-and-logit-log-likelihoods",
    "title": "Binary Response Models",
    "section": "Probit and logit log-likelihoods",
    "text": "Probit and logit log-likelihoods\nFilling \\(F\\) into the parameterized log-likelihood gives the model we actually estimate. This is the workhorse log-likelihood, and choosing \\(F\\) fills in the specific model. Writing each one out makes the two-part structure concrete: the link sits inside the \\(y=1\\) term, and one minus the link sits inside the \\(y=0\\) term.\nThe probit uses the standard normal CDF \\(\\Phi\\) as the link:\n\\[\n\\ln\\mathcal{L}(\\beta \\mid y) = \\sum_{i=1}^{n}\\Big[\\, y_i \\ln \\Phi(x_i\\beta) + (1-y_i)\\ln\\big(1 - \\Phi(x_i\\beta)\\big)\\,\\Big]\n\\]\nThe logit uses the logistic CDF, \\(\\Lambda(x_i\\beta) = \\tfrac{1}{1+e^{-x_i\\beta}}\\):\n\\[\n\\ln\\mathcal{L}(\\beta \\mid y) = \\sum_{i=1}^{n}\\left\\{\\, y_i \\ln\\!\\left(\\frac{1}{1+e^{-x_i\\beta}}\\right) + (1-y_i)\\ln\\!\\left[\\,1 - \\frac{1}{1+e^{-x_i\\beta}}\\,\\right] \\right\\}\n\\]\nAs we did earlier, think of the two terms as the two outcomes, zero and one. The first term is the contribution of the cases where \\(y=1\\) \u2014 the probability the model assigns to a one, \\(F(x_i\\beta)\\). The second term is the contribution of the cases where \\(y=0\\) \u2014 the probability it assigns to a zero, \\(1 - F(x_i\\beta)\\). Choosing the link changes what sits inside the parentheses; it does not change that two-part structure. The probit puts \\(\\Phi\\) in both slots, the logit puts \\(\\Lambda\\) in both slots, and any other binary-response model we write does the same thing with its own \\(F\\).\nThat is the likelihood. Maximizing it \u2014 taking its derivatives and climbing to the \\(\\beta\\) that makes the observed data most probable \u2014 is the topic for the next couple of weeks. For now, we\u2019ll estimate the model and interpret it."
  },
  {
    "objectID": "binarymodels26.html#predicted-probabilities",
    "href": "binarymodels26.html#predicted-probabilities",
    "title": "Binary Response Models",
    "section": "Predicted probabilities",
    "text": "Predicted probabilities\nPredicted probabilities are the most commonly reported quantities of interest for binary response models. For the logit, the predicted probability is3\n\\[\n\\widehat{Pr}(y=1\\mid X) = \\frac{1}{1+e^{-x\\widehat\\beta}},\n\\]\nand for the probit the probability \\(y=1\\) is:\n\\[\n\\widehat{Pr}(y=1\\mid X) = \\Phi(x\\widehat\\beta).\n\\]\nIn both cases we are doing the same thing \u2014 transforming the linear prediction \\(x\\widehat\\beta\\) through the link function so it maps onto the \\([0,1]\\) probability space.\nThe figure below does this for democracy. These are at-means (adjusted) predictions \u2014 democracy varies across its range while the other covariates are held at representative values (the mode for the binary covariates, the median for the skewed capability ratio). We\u2019ll revisit prediction methods specifically in the context of nonlinear models in the coming weeks.\n\n\ncode\n# At-means (adjusted) predictions: democracy varies across its range; the other\n# x-variables are held at representative values -- the mode for the binary\n# covariates (border, ally), the median for skewed capability ratio.\n# (datagrid's default would hold numerics at their mean; I set them\n# to the mode/median to address skewness)\nmodal &lt;- function(x) as.numeric(names(which.max(table(x)))) #function to find mode - R lacks such a function\n\npreds_dem &lt;- predictions(\n  m_logit,\n  newdata = datagrid(deml   = seq(-10, 10, length.out = 21), #functions from \"marginaleffects\" library to generate data for predictions\n                     border = modal(dp$border),\n                     ally   = modal(dp$ally),\n                     lncaprat = median(dp$lncaprat, na.rm = TRUE))\n)\n\nggplot(preds_dem, aes(deml, estimate)) +\n  geom_ribbon(aes(ymin = conf.low, ymax = conf.high),\n              fill = bu_green, alpha = 0.2) +\n  geom_line(color = bu_green, linewidth = 1) +\n  labs(title = \"Predicted Pr(dispute = 1) across democracy\",\n       x = \"minimum dyad democracy (Polity)\",\n       y = \"Pr(y = 1)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\nThe prediction plot indicates that for a non-bordering, non-allied dyad at median capabilities, the probability of a militarized dispute is about 0.048 when the two states are maximally autocratic (minimum democracy \\(=-10\\)), and about 0.012 when they are maximally democratic (\\(=+10\\)). So moving a dyad from fully autocratic to fully democratic cuts its predicted chance of a dispute to roughly a quarter of what it was."
  },
  {
    "objectID": "binarymodels26.html#marginal-effects",
    "href": "binarymodels26.html#marginal-effects",
    "title": "Binary Response Models",
    "section": "Marginal effects",
    "text": "Marginal effects\nStart with the linear model, where this is easy and familiar. The marginal effect of \\(x_k\\) \u2014 the change in \\(\\widehat y\\) for a one-unit change in \\(x_k\\) \u2014 is just \\(\\widehat\\beta_k\\):\n\\[\n\\frac{\\partial \\widehat y}{\\partial x_k} = \\frac{\\partial\\, x\\widehat\\beta}{\\partial x_k} = \\widehat\\beta_k.\n\\]\nIt is constant \u2014 the same at every value of \\(x\\).\n\n\ncode\nme_line &lt;- tibble(x = seq(0, 10, 0.1), y = 2 * x)\n\nggplot(me_line, aes(x, y)) +\n  geom_line(color = bu_green, linewidth = 1) +\n  annotate(\"segment\", x = 5,  xend = 5,  y = 0, yend = 10, color = \"red\") +\n  annotate(\"segment\", x = 10, xend = 10, y = 0, yend = 20, color = \"red\") +\n  annotate(\"text\", x = 3.5, y = 16, label = \"y = 2x\") +\n  labs(title = \"A linear marginal effect is constant\", x = \"x\", y = \"y\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\n\nThe effect of \\(x\\) on \\(y\\) here is 2 \u2014 the same at \\(x=5\\) as at \\(x=10\\). The nonlinear model breaks that. Because the prediction runs through the curve \\(F\\), the marginal effect depends on where on the curve we are \u2014 that is, on where \\(x\\widehat\\beta\\) sits with respect to that curve:\n\\[\n\\frac{\\partial Pr(y=1)}{\\partial x_k} = \\frac{\\partial F(x\\beta)}{\\partial x\\beta}\\cdot\\frac{\\partial x\\beta}{\\partial x_k} = f(x\\beta)\\,\\widehat{\\beta}_k,\n\\]\nwhere the derivative of the CDF \\(F\\) is its PDF \\(f\\). So the marginal effect is \\(\\widehat\\beta_k\\) weighted by the height of the PDF at \\(x\\widehat\\beta\\) \u2014 large where the density is tall, small where it is flat.\n\n\n\n\n\n\nImportant Concept\n\n\n\nThe effect of \\(x\\) on \\(Pr(y=1)\\) is not constant. It is largest where the latent PDF is densest \u2014 for probit and logit, that\u2019s near \\(x\\beta=0\\), i.e., where \\(Pr(y=1) \\approx 0.5\\). Near the floor (0) or ceiling (1), the same \\(\\widehat{\\beta}\\) moves the probability hardly at all.\n\n\nTake a look at the next figure \u2014 it shows the logit CDF with tangent line slopes at three different points. The link function is a single fixed sigmoid; the marginal effect at any point is its slope there. In the middle, the curve is steep, so a change in \\(x\\beta\\) moves the probability a lot; out in the tails it is nearly flat, so the same change barely moves it. Where a given case sits on the curve is set by its own values of the \\(x\\) variables \u2014 as they increase, it slides right toward the steep middle; as they decrease, it slides left into the flat tail. That is why one coefficient can deliver a large effect for one case and a tiny one for another: same \\(\\widehat\\beta\\), different place on the curve.\n\n\n\n\n\n\nTwo important asides on marginal effects\n\n\n\nFirst, note the symmetry of the CDF (true for logistic and normal) \u2014 the slopes in the tails are the same even though the values of \\(x\\beta\\) would be different from one another, as would the predicted probabilities. It is also the case that the maximum slope in the middle at zero (0.25) is the maximum height of the logit PDF \u2014 we\u2019ll see the two side by side shortly. This implies that the maximum marginal effect of any \\(x_k\\) variable in a logit model is \\(0.25\\,\\widehat\\beta_k\\). You\u2019ll see this illustrated below.\nSecond, as indicated above, a change in any \\(x_k\\) will shift the prediction on the CDF. Changing the value of a control variable will change the predicted probability even if the variable of interest stays the same. In a couple of weeks, we\u2019ll return to this \u2014 the inherent interactivity of nonlinear models, which Nagler describes as being \u201c\u2018interactive\u2019 in all of their variables\u201d and inherent to the specification (Nagler 1994, 1991; Ai and Norton 2003; Berry, DeMeritt, and Esarey 2010).\n\n\n\n\ncode\n# The marginal effect of a covariate is the SLOPE of the link at the point where\n# a case sits. For the logit that slope is the density lambda(xb), which works out\n# to Lambda(xb)*(1-Lambda(xb)) = Pr(1)*Pr(0) (derived in \"Logit marginal effects\"\n# below):\n# tallest in the middle (0.25 at xb = 0, where Pr = .5), flat in the tails. A\n# case's other covariates set WHERE on the curve it sits -- raise them and it\n# slides toward the steep middle, lower them and it slides into the flat tail.\n# Same coefficient, different effect.\nsig &lt;- tibble(xb = seq(-6, 6, length.out = 400), p = plogis(xb))\n\npts &lt;- tibble(xb = c(-3, 0, 3)) %&gt;%          # three positions on the curve\n  mutate(p = plogis(xb), slope = p * (1 - p))\n\nd &lt;- 1.1                                      # half-length of each tangent, in xb units\ntangents &lt;- pts %&gt;%\n  mutate(x0 = xb - d, x1 = xb + d,\n         y0 = p - slope * d, y1 = p + slope * d)\n\nggplot(sig, aes(xb, p)) +\n  geom_line(color = bu_green, linewidth = 1) +\n  geom_segment(data = tangents, aes(x = x0, xend = x1, y = y0, yend = y1),\n               color = bu_black, linewidth = 0.8) +\n  geom_point(data = pts, aes(xb, p), color = bu_black, size = 2) +\n  geom_text(data = pts, aes(xb, p, label = sprintf(\"slope = %.2f\", slope)),\n            vjust = -1.3, size = 3.3) +\n  annotate(\"segment\", x = -3.3, xend = 3.3, y = -0.10, yend = -0.10,\n           arrow = arrow(length = unit(0.15, \"cm\"), ends = \"both\"), color = bu_black) +\n  annotate(\"text\", x = 0, y = -0.16, size = 3, color = bu_black,\n           label = \"a case's other covariates slide it along the curve\") +\n  scale_y_continuous(limits = c(-0.18, 1.08)) +\n  labs(title = \"The marginal effect is the slope of the link -- steepest in the middle\",\n       x = expression(x*beta), y = expression(pi == Lambda(x*beta))) +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a04\n\n\n\n\n\nFor the democratic-peace data this is why the average marginal effects below are quite small: disputes are rare, so most dyads sit far out on the flat lefthand tail, where even a relatively large coefficient moves the probability only a little.\n\nLogit marginal effects\nFor the logit, the PDF \\(\\lambda\\) has a particularly clean form. Working it through:\n\\[\n\\begin{aligned}\n\\frac{\\partial \\Lambda(x\\beta)}{\\partial x_k}\n&= \\lambda(x\\beta)\\,\\widehat\\beta_k \\\\\n&= \\frac{e^{x\\beta}}{(1+e^{x\\beta})^2}\\,\\widehat\\beta_k \\\\\n&= \\frac{e^{x\\beta}}{1+e^{x\\beta}}\\cdot\\frac{1}{1+e^{x\\beta}}\\,\\widehat\\beta_k \\\\\n&= \\Lambda(x\\beta)\\big(1-\\Lambda(x\\beta)\\big)\\,\\widehat\\beta_k.\n\\end{aligned}\n\\]\nLooking at the last line above, the marginal effect is the probability of a one times the probability of a zero times \\(\\widehat\\beta_k\\). That product \\(\\Lambda(1-\\Lambda)\\) is largest when \\(\\Lambda = 0.5\\), where it is \\(0.5\\times0.5 = 0.25\\). So the largest marginal effect any covariate can have in a logit is \\(0.25\\,\\widehat\\beta_k\\).\nIn the democratic-peace model, the coefficient on democracy is about -0.07. So the largest effect a one-unit change in democracy can have on the probability of a dispute is one-quarter of that \u2014 about -0.0175 \u2014 and that only near \\(Pr(y=1)=0.5\\), which for these rare-event data almost never happens.\n\n\nProbit marginal effects\nThe probit works the same way, with the standard normal PDF \\(\\phi\\) in place of \\(\\lambda\\):\n\\[\n\\frac{\\partial \\Phi(x\\beta)}{\\partial x_k} = \\phi(x\\beta)\\,\\widehat\\beta_k.\n\\]\nThe standard normal density peaks at \\(x\\beta = 0\\), where \\(\\phi(0) = \\tfrac{1}{\\sqrt{2\\pi}} \\approx 0.4\\). So the maximum marginal effect of any covariate in a probit is about \\(0.4\\,\\widehat\\beta_k\\).\nPut the two link functions side by side to see where \\(0.25\\) and \\(0.4\\) come from \u2014 and to tie the marginal effect back to the density we started with.\n\n\ncode\n# Each link's PDF is the slope of its CDF, and the PDF's peak (at xb = 0) is the\n# largest marginal-effect weight: 0.25 for the logit, 0.399 for the probit.\n# Colors match fig-mapping: logit = green (solid), probit = light green (dashed).\nz    &lt;- seq(-5, 5, length.out = 400)\ndens &lt;- tibble(z = z,\n               logit_cdf  = plogis(z), logit_pdf  = dlogis(z),\n               probit_cdf = pnorm(z),  probit_pdf = dnorm(z))\n\nggplot(dens, aes(z)) +\n  geom_line(aes(y = logit_cdf),  color = bu_green,      linewidth = 1) +\n  geom_line(aes(y = probit_cdf), color = bu_lightgreen, linewidth = 1, linetype = \"dashed\") +\n  geom_line(aes(y = logit_pdf),  color = bu_green,      linewidth = 0.8) +\n  geom_line(aes(y = probit_pdf), color = bu_lightgreen, linewidth = 0.8, linetype = \"dashed\") +\n  geom_hline(yintercept = 0.25,   color = bu_green,      linetype = \"dotted\") +\n  geom_hline(yintercept = 0.3989, color = bu_lightgreen, linetype = \"dotted\") +\n  annotate(\"text\", x = -4.8, y = 0.28, hjust = 0, size = 3, color = bu_green,\n           label = \"logit PDF peak = 0.25\") +\n  annotate(\"text\", x = -4.8, y = 0.42, hjust = 0, size = 3, color = bu_black,\n           label = \"probit PDF peak = 0.399\") +\n  annotate(\"text\", x = 3.4,  y = 0.96, size = 3, color = bu_black, label = \"CDF = the link\") +\n  annotate(\"text\", x = 1.15, y = 0.33, size = 3, color = bu_black, label = \"PDF = its slope\") +\n  labs(title = \"The link's slope is its PDF; the PDF's peak is the largest marginal effect\",\n       x = expression(x*beta), y = \"link (CDF) and its slope (PDF)\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a05\n\n\n\n\n\nThe PDF under each S-curve is its slope \u2014 the density \\(f\\) \u2014 the very weight from Figure\u00a04, now shown for both links. Its peak sits at \\(x\\beta = 0\\): \\(0.25\\) for the logit, \\(0.399\\) for the probit. And for the probit the plot is the normal bell curve from Figure\u00a02, whose area past the threshold gave us \\(Pr(y=1)\\). There we read the model off the density\u2019s area; here we read the marginal effect off its height. Same density, two jobs.\nIn practice we rarely report the maximum; we report the average marginal effect \u2014 the effect computed for each case in the sample and averaged across all of them. That\u2019s a quantity-of-interest method we develop fully later, but here is the average marginal effect for the logit:\n\n\ncode\names &lt;- avg_slopes(m_logit)\n\names %&gt;%\n  as_tibble() %&gt;%\n  dplyr::select(term, estimate, std.error, conf.low, conf.high) %&gt;%\n  knitr::kable(digits = 3, caption = \"Average marginal effects, logit\")\n\n\n\nAverage marginal effects, logit\n\n\nterm\nestimate\nstd.error\nconf.low\nconf.high\n\n\n\n\nally\n-0.032\n0.003\n-0.038\n-0.027\n\n\nborder\n0.046\n0.004\n0.037\n0.055\n\n\ndeml\n-0.003\n0.000\n-0.003\n-0.002\n\n\nlncaprat\n-0.009\n0.001\n-0.011\n-0.007\n\n\n\n\n\nLooking at these effects, we can say the average marginal effect of the minimum democracy score is about -0.0029: averaging over every dyad in the sample, raising the lower democracy score by one point lowers the predicted probability of a militarized dispute by about that much \u2014 roughly three in a thousand. It is small for the reason the callout gave: disputes are rare, so these data sit far out on the flat tail of the logistic curve, where even a genuine effect moves the probability only a little. Shared border, by contrast, carries the largest average marginal effect in the model, about 0.046 \u2014 contiguity raises the average predicted probability of a dispute by four or five percentage points."
  },
  {
    "objectID": "binarymodels26.html#logit-odds-and-log-odds",
    "href": "binarymodels26.html#logit-odds-and-log-odds",
    "title": "Binary Response Models",
    "section": "Logit odds and log-odds",
    "text": "Logit odds and log-odds\nThe logit has one more interpretation that the probit lacks, and it can be a useful one. The odds of an event are the probability it happens over the probability it doesn\u2019t:\n\\[\n\\Omega(X) = \\frac{Pr(y=1)}{1-Pr(y=1)} = \\frac{\\Lambda(X\\beta)}{1-\\Lambda(X\\beta)}.\n\\]\nTake logs, and the log-odds turn out to be exactly linear in \\(X\\):\n\\[\n\\ln\\Omega(X) = X\\beta,\n\\]\nso \\(\\partial \\ln\\Omega / \\partial x_k = \\widehat\\beta_k\\), constant. This is why a logit coefficient is sometimes just called \u201cthe logit\u201d \u2014 it is the change in the log-odds for a one-unit change in \\(x_k\\).\nTo read a coefficient on this scale, take it at face value. The coefficient on shared border is about 1.04, so moving border from 0 to 1 adds about 1.04 to the log-odds of a dispute \u2014 and it adds the same amount whatever the other covariates are. Democracy\u2019s coefficient is about -0.07; being negative, each one-point rise in the minimum democracy score subtracts about 0.07 from the log-odds. Additive and constant \u2014 that is the appeal of the log-odds scale. The catch is that no one has much intuition for a log-odds, which is why we usually exponentiate back to odds.\n\nOdds ratios\nLog-odds are linear but not very intuitive. Exponentiating gets us back to odds, and the ratio of the odds at \\(x_k+1\\) versus at \\(x_k\\) is simply\n\\[\n\\frac{\\Omega(x_k+1)}{\\Omega(x_k)} = e^{\\widehat\\beta_k}.\n\\]\nThe change need not be one unit; for a change of \\(\\delta\\) it\u2019s \\(e^{\\widehat\\beta_k \\delta}\\). Most usefully,\n\\[100\\cdot(e^{\\widehat\\beta_k}-1)\\]\nis the percentage change in the odds for a one-unit change in \\(x_k\\) \u2014 a summary that doesn\u2019t depend on the values of the other \\(X\\) variables.\nFor example, the logit coefficient on shared border is about 1.04. Exponentiating that coefficient gives roughly 2.82 \u2014 so sharing a border multiplies the odds of a dispute by about that factor, a 182% increase in the odds.\nDemocracy runs the other way. Its coefficient is about -0.07, so exponentiating gives about 0.932 \u2014 each one-point rise in the lower democracy score multiplies the odds of a dispute by that factor, a -7% change. The minus sign carries straight through to the odds: more democracy, lower odds of conflict."
  },
  {
    "objectID": "binarymodels26.html#footnotes",
    "href": "binarymodels26.html#footnotes",
    "title": "Binary Response Models",
    "section": "Footnotes",
    "text": "Footnotes\n\n\nA probability mass function puts a probability on each value a discrete variable can take: each value has some assigned probability, and these probabilities sum to one. A probability density function describes a continuous variable, where any single value has probability zero \u2014 there are uncountably many of them, so none can carry positive probability. The density \\(f(x)\\) is not a probability but a rate (probability per unit of \\(x\\)); you get an actual probability only by integrating it over a range \u2014 the area under the curve between two points. Hence mass at a point, density over an interval.\u21a9\ufe0e\nI used Claude Code to generate Figures 2 and 4 in these slides.\u21a9\ufe0e\nThe logit probability is just as often written as \\(\\Lambda(x\\beta) = \\frac{e^{x\\beta}}{1+e^{x\\beta}}\\), but it\u2019s the same function. To see how, multiply the top and bottom of \\(\\frac{1}{1+e^{-x\\beta}}\\) by \\(e^{x\\beta}\\): the top becomes \\(e^{x\\beta}\\), and the bottom becomes \\(e^{x\\beta}\\left(1+e^{-x\\beta}\\right) = e^{x\\beta}+1\\), since \\(e^{x\\beta}e^{-x\\beta}=e^{0}=1\\). So \\(\\dfrac{1}{1+e^{-x\\beta}} = \\dfrac{e^{x\\beta}}{1+e^{x\\beta}}\\).\u21a9\ufe0e"
  },
  {
    "objectID": "slides.html",
    "href": "slides.html",
    "title": "Slides",
    "section": "",
    "text": "Lecture slides and notes will be linked here as the semester proceeds.\n\nLinear Model Prediction Methods \u2014 prediction and quantities of interest in the linear model (at-means and average effects; ML, delta, and simulation-based uncertainty), previewing the methods we generalize to nonlinear models.\nBinary Response Models \u2014 building a maximum-likelihood model for a binary outcome: why the linear probability model fails, the latent-variable and random-utility motivations, the likelihood and link functions, and interpretation via predicted probabilities, marginal effects, and odds.\nLikelihood Theory & Estimation Mechanics \u2014 what a likelihood is and how it differs from a probability; the estimation machine and how little of it changes from one model to the next; analytic methods and closed-form solutions, and where they fail; and the two derivatives \u2014 the score and the Hessian \u2014 that locate the estimate and set its standard error.\nMaximizing the Log-Likelihood Function \u2014 Newton\u2013Raphson from the ground up: where the method comes from, what its two halves do, one step worked by hand, then the whole loop run to convergence on a real logit, with an animated climb, the iteration log, starting values, and the properties that justify the estimator.\nDiagnostics, Classification, and Fit \u2014 what to check after a binary-response model is fitted: whether the link\u2019s symmetry assumption suits the data, how classification and ROC curves work and why they mean more for a House roll call than for the democratic peace, how separation breaks the estimator quietly (especially when you add fixed effects), rare-events bias and the Firth correction, and the trinity of tests with the information criteria.\nPrediction Methods for MLE Models \u2014 quantities of interest from nonlinear models: at-means and average effects, building a scenario\u2019s linear prediction, King\u2013Tomz\u2013Wittenberg simulation, ML and delta-method standard errors with end-point transformation, the bootstrap, average effects bounded two ways, and guidance on which approach and which technique to choose.\n\n\n\n\n Back to top"
  },
  {
    "objectID": "likelihood26.html",
    "href": "likelihood26.html",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "",
    "text": "Last week we built a model for a binary \\(y\\) from the ground up. We looked at \\(y\\), wrote down the probability distribution that described it, took the joint probability of the whole sample, logged it, made the distribution\u2019s parameter a function of \\(x\\beta\\), and chose a link function \\(F\\) to map the linear prediction on the appropriate (probability) space. Six steps, and at the end of them we were holding a log-likelihood function:\n\\[\n\\ell(\\beta) = \\sum_{i=1}^{n}\\big[\\, y_i \\log \\pi_i + (1-y_i)\\log(1-\\pi_i)\\,\\big],\n\\qquad \\pi_i = F(x_i\\beta).\n\\]\nThen we used glm to estimate the logit and generated quantities of interest.\nWhat we haven\u2019t covered is how the model got estimated \u2014 what goes on in the magic box when we call glm with a binomial family and a logit link. Filling that gap is the task here, answering these questions:\n\nWhat is a likelihood, and how does it differ from a probability?\nGiven data and a log-likelihood, how do we solve for the unknowns \u2014 and what, dimensionally, are we solving?\nWhy are some estimates precise with small standard errors and others are not, and how does that arise from the data and the math of likelihood?\n\nThis will illustrate something we mentioned last week. In interpretation, a nonlinear model differs from a linear one in exactly one respect: we transform the linear prediction by \\(F\\). In estimation, the two technologies are genuinely different. OLS gives you a single formula to apply to the data. Maximum likelihood, in almost every case we care about, does not \u2014 it builds a formula out of the data, then tries different \u201cmodels,\u201d comparing them until it finds the one most likely to have generated what we observed."
  },
  {
    "objectID": "likelihood26.html#sec-machine",
    "href": "likelihood26.html#sec-machine",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "The machine",
    "text": "The machine\nHere is the whole thing as a single chain. Read the arrows as steps, not as matrix operations: this is the order in which you actually do the work, from a spreadsheet on the left to a quantity of interest on the right. The top line is what you decide. The bottom line is what the machine does once you have decided.\n\\[\n\\begin{aligned}\n\\textbf{choose:}\\;&\\underbrace{\\text{data}}_{\\text{spreadsheet}} \\;\\to\\;\n\\underbrace{X,\\; y}_{n \\times k,\\; n \\times 1} \\;\\to\\;\n\\underbrace{\\theta}_{\\text{what we want}} \\;\\to\\;\n\\underbrace{\\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\,f(y \\mid \\theta)\\,}}_{\\textbf{swaps}} \\;\\to\\;\n\\underbrace{\\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\,F\\,}}_{\\textbf{swaps}} \\\\[12pt]\n\\textbf{compute:}\\;&\\underbrace{X\\beta}_{n \\times 1} \\;\\to\\;\n\\underbrace{\\widehat\\theta = F(X\\beta)}_{n \\times 1} \\;\\to\\;\n\\underbrace{\\ell(\\beta)}_{\\text{one number}} \\;\\to\\;\n\\underbrace{\\nabla\\ell,\\; H}_{k \\times 1,\\; k \\times k} \\;\\to\\;\n\\underbrace{(-H)^{-1}}_{k \\times k} \\;\\to\\;\n\\underbrace{\\text{SE}}_{k \\times 1} \\;\\to\\; \\text{QI}\n\\end{aligned}\n\\]\nThe two green boxes are the only things that change from one model to the next. Everything else \u2014 forming \\(X\\beta\\), the derivatives, the inversion, the square roots \u2014 is identical whether we are estimating a logit, a Poisson, or a Cox model. When we move to counts in a few weeks, we swap those two boxes and nothing else moves.\nWhat you choose:\n\nThe data are a spreadsheet, \\(n\\) rows by \\(k+1\\) columns \u2014 one outcome and \\(k\\) covariates, counting the constant. The first thing estimation does is split it into the design matrix \\(X\\) (\\(n \\times k\\)) and the outcome \\(y\\) (\\(n \\times 1\\)).\n\\(\\theta\\) is the quantity you want to model \u2014 a probability, an expected count, a hazard rate, a time until failure. This is a substantive decision and it comes first. Notice what it does not settle: knowing \\(\\theta\\) is a probability tells you it has to land in \\([0,1]\\); it tells you nothing about which S-shaped function gets it there.\n\\(f(y \\mid \\theta)\\) \u2014 the first swap \u2014 is the distribution that generates \\(y\\) given that quantity. Look at \\(y\\) and it is nearly decided for you: a zero or a one gets the Bernoulli, whose parameter is the probability we wanted; a count gets the Poisson, whose parameter is a rate; a duration gets an exponential or a Weibull.\n\\(F\\) \u2014 the second swap \u2014 is the link, the function mapping the linear predictor onto the space \\(\\theta\\) has to occupy. Knowing \\(\\theta\\) is a probability tells us we need something running from the real line into \\([0,1]\\), and there are many such things \u2014 logit, probit, cloglog are three of them. Choosing among them is a genuinely separate decision, which is why it is a separate step.\n\nWhat the machine does:\n\nForm the linear predictor \\(X\\beta\\) \u2014 an \\(n \\times k\\) matrix times a \\(k \\times 1\\) vector, one number per case. This is the part every model shares with OLS.\nPush it through the link for the fitted values \\(\\widehat\\theta = F(X\\beta)\\), still \\(n \\times 1\\): the model\u2019s estimate of that quantity for every row. (\\(\\theta\\) up top is the quantity we set out to model; \\(\\widehat\\theta\\) down here is what a particular \\(\\beta\\) predicts for it.)\nScore those fitted values against the observed \\(y\\) using \\(f\\), sum the logs, and you have the log-likelihood \\(\\ell(\\beta)\\) \u2014 one number saying how well this particular \\(\\beta\\) accounts for the whole sample. Note where \\(y\\) went: it comes in at the far left and then sits out entirely until this step. \\(\\ell(\\beta)\\) is the one place in the machine where the data and the model\u2019s predictions are compared.\nThe gradient, or score, \\(\\nabla\\ell(\\beta)\\), is \\(k \\times 1\\) \u2014 one partial derivative per parameter: a vector, not to be confused with the \\(k \\times k\\) Hessian.\nThe Hessian \\(H\\) is \\(k \\times k\\) \u2014 the second derivative of \\(\\ell\\) with respect to every pair of parameters. Its negative inverse \\((-H)^{-1}\\) is the \\(k \\times k\\) variance\u2013covariance matrix, and the square roots of its diagonal are the standard errors.\n\nThe score and the Hessian probably mean nothing to you yet; the second half of these notes is about what they are. What to take from the chain now is its shape: two substantive choices, made in the order a researcher actually makes them, then a machine that runs identically every time."
  },
  {
    "objectID": "likelihood26.html#the-binomial-log-likelihood",
    "href": "likelihood26.html#the-binomial-log-likelihood",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "The binomial log-likelihood",
    "text": "The binomial log-likelihood\nMake it concrete with the binomial. For a single binary outcome \\(y_i\\), write \\(\\pi_i = Pr(y_i = 1)\\) for the probability of a one. The density of one observation is the Bernoulli, and we map the linear predictor \\(x_i\\beta\\) into that probability with a link function \\(F\\):\n\\[\nf(y_i \\mid \\pi_i) = \\pi_i^{y_i}(1-\\pi_i)^{1-y_i},\n\\qquad\n\\pi_i = F(x_i\\beta).\n\\]\nThose two objects are the two green boxes from the machine, in that order. The quantity we want is \\(\\pi_i\\), a probability. The Bernoulli \\(f(y_i \\mid \\pi_i)\\) is the first box, and looking at \\(y\\) all but picks it: the outcome is a zero or a one. The link \\(F\\) is the second box and a separate decision \u2014 \\(\\pi_i\\) must land in \\([0,1]\\) and \\(x_i\\beta\\) has no such manners, it will happily return \\(-4.2\\) or \\(17\\). Any well-behaved CDF would discipline it; which one we pick is a separate argument, and one we have already had.\nThose are the only two substantive choices in the model. Everything downstream \u2014 forming \\(X\\beta\\), summing logs, differentiating, inverting, taking square roots \u2014 is machinery that neither knows nor cares that the outcome is binary. Build the Poisson in a few weeks and we change exactly these two lines, then run the identical remainder.\nTake the log of that density, sum over the \\(n\\) observations, and substitute the link for \\(\\pi_i\\). That gives us the form we will use for the rest of the semester:\n\\[\n\\ell(\\beta) \\;=\\; \\sum_{i=1}^{n}\\Big[\\;\n\\underbrace{y_i \\log F(x_i\\beta)}_{\\text{the ones}}\n\\;+\\;\n\\underbrace{(1-y_i)\\log\\big(1 - F(x_i\\beta)\\big)}_{\\text{the zeros}}\n\\;\\Big]\n\\]\nAs we discussed last week, the two parts of this log-likelihood are mirror images, corresponding to the structure of a binary variable. The \\(y_i\\) out front of the first term and the \\((1-y_i)\\) out front of the second are doing switching work: when \\(y_i = 1\\) the second term is multiplied by zero and vanishes, and when \\(y_i = 0\\) the first term vanishes. So every observation in the sample contributes exactly one of the two, never both. The ones contribute \\(\\log Pr(y_i = 1)\\), and the zeros contribute \\(\\log Pr(y_i = 0)\\)."
  },
  {
    "objectID": "likelihood26.html#analytic-methods-and-closed-form-solutions",
    "href": "likelihood26.html#analytic-methods-and-closed-form-solutions",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "Analytic methods, and closed-form solutions",
    "text": "Analytic methods, and closed-form solutions\nTake the binomial with no \\(x\\) variables at all. Every observation shares the same probability \\(\\pi\\) \u2014 there is one unknown, a single number, and we want the value of it that makes our observed string of zeros and ones most probable. Write the log-likelihood, differentiate with respect to \\(\\pi\\), set it to zero:\n\\[\n\\begin{aligned}\n\\ell(\\pi) &= \\sum_{i=1}^{n}\\big[\\, y_i\\log\\pi + (1-y_i)\\log(1-\\pi)\\,\\big] \\\\[4pt]\n          &= \\Big(\\textstyle\\sum_i y_i\\Big)\\log\\pi \\;+\\; \\Big(n - \\textstyle\\sum_i y_i\\Big)\\log(1-\\pi) \\\\[4pt]\n\\frac{d\\ell}{d\\pi} &= \\frac{\\sum_i y_i}{\\pi} \\;-\\; \\frac{n - \\sum_i y_i}{1-\\pi} \\;=\\; 0 \\\\[4pt]\n(1-\\pi)\\textstyle\\sum_i y_i &= \\pi\\Big(n - \\textstyle\\sum_i y_i\\Big) \\\\[4pt]\n\\textstyle\\sum_i y_i &= \\pi\\, n \\\\[4pt]\n\\widehat\\pi &= \\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\;\\frac{\\sum_i y_i}{n} \\;=\\; \\bar y\\;}\n\\end{aligned}\n\\]\nThe second line just counts: \\(\\sum_i y_i\\) observations contribute a \\(\\log\\pi\\), the remaining \\(n - \\sum_i y_i\\) contribute a \\(\\log(1-\\pi)\\). Two steps of algebra from there, and the answer is the sample mean of \\(y\\). No software, no starting value, no algorithm \u2014 a formula.\nOn the dispute data, that formula says the maximum likelihood estimate of \\(\\pi\\) is the observed dispute rate:\n\n\ncode\ndp &lt;- read_dta(here::here(\"data\", \"dp.dta\"))\ndp$lncaprat &lt;- log(dp$caprat)   # a capability ratio is multiplicative, so model it on the log scale\n\nround(mean(dp$dispute), 4)\n\n\n[1] 0.0451\n\n\nTwo names for what we just did, because you will meet both constantly. Solving a model by taking a derivative, setting it to zero, and rearranging is an analytic method \u2014 algebra and calculus. When an analytic method works, what it produces is a closed-form solution: an explicit formula for the estimate in terms of the data, like \\(\\widehat\\pi = \\bar y\\). No starting values, no iteration, just straightforward computation.\nFigure\u00a03 draws the log-likelihood this formula maximizes, with the closed-form answer marked on it; it uses the democratic peace \\(y\\) variable, presence or absence of a dispute.\n\n\ncode\n# The log-likelihood over its one unknown, with the value the algebra produced\n# marked. I plot RELATIVE log-likelihood (subtracting the maximum) because the\n# level carries no information -- only comparisons across values of pi do.\n\npi_hat &lt;- mean(dp$dispute)\ng_pi   &lt;- seq(0.030, 0.070, length.out = 400)\nll_pi  &lt;- sum(dp$dispute) * log(g_pi) + sum(1 - dp$dispute) * log(1 - g_pi)\n\nggplot(tibble(pi = g_pi, rel = ll_pi - max(ll_pi)), aes(pi, rel)) +\n  geom_line(linewidth = 1, color = bu_green) +\n  geom_vline(xintercept = pi_hat, linetype = \"dashed\", color = bu_black) +\n  annotate(\"text\", x = pi_hat, y = -95, hjust = -0.1, size = 3.6, color = bu_black,\n           label = paste0(\"hat(pi) == \", round(pi_hat, 4)), parse = TRUE) +\n  labs(title = \"Bernoulli: militarized disputes\",\n       subtitle = expression(hat(pi) == bar(y) ~ \", the sample proportion, found by algebra\"),\n       x = expression(pi), y = \"relative log-likelihood\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a03\n\n\n\n\n\nOne smooth hill with a single top: that shape is the structural fact that lets the calculus work. And hold onto its width. This hill is built on nearly 21,000 dyad-years, and by the end of these notes the width of a log-likelihood at its top will be the standard error.\nIf maximum likelihood always went like this, it would be a footnote in a calculus course rather than a semester-long class. It doesn\u2019t, and it\u2019s worth being precise about where it falls apart.\n\nWhere the analytic method fails\nWe don\u2019t actually want a single \\(\\pi\\) for the whole sample. We want it to vary with covariates; that is the entire point of a regression. So we parameterize \u2014 \\(\\pi_i = \\Lambda(x_i\\beta)\\) \u2014 and differentiate again, now with respect to \\(\\beta\\) rather than \\(\\pi\\).\nPut the two derivatives we built earlier side by side, the binomial\u2019s and the Normal\u2019s, and set each to zero. They are as close to identical as two expressions can be:\n\\[\n\\begin{aligned}\n\\text{binomial:}\\quad\n  \\frac{\\partial \\ell}{\\partial \\beta} &= X'\\big(y - \\Lambda(X\\beta)\\big) \\;=\\; 0 \\\\[8pt]\n\\text{Normal:}\\quad\n  \\frac{\\partial \\ell}{\\partial \\beta} &= X'\\big(y - X\\beta\\big) \\;=\\; 0\n\\end{aligned}\n\\]\nSame \\(X'\\), same \\(y\\), same \u201cobserved minus fitted\u201d in the middle. The only difference is the four characters wrapped around \\(X\\beta\\) on the top line \u2014 and that difference decides everything.\nOn the bottom line, \\(\\beta\\) sits out in the open, multiplied by matrices we already know. Distribute, collect, invert, and it comes free: \\(X'y = X'X\\beta\\), so \\(\\widehat\\beta = (X'X)^{-1}X'y\\). We did exactly this a few pages ago, and the answer was OLS.\nOn the top line, try the same moves. Distribute the \\(X'\\) and you get \\(X'y = X'\\Lambda(X\\beta)\\) \u2014 and then you are stuck, because there is no way to peel \\(\\Lambda\\) off the right-hand side and leave \\(\\beta\\) standing alone. It is inside a nonlinear function, wrapped up with the data, and no rearranging extracts it. The same is true of the Poisson\u2019s \\(X'(y - e^{X\\beta})\\). There is no closed form. Not one we have failed to find \u2014 one that does not exist.\nWhat we use instead is a numerical method: a procedure that searches for the answer by evaluating the function over and over, rather than solving for it. Guess a \\(\\beta\\), see what the function says, use that to make a better guess, repeat until you stop improving. The particular numerical method this course uses is Newton\u2013Raphson; we\u2019ll build it in the maximization notes.\nTo prepare for the Newton-Raphson, let\u2019s assemble two things Newton\u2013Raphson needs \u2014 the first and second derivatives of the log-likelihood \u2014 and show what they tell us about the estimate quite apart from any algorithm.\n\n\n\n\n\n\nTake away\n\n\n\n\nWriting down a log-likelihood and solving it are two different jobs. The first recipe always works the same way; the second is where models differ in difficulty.\nAn analytic method \u2014 differentiate, set to zero, rearrange \u2014 gives a closed-form solution when it works: an explicit formula, like \\(\\widehat\\pi = \\bar y\\). No starting values, no iteration.\nAdd covariates and it stops working. \\(\\beta\\) ends up buried inside a nonlinear link, \\(\\Lambda(x\\beta)\\), and no rearranging gets it out.\nThe linear model is the exception, and it is worth knowing exactly why: its link is the identity, so \\(\\beta\\) sits out in the open and one line of algebra frees it.\nWe use a numerical method instead: search rather than solve."
  },
  {
    "objectID": "likelihood26.html#the-first-derivative-score-gradient",
    "href": "likelihood26.html#the-first-derivative-score-gradient",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "The first derivative (score, gradient)",
    "text": "The first derivative (score, gradient)\nThe first derivative of the log-likelihood goes by two names we use interchangeably \u2014 the score, or the gradient \u2014 written \\(\\nabla\\ell(\\beta)\\) either way.\nThe log-likelihood assigns one number to every candidate value of \\(\\beta\\): how probable this sample is under it. The score asks a question about that number, one coefficient at a time \u2014 nudge this coefficient up a hair, holding the others still; does the sample get more probable or less? One answer per coefficient, so the score is a \\(k \\times 1\\) vector. Positive says raise it, negative says lower it, and when every entry is zero at once \u2014 no nudge to anything helps \u2014 we are on top of the hill.\nFor the logit, the score is:\n\\[\n\\nabla\\ell(\\beta) \\;=\\; \\sum_{i=1}^{n}(y_i - \\pi_i)\\,x_i' \\;=\\; X'(y - \\pi).\n\\]\nThis should look familiar. \\((y - \\pi)\\) is the model\u2019s mistakes, one per row; multiplying by \\(X'\\) runs each covariate\u2019s column against those mistakes and sums, one number per covariate. So:\n\neach element of the score is the leftover association between one covariate and what the model is currently getting wrong.\n\nSay the model systematically under-predicts disputes in bordering dyads. Then the mistakes are positive in exactly the rows where border is 1, their products are positive, and the border score comes out positive: this coefficient is too small, and that column still has unused information in it.\nThink of finding the maximum the same way. Setting \\(\\nabla\\ell(\\beta) = 0\\) says there is no leftover association between any covariate and the mistakes \u2014 every column of \\(X\\) is used up.\nLet\u2019s see that at work. Figure\u00a04 stands at three candidate values of \\(\\beta_0\\) \u2014 one too low, one too high, one right \u2014 and reads the first derivative at each.\n\n\ncode\n# What a single guess tells you. Three candidate values of beta-0 on the\n# intercept-only dispute hill: one too low, one too high, one right. At each we\n# read the FIRST derivative and ask what it says about where the maximum is.\nsy &lt;- sum(dp$dispute); nn &lt;- nrow(dp)\nll0   &lt;- function(b) sy * log(plogis(b)) + (nn - sy) * log(1 - plogis(b))\nd1_0  &lt;- function(b) sy - nn * plogis(b)                 # score\nd2_0  &lt;- function(b) -nn * plogis(b) * (1 - plogis(b))   # Hessian\nb_mle &lt;- qlogis(mean(dp$dispute))\n\nguess &lt;- tibble(b = c(-4.0, b_mle, -2.2),\n                lab = c(\"guess too low\", \"the answer\", \"guess too high\")) %&gt;%\n  mutate(ll = ll0(b), score = d1_0(b))\n\ngb   &lt;- seq(-4.6, -1.8, length.out = 400)\nhill &lt;- tibble(b = gb, ll = ll0(gb), score = d1_0(gb))\n\n# short tangent at each guess, so the slope is visible ON the curve\ntang &lt;- guess %&gt;% rowwise() %&gt;%\n  do(tibble(b = .$b + c(-0.32, 0.32), lab = .$lab,\n            y = ll0(.$b) + d1_0(.$b) * c(-0.32, 0.32))) %&gt;% ungroup()\n\np_top &lt;- ggplot(hill, aes(b, ll)) +\n  geom_line(linewidth = 1.1, color = bu_green) +\n  geom_line(data = tang, aes(b, y, group = lab), color = bu_black,\n            linewidth = 0.9, linetype = \"dotted\") +\n  geom_point(data = guess, aes(b, ll), size = 3, color = bu_lightgreen) +\n  geom_vline(xintercept = b_mle, linetype = \"dashed\", color = bu_grey) +\n  annotate(\"text\", x = -4.05, y = ll0(-4.0), hjust = 1.05, vjust = 1.9, size = 3.2,\n           color = bu_black, label = \"guess too low:\\nstill climbing\") +\n  annotate(\"text\", x = b_mle, y = ll0(b_mle), vjust = -0.9, size = 3.2,\n           color = bu_black, label = \"the answer: flat\") +\n  annotate(\"text\", x = -2.35, y = ll0(-2.2), hjust = 1, vjust = 2.4, size = 3.2,\n           color = bu_black, label = \"guess too high:\\nalready falling\") +\n  coord_cartesian(ylim = c(min(hill$ll), max(hill$ll) + 190)) +\n  labs(title = \"Stand at a guess and look at the slope\",\n       subtitle = \"dotted lines are the tangent -- the first derivative -- at three candidate values\",\n       x = NULL, y = \"log-likelihood\") +\n  theme_minimal()\n\np_bot &lt;- ggplot(hill, aes(b, score)) +\n  geom_hline(yintercept = 0, color = bu_black, linewidth = 0.5) +\n  geom_line(linewidth = 1.1, color = bu_lightgreen) +\n  geom_point(data = guess, aes(b, score), size = 3, color = bu_green) +\n  geom_vline(xintercept = b_mle, linetype = \"dashed\", color = bu_grey) +\n  geom_segment(data = guess %&gt;% filter(lab != \"the answer\"),\n               aes(x = b, xend = b, y = 0, yend = score),\n               color = bu_grey, linetype = \"dotted\") +\n  annotate(\"text\", x = -4.0, y = d1_0(-4.0), vjust = -1.0, size = 3.2, color = bu_black,\n           label = \"score &gt; 0: the answer is to the RIGHT\") +\n  annotate(\"text\", x = -2.2, y = d1_0(-2.2), vjust = 1.9, size = 3.2, color = bu_black,\n           label = \"score &lt; 0: the answer is to the LEFT\") +\n  annotate(\"text\", x = b_mle, y = 0, vjust = -1.2, hjust = -0.05, size = 3.2,\n           color = bu_black, label = \"score = 0: stop\") +\n  labs(title = \"Read that slope as a number: the score\",\n       subtitle = \"what matters here is where the curve CROSSES zero, not the curve's own slope\",\n       x = expression(\"intercept,\" ~ beta[0]), y = \"score (first derivative)\") +\n  theme_minimal()\n\np_top / p_bot\n\n\n\n\n\n\n\n\nFigure\u00a04\n\n\n\n\n\nIn the top panel we stand at each of the three guesses and look at the slope of the hill underneath us \u2014 the dotted tangent lines. At \\(\\beta_0 = -4\\) the hill is still rising, so the answer is further right. At \\(-2.2\\) it is falling, so we have gone past and the answer is back to the left. At the dashed line the tangent is flat, and there is no direction left that improves anything, so we\u2019re at a flat point, hopefully a maximum.\nThe bottom panel is the same first derivative but presented as a number rather than a tangent line: this is the score at each of the three values of \\(\\beta\\). This is a plot of the first derivatives, NOT of the likelihood (compare the y-axes in the two panels). What matters in the bottom panel is where the curve crosses zero, not whether the curve itself looks flat. The score is a steeply sloping line through most of this range \u2014 that\u2019s fine and expected, since the slope of the hill really is changing fast. The estimate is the single place the score\u2019s value hits zero. A positive score says the maximum is to the right; a negative score says it is to the left; a zero score says stop."
  },
  {
    "objectID": "likelihood26.html#the-hessian-and-information-how-much-the-data-actually-tell-us",
    "href": "likelihood26.html#the-hessian-and-information-how-much-the-data-actually-tell-us",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "The Hessian and information: how much the data actually tell us",
    "text": "The Hessian and information: how much the data actually tell us\nThe second derivative of the log-likelihood at \\(\\beta\\) is called the Hessian. It counts how much each observation tells us about \\(\\beta\\); adding that up over the sample is what produces the standard errors. It measures the curvature of the function at \\(\\beta\\) - it measures information, and indicates precision.\nThe Hessian, \\(H(\\beta)\\) is the rate at which the slope itself changes, which is the curvature of the hill at the point we\u2019re standing on - the log-likelihood value on the function associated with the values of \\(\\beta\\) we guessed. There is a curvature for every pair of coefficients, so it is a \\(k \\times k\\) matrix, not a vector. For the logit it works out to\n\\[\nH(\\beta) = -\\sum_{i=1}^{n}\\pi_i(1-\\pi_i)\\,x_i'x_i \\;=\\; -X'WX,\n\\qquad W = \\mathrm{diag}\\big(\\pi_i(1-\\pi_i)\\big).\n\\]\nThat sum explains the word information. Each observation contributes one term: its covariates (\\(x_i'x_i\\)) scaled by the weight \\(\\pi_i(1-\\pi_i)\\). You should note that \\(\\pi_i(1-\\pi_i)\\) is the variance of the binomial, so we\u2019re weighting by variances. Two things determine how much curvature the sample delivers:\n\nHow much the covariates move. A column that barely varies contributes almost nothing to \\(x_i'x_i\\), and a column that doesn\u2019t vary at all contributes nothing whatsoever \u2014 there is no information about a coefficient in a variable that never changes.\nHow uncertain the model is about each row. The weight \\(\\pi_i(1-\\pi_i)\\) is largest \u2014 \\(0.25\\) \u2014 when \\(\\pi_i = 0.5\\), and it falls toward zero as \\(\\pi_i\\) approaches either 0 or 1. A row the model already calls at \\(\\pi = 0.99\\) contributes essentially nothing: it is telling us what we already know. The rows that teach us about \\(\\beta\\) are the ones the model finds hard to call.\n\nBecause the terms add over observations, information accumulates with the sample: more rows, more curvature, sharper hill. That is the mechanical reason big samples give small standard errors.\nIf we take the negative of this matrix, \\(-H = X'WX\\), the result is called the information matrix. It is a running total of how the sample informs us about \\(\\beta\\).\nIf \\(-H\\) measures how much we know, then its inverse measures how much we don\u2019t \u2014 and that is the variance of the guess at \\(\\beta\\). It\u2019s important to understand the second derivative (Hessian) provides these measures of uncertainty.\nFigure\u00a05 draws the weight \u2014 the second ingredient \u2014 and then shows where the dispute data actually sit on it.2\n\n\ncode\n# How much does one observation contribute to the information? The weight\n# pi*(1-pi) peaks at pi = .5 and vanishes at either extreme. The rug shows the\n# fitted probabilities from the real dispute logit -- disputes are rare, so\n# nearly every row sits in the low-weight corner on the left.\n\nm_full &lt;- glm(dispute ~ lncaprat + border + deml, family = binomial, data = dp)\npi_i   &lt;- fitted(m_full)\nw_i    &lt;- pi_i * (1 - pi_i)\n\ncurve_d &lt;- tibble(p = seq(0.001, 0.999, length.out = 400), w = p * (1 - p))\n\np_w &lt;- ggplot(curve_d, aes(p, w)) +\n  geom_line(linewidth = 1.1, color = bu_green) +\n  geom_vline(xintercept = 0.5, linetype = \"dotted\", color = bu_black) +\n  annotate(\"text\", x = 0.5, y = 0.295, size = 3.4, color = bu_black,\n           label = \"a coin-flip row carries the most: 0.25\") +\n  annotate(\"text\", x = 0.99, y = 0.012, size = 3.4, color = bu_black, hjust = 1,\n           label = \"a near-certain row carries almost nothing\") +\n  coord_cartesian(ylim = c(0, 0.32)) +\n  labs(title = \"What one observation contributes\",\n       subtitle = expression(\"the weight \" ~ pi[i](1 - pi[i]) ~ \" in \" ~ X*minute*W*X),\n       x = expression(\"fitted probability,\" ~ pi[i]), y = \"weight\") +\n  theme_minimal()\n\np_rug &lt;- ggplot(tibble(p = pi_i), aes(p)) +\n  geom_histogram(bins = 60, fill = bu_lightgreen, color = NA) +\n  geom_vline(xintercept = 0.5, linetype = \"dotted\", color = bu_black) +\n  annotate(\"text\", x = 0.20, y = Inf, vjust = 1.8, hjust = 0, size = 3.4, color = bu_black,\n           label = \"almost every dyad-year is\\na near-certain zero\") +\n  coord_cartesian(xlim = c(0, 1)) +\n  labs(title = \"Where the dispute data actually sit\",\n       subtitle = \"fitted probabilities from the three-covariate logit\",\n       x = expression(\"fitted probability,\" ~ pi[i]), y = \"dyad-years\") +\n  theme_minimal()\n\np_w + p_rug\n\n\n\n\n\n\n\n\nFigure\u00a05\n\n\n\n\n\nTogether the panels show a fact the sample size conceals. There are 20,990 dyad-years in dp, but disputes are rare, so nearly every row is one the model calls a near-certain zero and contributes almost no weight. The weights sum to 875.6, against the 5,248 we would have if every row were a coin flip \u2014 under a fifth of the curvature the row count advertises. The sample is large; the information in it is a good deal smaller than large. More on this when we get to rare events.\nThe second derivative does not lend itself to that kind of picture \u2014 there is no tangent to draw, and the useful thing about it is not its shape but its sign and its size. Take the sign first. It is negative everywhere on this likelihood, which is what guarantees the flat spot the score finds is a peak rather than a trough, and that there is only one of them. (If it went positive somewhere, the score could cross zero more than once and \u201cset the derivative to zero\u201d would stop identifying a unique answer. For the binomial it cannot happen.) The size is what the rest of this section is about, and Figure\u00a05 is the closest thing to a picture of it: not the second derivative itself, but the per-observation weight that the sum in \\(-X'WX\\) is built from.\nThat the weights are strictly positive is what makes this true in general: it makes \\(X'WX\\) positive definite and \\(H\\) negative definite everywhere, so there is one hill, one top, and no false summits. Note the qualifier above \u2014 when it exists. There are data configurations for which no top exists at all, and we take those up later in the term.\nOne last thing to know about the Hessian - it is the source of every standard error in the model:\n\\[\n\\widehat{\\text{SE}}(\\widehat\\beta) = \\sqrt{\\text{diag}\\big[(-H)^{-1}\\big]} = \\sqrt{\\text{diag}\\big[(X'WX)^{-1}\\big]},\n\\]\nthe square roots of the diagonal of the negated, inverted Hessian. Nothing new has to be computed and no extra assumption gets made; the uncertainty was already sitting inside the curvature. The next section is about why that is so, and what it means.\n\n\n\n\n\n\nTake away\n\n\n\n\nThe score \\(\\nabla\\ell(\\beta)\\) is the slope of the log-likelihood, one entry per coefficient. Each entry is the leftover association between one covariate and what the model is currently getting wrong.\nSetting the score to zero says there is nothing left in any column of \\(X\\) that the model hasn\u2019t used \u2014 the same condition OLS imposes with \\(X'e = 0\\).\nThe Hessian measures curvature, a \\(k \\times k\\) matrix because there is a curvature for every pair of coefficients. Negate it and it is the information matrix: it measures how much this sample has to say about \\(\\beta\\).\nInformation accumulates over observations, and each observation\u2019s contribution is weighted by \\(\\pi_i(1-\\pi_i)\\). Rows the model already calls confidently teach us almost nothing; the informative rows are the ones it can\u2019t call.\nThese two derivatives are what any numerical method, Newton\u2013Raphson included, runs on \u2014 and they also stand on their own: between them they say where the estimate is and how much to trust it."
  },
  {
    "objectID": "likelihood26.html#footnotes",
    "href": "likelihood26.html#footnotes",
    "title": "Likelihood Theory & Estimation Mechanics",
    "section": "Footnotes",
    "text": "Footnotes\n\n\nIn much of what follows, I\u2019m using notation from King (1989).\u21a9\ufe0e\nI used Claude Code to build figures 5 and 6 in this slide deck.\u21a9\ufe0e"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Maximum Likelihood, Fall 2026",
    "section": "",
    "text": "This is the course website for PLSC 606J, Maximum Likelihood Estimation, Fall 2026. The course is an advanced course in data science focusing on maximum likelihood methods and their applications in political science.\n\nSyllabus\nSlides\nCode\n\n\n\n\n Back to top"
  },
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About",
    "section": "",
    "text": "About this site:\nCreated with Quarto.\nAbout me:\nProfessor of political science, PhD Florida State 1999. I\u2019ve taught this class for a long time, but never the same way twice. I study models of political violence, collect data on protests and repression.\n\n\n\n Back to top"
  },
  {
    "objectID": "llfmax26.html",
    "href": "llfmax26.html",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "",
    "text": "The likelihood notes began to introduce how we produce maximum likelihood estimates:\n\nguess some values for the \\(\\beta\\)s.\nevaluate the log-likelihood at those values of \\(\\beta\\).\nevaluate the first derivative of the log-likelihood function at that log-likelihood value.\nevaluate the second derivative of the function at that log-likelihood value.\n\nSo what do we do with these parts to generate estimates? We use those two derivatives to determine how far we are from a maximum and what direction we need to move to go to that maximum. We use that information to make another, more informed guess at the values of \\(\\beta\\), and we do this again.\nThese notes describe the general algorithm, the Newton-Raphson method. Newton-Raphson is a numerical optimization method originating with Newton\u2019s root finding technique; it iterates over increasingly refined guesses at \\(\\beta\\) until guesses are not producing different, improved estimates.\nThis allows us to execute the second line of the machine we developed in the likelihood notes:\n\\[\n\\begin{aligned}\n\\textbf{choose:}\\;&\\underbrace{\\text{data}}_{\\text{spreadsheet}} \\;\\to\\;\n\\underbrace{X,\\; y}_{n \\times k,\\; n \\times 1} \\;\\to\\;\n\\underbrace{\\theta}_{\\text{what we want}} \\;\\to\\;\n\\underbrace{\\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\,f(y \\mid \\theta)\\,}}_{\\textbf{swaps}} \\;\\to\\;\n\\underbrace{\\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\,F\\,}}_{\\textbf{swaps}} \\\\[12pt]\n\\textbf{compute:}\\;&\\underbrace{X\\beta}_{n \\times 1} \\;\\to\\;\n\\underbrace{\\widehat\\theta = F(X\\beta)}_{n \\times 1} \\;\\to\\;\n\\underbrace{\\ell(\\beta)}_{\\text{one number}} \\;\\to\\;\n\\underbrace{\\nabla\\ell,\\; H}_{k \\times 1,\\; k \\times k} \\;\\to\\;\n\\underbrace{(-H)^{-1}}_{k \\times k} \\;\\to\\;\n\\underbrace{\\text{SE}}_{k \\times 1} \\;\\to\\; \\text{QI}\n\\end{aligned}\n\\]\nYou\u2019ll recognize the gradient (or score) \\(\\nabla\\ell\\), and the Hessian - the two derivatives from the likelihood notes.\nWe\u2019ll continue to work with the binomial log-likelihood for familiarity, but recall the generality of the machine. Newton-Raphson operates the same way regardless of the log-likelihood function we use, so it too is incredibly general.\nWe\u2019ve previously written down a set of steps to build the likelihood based on the data we have. Now, we\u2019ll write a second \u201crecipe\u201d describing the maximization process (started above). This is the process by which we use the data, the model, the log-likelihood, and the two derivatives to generate \\(\\beta\\) and the standard errors.\n\n\n\n\n\n\nMaximization using Newton-Raphson\n\n\n\n\nChoose starting values for \\(\\beta\\), call them \\(\\beta^{(0)}\\). Any values will do; all zeros is the usual choice, and it\u2019s the one we use below.\nEvaluate the likelihood at those starting values. Form \\(X\\beta^{(t)}\\), push it through the link to get the fitted values, and from those compute the first and second derivatives of the log-likelihood: the score or gradient (which direction is up toward a maximum) and the Hessian (how sharply the surface is curving).\nTake a step. Feed the score and the Hessian into the Newton\u2013Raphson update rule, which returns a new set of values, \\(\\beta^{(t+1)}\\).\nCheck whether we\u2019re done. There are two tests here and it is worth keeping them apart, because they ask different questions. (a) Has the answer stopped moving? Compare \\(\\beta^{(t+1)}\\) with \\(\\beta^{(t)}\\); if the change is effectively zero, another step isn\u2019t buying us anything. (b) Is the surface actually flat here? Evaluate the score at the new values; at a maximum every element of \\(\\nabla\\ell\\) should be zero, or near enough. Test (b) is the mathematical condition for a maximum \u2014 it is \u201cset the derivative to zero,\u201d checked numerically. If we\u2019re done, those values are \\(\\widehat\\beta\\), the maximum likelihood estimates.\nIf the change isn\u2019t small enough, go back to step 2 with the new values in hand and do it again.\nWhen the loop stops, take the standard errors from the Hessian at the answer: invert it, negate it, and take the square roots of the diagonal.\n\n\n\nThat list is the map for everything that follows. The rest of this section explains where the rule in step 3 comes from and what its two halves do; then we run steps 1 through 3 once, by hand; then we hand the whole thing to a loop and watch steps 4 and 5 run it to convergence."
  },
  {
    "objectID": "llfmax26.html#where-the-method-comes-from",
    "href": "llfmax26.html#where-the-method-comes-from",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Where the method comes from",
    "text": "Where the method comes from\nNewton\u2013Raphson is a general method for solving equations numerically, and it predates statistics by about two centuries: Newton described a version of it in the 1660s, and Joseph Raphson published the cleaner iterative form we still use in 1690. It has been used ever since for anything where an equation resists algebra \u2014 computing square roots by hand, locating planetary orbits, solving systems in engineering. Its use is very general.\nThe idea behind it is this. If you can evaluate a function at a point, and you can evaluate its slope at that point, then you can pretend the function is a straight line, follow that line to where it would cross zero, and jump there. The pretense is wrong, so you don\u2019t land on the answer \u2014 but you land closer than you were, and you can do it again from the new point. Repeat until the jumps are so small that they don\u2019t matter.\nMost estimation software uses Newton-Raphson as the basis for optimization. Stata\u2019s ml uses a modified Newton\u2013Raphson as its default. R\u2019s glm runs iteratively reweighted least squares, which for the logit is these same steps. Other methods you\u2019ll see include Fisher scoring, and BFGS. All are based in Newton-Raphson method."
  },
  {
    "objectID": "llfmax26.html#iteration",
    "href": "llfmax26.html#iteration",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Iteration",
    "text": "Iteration\nPreviously, we\u2019ve studied the two derivatives of the binomial log-likelihood function. The first derivative (gradient) is:\n\\[\n\\nabla\\ell(\\beta) \\;=\\;  X'(y - \\pi).\n\\]\nand the second derivative (Hessian) is:\n\\[\nH(\\beta) =  -X'WX,\n\\qquad W = \\mathrm{diag}\\big(\\pi_i(1-\\pi_i)\\big).\n\\]\nThe general form of the Newton-Raphson (for each step) is:\n\\[\n\\beta^{(t+1)} \\;=\\; \\beta^{(t)} \\;-\\; H\\big(\\beta^{(t)}\\big)^{-1}\\,\\nabla\\ell\\big(\\beta^{(t)}\\big),\n\\]\nwhich holds for any log-likelihood at all (emphasizing the generality of the machine). Now substitute the two derivatives above, \\(\\nabla\\ell(\\beta) = X'(y - \\pi)\\) and \\(H(\\beta) = -X'WX\\):\n\\[\n\\begin{aligned}\n\\beta^{(t+1)} &= \\beta^{(t)} - \\big(-X'WX\\big)^{-1}X'(y-\\pi) \\\\[6pt]\n              &= \\bbox[#A7DA92, 3pt, border: 1px solid #005A43]{\\;\\beta^{(t)} + (X'WX)^{-1}X'(y - \\pi)\\;},\n\\qquad W = \\mathrm{diag}\\big(\\pi_i(1-\\pi_i)\\big).\n\\end{aligned}\n\\]\nThe two minus signs cancelled which is why the update ends up adding the correction rather than subtracting it.\nThis is the Newton-Raphson step we iterate to find the values of \\(\\beta\\) associated with the maximum of the likelihood function. Note that the parts are familiar. \\(\\pi\\) is \\(F(X\\beta)\\) from inside the log-likelihood, \\(X'(y-\\pi)\\) is its first derivative, and \\(W\\) collects its second derivative. The log-likelihood is not missing from the update rule. It is the only thing in it."
  },
  {
    "objectID": "llfmax26.html#which-way-and-how-far",
    "href": "llfmax26.html#which-way-and-how-far",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Which way, and how far",
    "text": "Which way, and how far\nSo what does the Newton-Raphson step mean? This is usually described as \u201chill climbing.\u201d To reach the top of the hill, we need to know two things: which way is the top? How far away is the top? The gradient tells us which direction is up. The Hessian tells us how sharply the slope is changing, and therefore how far to go without stepping over the top: sharply curved, step conservatively; nearly flat, step boldly.\n\\[\n\\beta^{(t+1)} = \\beta^{(t)} + \\underbrace{(X'WX)^{-1}}_{\\text{how far}}\\underbrace{X'(y-\\pi)}_{\\text{which way}},\n\\]\nWe iterate this function until the score is essentially zero. The gradient points the direction and the Hessian scales the size of our steps up the hill.\nLet\u2019s focus just briefly on why the Hessian-supplied size of the step (\u201chow far\u201d) is so important. Figure\u00a01 shows two hills with an identical slope at the current guess, so the score prescribes the same direction up the hill on both.1\n\n\ncode\n# Same slope at the starting point, different curvature. The score says \"go\n# right\" in both panels and says it equally loudly. Only the second derivative\n# knows that the sharp hill's top is close and the flat hill's top is far.\n\npeak  &lt;- 4\nslope &lt;- 3\n\none_hill &lt;- function(a, ttl, sub, col) {\n  start &lt;- peak - slope / (2 * a)                 # point where the slope equals `slope`\n  step  &lt;- slope / (2 * a)                        # the Newton step from there\n  g     &lt;- seq(peak - 6, peak + 6, length.out = 400)\n  d     &lt;- tibble(b = g, y = -a * (g - peak)^2)\n  tg    &lt;- tibble(b = g, y = -a * (start - peak)^2 + slope * (g - start))\n\n  ggplot(d, aes(b, y)) +\n    geom_line(data = tg, aes(b, y), color = bu_grey, linewidth = 0.7) +\n    geom_line(linewidth = 1.1, color = col) +\n    geom_point(x = start, y = -a * (start - peak)^2, size = 3, color = bu_black) +\n    geom_vline(xintercept = peak, linetype = \"dotted\", color = bu_black) +\n    annotate(\"segment\", x = start, xend = start + step, y = -34, yend = -34,\n             arrow = arrow(length = unit(0.18, \"cm\"), type = \"closed\"), color = bu_black) +\n    annotate(\"text\", x = start + step / 2, y = -34, vjust = -0.7, size = 3.4,\n             color = bu_black, label = paste0(\"step = \", round(step, 2))) +\n    coord_cartesian(xlim = c(peak - 6, peak + 4), ylim = c(-38, 3)) +\n    labs(title = ttl, subtitle = sub, x = expression(beta), y = \"log-likelihood\") +\n    theme_minimal()\n}\n\none_hill(2.0, \"Sharp hill: step small\",\n         \"steep curvature -- the top is close, so don't overshoot\", bu_green) +\none_hill(0.3, \"Flat hill: step big\",\n         \"gentle curvature -- the top is far, so cover ground\", bu_lightgreen)\n\n\n\n\n\n\n\n\nFigure\u00a01\n\n\n\n\n\nThe grey tangent has the same slope in both panels, so the gradient says the same thing in both \u2014 and says nothing about distance. What separates them is the second derivative. On the sharp hill the slope falls away fast, so the peak is near and a large step would sail over it. On the flat hill the slope barely changes, so the peak is far and we need to take a larger step. That is the job of \\((X'WX)^{-1}\\) \u2014 the inverted Hessian \u2014 telling us how far to travel.\nI\u2019ll work the whole way through on the dispute data, using the same model we carried through the likelihood notes: dispute onset as a function of the logged capability ratio, shared border, and the lower democracy score in the dyad."
  },
  {
    "objectID": "llfmax26.html#example---calculating-one-step",
    "href": "llfmax26.html#example---calculating-one-step",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Example - calculating one step",
    "text": "Example - calculating one step\nLet\u2019s take a sample of the dispute data small enough to look at. Disputes are rare \u2014 about 4.5% of dyad-years \u2014 so a plain random draw of ten rows would almost certainly come back all zeros, and a logit with no ones in it has nothing to estimate. So I sample deliberately: four rows with a dispute, six without. That gives us a design matrix \\(X\\) and an outcome vector \\(y\\) we can print in full and check by hand:\n\n\ncode\nset.seed(20260610)\nsl &lt;- bind_rows(\n  dp %&gt;% filter(dispute == 1) %&gt;% slice_sample(n = 4),\n  dp %&gt;% filter(dispute == 0) %&gt;% slice_sample(n = 6)\n)\n\nX &lt;- model.matrix(~ lncaprat + border + deml, data = sl)   # 10 x 4\ny &lt;- sl$dispute                                            # 10 x 1\n\ntibble(y, X)\n\n\n# A tibble: 10 x 2\n       y X[,\"(Intercept)\"] [,\"lncaprat\"] [,\"border\"] [,\"deml\"]\n   &lt;dbl&gt;             &lt;dbl&gt;         &lt;dbl&gt;       &lt;dbl&gt;     &lt;dbl&gt;\n 1     1                 1         0.322           1        -3\n 2     1                 1         0.952           0        -8\n 3     1                 1         0.315           1         2\n 4     1                 1         1.11            0        -8\n 5     0                 1         2.73            0        -8\n 6     0                 1         2.42            0        10\n 7     0                 1         7.12            0        10\n 8     0                 1         2.48            0        -7\n 9     0                 1         3.29            0        -7\n10     0                 1         2.07            0        -9\n\n\nNow take one step, by hand, on numbers small enough to look at. The Newton\u2013Raphson step for the logit, once more:\n\\[\n\\underbrace{\\beta^{(t+1)}}_{\\text{next guess}} \\;=\\;\n\\underbrace{\\beta^{(t)}}_{\\text{current guess}} \\;+\\;\n\\underbrace{(X'WX)^{-1}\\,X'(y - \\pi)}_{\\text{the correction}},\n\\qquad W = \\mathrm{diag}\\big(\\pi_i(1-\\pi_i)\\big).\n\\]\nI\u2019ll reiterate this is a formula for one step, the next guess, written entirely in terms of the guess we already have. Everything on the right is either data (\\(X\\), \\(y\\)) or computable from the current guess (\\(\\pi\\), \\(W\\)) \u2014 nothing there requires knowing the answer.\nThe pieces:\n\n\\(\\beta^{(t)}\\) is where we are now, and the superscript is just a counter. Start anywhere; \\(\\beta = 0\\) is usual and is what we use below.\n\\(y - \\pi\\) is the model\u2019s mistakes. Run our current guess, \\(\\beta^{(t)}\\), through the likelihood to get \\(X\\beta^{(t)}\\), transform through the link, \\(\\Lambda\\) and we get \\(\\pi\\). This is what the current guess predicts, giving us one predicted probability per row. Subtract it from \\(y\\) and you have one mistake per row, large and positive where we saw a dispute the model called unlikely. This is the only place the observed outcome enters the step.\n\\(X'(y - \\pi)\\) turns those mistakes into a verdict on each coefficient. Multiplying by \\(X'\\) weights each row\u2019s mistake by that row\u2019s covariate values and sums, one number per covariate. If the rows where a covariate is large are the rows being under-predicted, that entry is positive: raise this coefficient. This is the which way part, and it is the score \\(\\nabla\\ell\\).\n\\((X'WX)^{-1}\\) converts direction into distance \u2014 the how far part. The weights \\(W_i = \\pi_i(1-\\pi_i)\\) are largest at \\(\\pi_i = 0.5\\) and shrink toward zero as \\(\\pi_i\\) approaches 0 or 1: a row the model already calls confidently has little to teach us.2\n\nLet our first set of guesses be \\(\\beta = 0\\). Every linear prediction is then zero, so every \\(\\pi_i\\) is \\(\\Lambda(0) = 0.5\\) \u2014 the model knows nothing and puts even odds on everything \u2014 and every weight is at its maximum of \\(0.25\\), so every row counts equally. Let\u2019s run this by hand:\n\n\ncode\nb0 &lt;- rep(0, ncol(X))\npi &lt;- plogis(as.vector(X %*% b0))      # fitted probs at b = 0: all 0.5, n x 1\nW  &lt;- pi * (1 - pi)                     # weights, n x 1\nscore &lt;- t(X) %*% (y - pi)             # k x 1\nnegH  &lt;- t(X) %*% (X * W)              # X'WX, k x k\nb1 &lt;- b0 + solve(negH, score)          # one Newton step\n\nround(as.vector(b1), 4)\n\n\n[1]  0.5673 -0.5569  1.6137  0.0072\n\n\nThese are our new estimates of the vector \\(\\beta\\), but they\u2019re just the next step - we would need to assess whether we\u2019ve reached the top of the hill, and if not, use these as the new \u201ccurrent guess\u201d and estimate another step.\nThat step was in four dimensions (four \\(\\beta\\)s). Here is the same thing looking for just one \\(\\beta\\) so I can draw the first two steps. This is an intercept-only model of disputes; let\u2019s start again with \\(\\beta_0 = 0\\):\n\n\ncode\n# Two Newton steps, drawn. At each guess the tangent is the score and the\n# curvature is the Hessian; those two numbers fit a parabola, and the step jumps\n# to the top of THAT parabola, not the top of the hill. Refit at the new guess\n# and go again -- the second step is much shorter than the first.\n\n\nsy &lt;- sum(dp$dispute); nn &lt;- nrow(dp)\nll0  &lt;- function(b) sy * log(plogis(b)) + (nn - sy) * log(1 - plogis(b))  # two-part ll\nd1_0 &lt;- function(b) sy - nn * plogis(b)                 # score\nd2_0 &lt;- function(b) -nn * plogis(b) * (1 - plogis(b))   # Hessian (a scalar here)\n\nb_start &lt;- 0\nstep1   &lt;- -d1_0(b_start) / d2_0(b_start)\nb_next  &lt;- b_start + step1\nstep2   &lt;- -d1_0(b_next) / d2_0(b_next)\nb_next2 &lt;- b_next + step2\nb_mle   &lt;- qlogis(mean(dp$dispute))\n\n# two more steps, quoted in the prose below but not drawn\nstep3   &lt;- -d1_0(b_next2) / d2_0(b_next2)\nstep4   &lt;- -d1_0(b_next2 + step3) / d2_0(b_next2 + step3)\n\ngb   &lt;- seq(-4.2, 0.6, length.out = 400)\nhill &lt;- tibble(b = gb, ll = ll0(gb))\n\n# tangent (the score) and fitted parabola at any guess\ntangent  &lt;- function(b0, w = 0.9) {\n  g &lt;- seq(b0 - w, b0 + w, length.out = 60)\n  tibble(b = g, y = ll0(b0) + d1_0(b0) * (g - b0))\n}\nparabola &lt;- function(b0) tibble(b = gb, y = ll0(b0) + d1_0(b0) * (b - b0) +\n                                            0.5 * d2_0(b0) * (b - b0)^2)\n# height of the parabola's own peak -- where the step lands before we climb back to the hill\np_top &lt;- function(b0, st) ll0(b0) + d1_0(b0) * st + 0.5 * d2_0(b0) * st^2\n\nggplot(hill, aes(b, ll)) +\n  geom_line(data = tangent(b_start), aes(b, y), color = bu_grey, linewidth = 0.7) +\n  geom_line(data = tangent(b_next),  aes(b, y), color = bu_grey, linewidth = 0.7) +\n  geom_line(linewidth = 1.1, color = bu_green) +\n  # the parabolas are drawn ON TOP of the hill on purpose. Near the point each\n  # one is fitted at, the parabola and the hill agree to within a fraction of a\n  # log-likelihood unit -- so if the hill is painted over them, the dashed curve\n  # vanishes exactly where it matters and looks like it misses the start point.\n  geom_line(data = parabola(b_start), aes(b, y), color = bu_lightgreen,\n            linewidth = 0.9, linetype = \"dashed\") +\n  geom_line(data = parabola(b_next), aes(b, y), color = bu_palegreen,\n            linewidth = 0.9, linetype = \"dashed\") +\n  annotate(\"segment\", x = b_next, xend = b_next, y = p_top(b_start, step1), yend = ll0(b_next),\n           color = bu_grey, linewidth = 0.4, linetype = \"dotted\") +\n  annotate(\"segment\", x = b_next2, xend = b_next2, y = p_top(b_next, step2), yend = ll0(b_next2),\n           color = bu_grey, linewidth = 0.4, linetype = \"dotted\") +\n  geom_point(x = b_next,  y = p_top(b_start, step1), size = 2.2, color = bu_lightgreen) +\n  geom_point(x = b_next2, y = p_top(b_next,  step2), size = 2.2, color = bu_palegreen) +\n  geom_point(x = b_start, y = ll0(b_start), size = 3, color = bu_black) +\n  geom_point(x = b_next,  y = ll0(b_next),  size = 3, color = bu_black) +\n  geom_point(x = b_next2, y = ll0(b_next2), size = 3, color = bu_black) +\n  geom_vline(xintercept = b_mle, linetype = \"dotted\", color = bu_black) +\n  annotate(\"segment\", x = b_start, xend = b_next, y = -13300, yend = -13300,\n           arrow = arrow(length = unit(0.18, \"cm\"), type = \"closed\"), color = bu_black) +\n  annotate(\"text\", x = (b_start + b_next) / 2, y = -13300, vjust = -0.7,\n           size = 3.4, color = bu_black, label = paste0(\"step 1: \", round(step1, 2))) +\n  annotate(\"segment\", x = b_next, xend = b_next2, y = -14300, yend = -14300,\n           arrow = arrow(length = unit(0.18, \"cm\"), type = \"closed\"), color = bu_black) +\n  annotate(\"text\", x = (b_next + b_next2) / 2, y = -14300, vjust = -0.7,\n           size = 3.4, color = bu_black, label = paste0(\"step 2: \", round(step2, 2))) +\n  annotate(\"text\", x = b_start, y = ll0(b_start), hjust = 1.35, vjust = 0.2,\n           size = 3.4, color = bu_black, label = \"start\") +\n  annotate(\"text\", x = -4.25, y = -5900, hjust = 0, size = 3.1, color = bu_black,\n           label = \"parabola refit after step 1\") +\n  annotate(\"text\", x = -3.1, y = -9500, hjust = 1, size = 3.1, color = bu_black,\n           label = \"parabola fit at the start\") +\n  annotate(\"text\", x = b_next, y = ll0(b_next), hjust = 1.15, vjust = 1.6,\n           size = 3.2, color = bu_black, label = \"after step 1\") +\n  annotate(\"text\", x = b_next2, y = ll0(b_next2), hjust = 1.1, vjust = -1.1,\n           size = 3.2, color = bu_black, label = \"after step 2\") +\n  annotate(\"text\", x = b_mle, y = -14900, hjust = -0.08, size = 3.4, color = bu_black,\n           label = paste0(\"MLE = \", round(b_mle, 2))) +\n  coord_cartesian(xlim = c(-4.3, 0.65), ylim = c(-15600, -3000)) +\n  labs(title = \"Two Newton steps on the intercept-only hill\",\n       subtitle = \"grey = the tangent (the score) at each guess;  dashed = the parabola Newton fits there;  each step lands at that parabola's peak\",\n       x = expression(\"intercept,\" ~ beta[0]), y = \"log-likelihood\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a02\n\n\n\n\n\nAt \\(\\beta_0 = 0\\) we measure the slope (the grey tangent \u2014 the score) and how fast it is changing (the curvature \u2014 the Hessian). Those two numbers fit exactly one parabola through the point, drawn in bright dashed green. Notice that it starts on the hill: at \\(\\beta_0 = 0\\) the parabola and the log-likelihood have the same height and the same slope, which is why the dashed curve runs along the hill near the start and only peels away once we get some distance from it. Newton jumps to the top of that parabola, not of the hill \u2014 a move of -1.82. You can see the gap between the likelihood function and the parabola, so we\u2019re not there yet. So take another step, generate tangent and curvature at the new guess \u2014 the second grey tangent, the pale parabola \u2014 and jump again: -0.79, less than half the first, landing within 0.45 of the answer.\nThe steps shrink fast because the parabola becomes a better stand-in as we near the top \u2014 up there the hill very nearly is a parabola. Keep going and this one takes steps of -1.82, -0.79, -0.37, -0.07, converging on -3.05.\nThe process, again, is iterative - step, recompute \\(\\pi\\) and \\(W\\), step again, stop when the change is effectively zero \u2014 and the guesses converge on the maximum."
  },
  {
    "objectID": "llfmax26.html#newton-raphson-for-multiple-unknowns",
    "href": "llfmax26.html#newton-raphson-for-multiple-unknowns",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Newton-Raphson for multiple unknowns",
    "text": "Newton-Raphson for multiple unknowns\nLet\u2019s do this once more for a real logit with four coefficients using the same update rule from above, run in a loop. We start somewhere, compute the gradient \\(\\nabla\\ell\\) and the Hessian \\(H\\) at that point, take the step, and repeat until the step stops changing anything \u2014 that last part is the convergence check.\nWe\u2019ll also have the loop record what it\u2019s doing on each pass: the log-likelihood it has reached, the largest element of the gradient \\(\\nabla\\ell\\) (which measures how far from flat, and so how far from the top, we still are), the size of the step it just took, and the coefficients themselves. Storing that history lets us watch the climb after the fact.\n\n\ncode\nX &lt;- model.matrix(~ lncaprat + border + deml, data = dp)\ny &lt;- dp$dispute\n\nloglik &lt;- function(b, X, y) {\n  p &lt;- plogis(as.vector(X %*% b))\n  sum(y * log(p) + (1 - y) * log(1 - p))\n}\n\nb    &lt;- rep(0, ncol(X))      # start at zero\nhist &lt;- list()\nfor (it in 1:50) {\n  p    &lt;- plogis(as.vector(X %*% b))\n  g    &lt;- t(X) %*% (y - p)              # score, k x 1\n  XtWX &lt;- t(X) %*% (X * (p * (1 - p)))  # -H, k x k\n  step &lt;- solve(XtWX, g)\n\n  hist[[it]] &lt;- c(iter = it, loglik = loglik(b, X, y),\n                  max_grad = max(abs(g)), max_step = max(abs(step)),\n                  setNames(b, colnames(X)))\n\n  b &lt;- b + step\n  if (max(abs(step)) &lt; 1e-8) break      # converged\n}\n\n# The loop's XtWX was computed BEFORE the last step, so recompute -H at the\n# final estimates. Here the difference is ~1e-13, but the habit matters: the\n# standard errors belong at b-hat, not one step behind it.\np_hat &lt;- plogis(as.vector(X %*% b))\nXtWX  &lt;- t(X) %*% (X * (p_hat * (1 - p_hat)))\nse    &lt;- sqrt(diag(solve(XtWX)))        # SEs from -H at b-hat\nn_it  &lt;- it\n\n\nIt converges in 8 iterations and recovers exactly what glm reports.\n\n\ncode\nm &lt;- glm(dispute ~ lncaprat + border + deml, family = binomial(\"logit\"), data = dp)\n\ntibble(term = colnames(X),\n       nr_coef  = as.vector(b),   nr_se  = se,\n       glm_coef = coef(m),        glm_se = sqrt(diag(vcov(m)))) %&gt;%\n  mutate(across(where(is.numeric), \\(x) round(x, 5)))\n\n\n# A tibble: 4 x 5\n  term        nr_coef   nr_se glm_coef  glm_se\n  &lt;chr&gt;         &lt;dbl&gt;   &lt;dbl&gt;    &lt;dbl&gt;   &lt;dbl&gt;\n1 (Intercept) -3.24   0.103    -3.24   0.103  \n2 lncaprat    -0.203  0.0238   -0.203  0.0238 \n3 border       0.783  0.0852    0.783  0.0852 \n4 deml        -0.0784 0.00681  -0.0784 0.00681\n\n\nFigure\u00a04 shows what those fifteen lines actually did: four coefficients, all starting at zero, all walking to their estimates at once.\n\n\ncode\n# Every coefficient's journey from 0 to its estimate. They move TOGETHER -- one\n# step of the algorithm updates the whole vector, which is what it means for the\n# Hessian to be a k x k matrix rather than k separate numbers.\nlog_tbl &lt;- as_tibble(do.call(rbind, hist))\n\npaths &lt;- log_tbl %&gt;%\n  select(iter, all_of(colnames(X))) %&gt;%\n  pivot_longer(-iter, names_to = \"term\", values_to = \"estimate\") %&gt;%\n  mutate(term = factor(term, levels = colnames(X)))\n\nfinals &lt;- tibble(term = factor(colnames(X), levels = colnames(X)),\n                 estimate = as.vector(b))\n\nggplot(paths, aes(iter, estimate)) +\n  geom_hline(data = finals, aes(yintercept = estimate),\n             linetype = \"dashed\", color = bu_black) +\n  geom_line(linewidth = 0.9, color = bu_green) +\n  geom_point(size = 1.8, color = bu_green) +\n  facet_wrap(~ term, scales = \"free_y\") +\n  labs(title = \"All four coefficients climb at once\",\n       subtitle = \"dashed line = the converged estimate; every coefficient starts at 0\",\n       x = \"iteration\", y = \"estimate\") +\n  theme_minimal()\n\n\n\n\n\n\n\n\nFigure\u00a04\n\n\n\n\n\nLook at how little happens after the first few passes. Every coefficient makes almost its entire journey in the first three or four iterations and then settles onto the dashed line. That pattern \u2014 enormous early progress, then fine-tuning \u2014 is characteristic, and the reason is visible in the update rule itself. The step is the gradient divided by the curvature. As we climb, the gradient collapses toward zero far faster than the curvature changes, so the ratio shrinks fast. The steps get small not because the hill gets steeper near the top, but because the slope runs out."
  },
  {
    "objectID": "llfmax26.html#footnotes",
    "href": "llfmax26.html#footnotes",
    "title": "Maximizing the Log-Likelihood Function",
    "section": "Footnotes",
    "text": "Footnotes\n\n\nI used Claude Code to generate Figures 1, 2, 3, and 7 in these slides.\u21a9\ufe0e\nThe correction term should look familiar: \\((X'WX)^{-1}X'(y-\\pi)\\) has the shape of \\((X'X)^{-1}X'y\\), with the mistakes replacing the \\(y\\) variable. Rescale those mistakes by the weights and it is exactly a weighted least-squares regression of them on \\(X\\) \u2014 which is why the algorithm glm runs is called iteratively reweighted least squares.\u21a9\ufe0e"
  }
]
